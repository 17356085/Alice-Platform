# Skill: bug-analysis

## 目标
将失败现象转化为标准化 Bug 分析结论。支持 RAG 自动匹配已知问题。

## 输入
- 失败日志
- 截图
- 复现步骤
- 代码上下文
- CI 结果

## 输出
- Bug 分析报告
- 根因分类
- 修复建议
- 回归建议

## 🔴 MANDATORY: 执行流程（不可跳过）

### L0：自动 RAG 已知问题匹配（🔴 必须执行，不可跳过）

在分析任何失败用例之前，**必须先调用 RAG 检索已知问题库**：

1. **提取错误签名**（对每个失败的测试用例）：
   - 异常类型（如 `NoSuchElementException`, `TimeoutException`, `AssertionError`, `ElementClickInterceptedException`）
   - 关键报错信息（如 “el-cascader”, “loading”, “遮罩”, “teleport”）
   - 失败的页面/模块名称
   - 涉及的 Element Plus 组件名

2. **调用 MCP Tool `rag_search_known_issues`**：
   ```json
   {
     “query”: “<异常类型> <报错关键信息> <涉及组件名> <页面名称>”,
     “n_results”: 5
   }
   ```

3. **对返回结果分类**：
   - 若 `distance < 0.5`（极高相似度）→ 🟢 **直接匹配**，引用已知坑位的解决方案，跳过 L1 深度分析
   - 若 `distance < 1.0`（高相似度）→ 🟡 **可能已知问题**，参考已知坑位作为分析方向
   - 若 `distance >= 1.0` 或无结果 → 🔵 未匹配，正常进入 L1 深度分析

4. **将匹配结果写入 BUG_ANALYSIS 报告的「已知问题匹配」栏**：
   ```markdown
   ## 已知问题匹配
   | 匹配编号 | 标题 | 相似度 | 处理方式 |
   |---------|------|--------|---------|
   | EP-001 | Teleport 渲染 | 0.32 | 🟢 直接匹配，引用解决方案 |
   | EP-003 | loading 遮罩 | 0.87 | 🟡 参考方向 |
   ```

### 降级规则

- 如果 MCP Tool `rag_search_known_issues` 调用失败（超时/服务不可用）：
  → 降级为手动读取 `governance/context/known-issues.yaml`
  → 基于关键字做简单匹配（在 YAML 中搜索异常类型/组件名/症状关键词）
  → 在报告中标注「⚠️ RAG 不可用，使用降级匹配」
- 如果 known-issues.yaml 也不可读：
  → 跳过已知问题匹配，直接进入深度分析
  → 在报告中标注「⚠️ 已知问题匹配不可用」

### L1：深度分析（仅在 RAG 未直接匹配时执行）
执行标准 5 层递进排查：定位器失效 → 等待不足 → 数据问题 → 环境问题 → 产品Bug

## 规则
- **L0 (RAG 自动匹配)**：失败后优先检索 known_issues 向量库 → 匹配已知坑位 → 直接引用解决方案
- 区分现象、证据、根因、建议
- 不能确认的结论必须标记”待验证”
- 必须输出影响范围与回归范围
- L0 步骤在任何情况下都不可跳过（即使 AI 认为”已知问题不相关”也必须执行检索）

## 依赖
- templates/bug-analysis.template.md
- workflows/automation-failure-closure.md
- aitest.rag_engine（RAG 向量检索）
- MCP Tool: `rag_search_known_issues`（aitest-tools server）

---

## Prompt 模板

> 以下 Prompt 可直接复制到 AI 对话中使用。替换 `{{ }}` 占位符即可。

### 单个自动化失败根因分析

```text
你是一个测试自动化失败分析专家。请分析以下自动化用例失败的原因。

## 🔴 L0：RAG 已知问题匹配（必须执行，不可跳过）

在分析前，必须调用 MCP Tool `rag_search_known_issues`：
  查询 = "{异常类型} {报错关键信息} {涉及组件名} {页面名称}"
  参数: n_results=5

→ 如果 distance < 0.5（极高相似度）：🟢 直接匹配已知坑位，引用解决方案，标记为「已知问题」
→ 如果 distance < 1.0（高相似度）：🟡 参考已知坑位作为分析方向
→ 如果 distance >= 1.0 或无结果：🔵 进入 L1-L5 标准排查

**降级规则**：
- RAG 不可用时 → 手动读取 `governance/context/known-issues.yaml` → 关键字匹配
- known-issues.yaml 也不可读时 → 跳过匹配，标注「⚠️ 匹配不可用」

## 失败信息
- 用例名称：{{test_search_by_name}}
- 失败阶段：{{搜索操作 / 断言验证}}
- 错误信息：
```
{{粘贴 pytest 错误输出}}
```

## 附件
- 失败截图：{{上传截图}}
- 页面HTML（失败时）：{{粘贴 HTML 片段}}
- Console日志：{{粘贴浏览器Console日志}}
- 测试代码：
```python
{{粘贴失败用例代码}}
```

## 任务
按以下结构分析，输出 BUG_ANALYSIS.md：

1. **现象描述**：发生了什么
2. **复现分析**：
   - 是否可稳定复现？（复现率：X/5）
   - 什么条件下触发？
3. **根因定位**（从高到低排查）：
   - [ ] 元素定位器失效？（页面结构变化/动态ID）
   - [ ] 等待时间不足？（Vue异步渲染未完成）
   - [ ] 测试数据问题？（数据被删除/数据冲突）
   - [ ] 环境问题？（服务不可用/网络波动）
   - [ ] 产品Bug？（实际是功能缺陷）
4. **责任归属**：
   - 🐛 产品Bug → 提交禅道
   - 🔧 脚本问题 → 修复定位器/等待策略
   - 🌐 环境问题 → 联系运维
   - 📊 数据问题 → 更新测试数据
5. **修复建议**：具体代码修改
6. **回归影响**：其他哪些用例可能受影响？

## 约束
- 区分现象、证据、根因、建议
- 不能确认的结论必须标记"待验证"
- 必须输出影响范围与回归范围
```

### 批量自动化失败分析

```text
分析以下批量自动化执行结果，找出系统性失败原因。

## 执行结果
```
{{粘贴 pytest 执行摘要 或 Allure 报告概览}}
总用例：{{N}} | 通过：{{N}} | 失败：{{N}} | 跳过：{{N}}
```

## 失败用例列表
{{粘贴所有失败用例的名称和错误信息}}

## 任务
1. **失败分类**：按失败原因将用例分组
   - 同类定位器失效：{{N}} 条
   - 同类等待不足：{{N}} 条
   - 同类数据问题：{{N}} 条
   - 环境问题：{{N}} 条
2. **模式识别**：
   - 是否集中在某个模块？
   - 是否集中在某个时间点（如某次部署后）？
   - 是否有共同的根因？
3. **优先级排序**：按影响面排列修复顺序
4. **修复计划**：逐类给出修复方案和预估工作量
5. **预防措施**：如何避免同类问题再次发生
```

---

## 检查清单

### 单个 Bug 分析
- [ ] 现象描述清晰（什么用例、什么阶段、什么错误）
- [ ] 复现率明确（X/5）
- [ ] 5 层根因排查全部执行（定位器→等待→数据→环境→产品Bug）
- [ ] 责任归属明确（产品/脚本/环境/数据）
- [ ] 修复建议具体到代码行
- [ ] 回归影响范围已评估
- [ ] 不确定的结论标注"待验证"

### 批量失败分析
- [ ] 失败用例按原因分组归类
- [ ] 模式识别：是否集中模块/时间点/共同根因
- [ ] 按影响面排列修复优先级
- [ ] 每类失败有修复方案 + 预估工时
- [ ] 有预防措施（CI 前置检查、定位器加固等）

---

## 产出物
→ `BUG_ANALYSIS.md`，存放至 `artifacts/`。
→ 输出格式参见 `templates/bug-analysis.template.md`。
