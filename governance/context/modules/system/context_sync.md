好的，明白了。你需要为 `system` 模块生成或完善其 `project.yaml` 和 `MODULE_INDEX.md`。

请提供该模块的以下信息，以便我生成准确的内容：

1.  **模块名称 (name)**: 例如 `system` 或其他更具体的名称。
2.  **项目URL (url)**: 该模块的代码仓库地址。
3.  **模块列表 / 结构**: `system` 模块下有哪些子模块或主要文件/目录？请列出它们的名称和简短描述（如果需要描述的话）。
4.  **关键文件**: 该模块根目录下有哪些重要的配置文件或文档（如 `config.yaml`, `README.md` 等）？

一旦你提供了这些信息，我将为你生成 `/governance/context/projects/system/project.yaml` 和 `/governance/context/projects/system/MODULE_INDEX.md`。