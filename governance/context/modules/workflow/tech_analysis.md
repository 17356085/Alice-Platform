审批链管理页面 → Element Plus 组件
├── 审批链列表 → el-table + el-pagination
├── 新建/编辑对话框 → el-dialog + el-form
├── 审批节点配置 → el-tree / el-transfer
├── 条件规则 → el-select + el-input
├── 审批人选择 → el-transfer + el-avatar
└── 状态标签 → el-tag