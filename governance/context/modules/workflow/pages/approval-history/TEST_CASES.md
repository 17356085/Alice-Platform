好的，我将根据您提供的 `PAGE_CONTEXT.md` 和 `RISK_MODEL.md` 的分析（虽然未直接提供RISK_MODEL，但基于页面上下文的设计），为 `approval-history` 页面设计测试用例。

**设计思路：**
1.  **严格遵循规则**：确保每个用例包含所有必要字段，P0用例覆盖核心查询、展示、分页、操作流程。
2.  **覆盖场景**：从页面上下文中提炼出核心的测试场景（初始状态、筛选、分页、操作、异常），并转化为具体的测试用例。
3.  **正反结合**：为每个主要功能点设计正向（成功）和反向（失败/边界）用例。
4.  **数据驱动**：在用例中明确指出需要的数据条件，参考页面上下文中的“测试数据设计要点”。

---

# TEST_CASES.md：审批历史页面测试用例

## 1. 用例总览
- **模块**：`workflow`
- **页面**：`approval-history`
- **总用例数**：15
- **P0 (核心)**：8
- **P1 (重要)**：4
- **P2 (一般)**：2
- **P3 (异常)**：1

## 2. 详细测试用例

| 用例ID | 标题 | 前置条件 | 步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_AH_001** | 验证页面首次加载的默认状态和数据 | 1. 用户已登录并有访问此页面的权限。 <br> 2. 测试数据库中存在多条状态、时间、发起人各异的审批记录。 | 1. 直接访问审批历史页面URL。 <br> 2. 观察页面加载过程。 | 1. 页面加载完成后，所有筛选控件（时间范围、状态、类型、发起人）显示为默认值。 <br> 2. 列表（`#approvalListTable`）加载出数据。 <br> 3. 加载动画（`#loadingSpinner`）显示并随后消失。 <br> 4. 分页组件（`#pagination`）显示“共X条”，且X > 0。 | P0 |
| **TC_AH_002** | 验证“重置”按钮功能 | 1. 页面已加载，且列表显示初始数据。 | 1. 修改任意筛选条件（如：将时间范围改为“最近7天”）。 <br> 2. 点击“查询”按钮，等待列表刷新完成。 <br> 3. 点击“重置”按钮。 | 1. 所有筛选控件恢复为页面初始加载时的默认值。 <br> 2. 列表数据恢复为与步骤0（首次加载）完全一致的状态。 <br> 3. 分页信息（如“共X条”）恢复为初始值。 | P0 |
| **TC_AH_003** | 验证使用快捷时间范围筛选查询 | 1. 页面已加载。 <br> 2. 数据库中存在“最近7天”内发起的记录。 | 1. 在时间范围控件（`#timeRangeSelector`）中，选择快捷选项“最近7天”。 <br> 2. 点击“查询”按钮（`#searchBtn`）。 | 1. 查询过程中，“查询”按钮显示为loading状态或禁用，防止重复点击。 <br> 2. 加载动画出现。 <br> 3. 列表刷新，仅显示发起时间在最近7天内的记录。 <br> 4. 分页总数“共X条”更新为符合条件的记录数。 | P0 |
| **TC_AH_004** | 验证自定义时间范围查询（有效范围） | 1. 页面已加载。 <br> 2. 数据库中存在指定时间范围内的记录。 | 1. 在时间范围控件中，通过日期选择器设置开始日期为“2024-01-15”，结束日期为“2024-01-31”。 <br> 2. 点击“查询”按钮。 | 1. 列表刷新，仅显示发起时间在“2024-01-15 00:00”至“2024-01-31 23:59”之间的记录。 <br> 2. 所有记录的时间字段格式符合“YYYY-MM-DD HH:mm”。 | P0 |
| **TC_AH_005** | 验证单个状态筛选查询 | 1. 页面已加载。 <br> 2. 数据库中存在“已通过”状态的记录。 | 1. 在状态筛选控件（`#statusFilter`）中，单选“已通过”。 <br> 2. 点击“查询”按钮。 | 1. 列表刷新，仅显示状态为“已通过”的记录。 <br> 2. 状态列显示统一的“已通过”标签样式。 | P0 |
| **TC_AH_006** | 验证多条件组合筛选查询 | 1. 页面已加载。 <br> 2. 数据库中存在满足特定组合条件的记录（如：由“张伟”在“2024-01-15”发起的“已通过”的“报销审批”）。 | 1. 设置时间范围：开始日期“2024-01-15”，结束日期“2024-01-15”。 <br> 2. 设置状态筛选：选择“已通过”。 <br> 3. 设置类型筛选：选择“报销审批”。 <br> 4. 在发起人搜索框（`#initiatorSearch`）中输入“张伟”，并从联想列表中选中。 <br> 5. 点击“查询”按钮。 | 1. 列表刷新，仅显示**同时满足**以下所有条件的记录：<br>   - 发起时间为2024-01-15全天 <br>   - 状态为“已通过” <br>   - 类型为“报销审批” <br>   - 发起人为“张伟”。 <br> 2. 查询结果正确反映了多个条件的交集。 | P0 |
| **TC_AH_007** | 验证列表数据展示的完整性 | 1. 页面已加载，且列表中有数据。 | 1. 随机选取列表中的几行数据。 <br> 2. 检查每一列的内容。 | 1. 表头名称与预期设计一致。 <br> 2. 每条记录的关键信息（如审批ID、标题、发起人、时间、状态、类型）均完整显示，无截断或乱码。 <br> 3. 时间字段格式统一为“YYYY-MM-DD HH:mm”。 <br> 4. 状态字段显示对应的样式标签（如不同颜色）。 | P0 |
| **TC_AH_008** | 验证分页功能及数据切换 | 1. 页面已加载，且总记录数大于每页显示条数（如默认10条）。 | 1. 观察当前为第一页，记录下本页的最后一条记录摘要。 <br> 2. 点击分页组件的“下一页”按钮。 <br> 3. 观察分页组件的页码高亮变化。 <br> 4. 记录下新一页的最后一条记录摘要。 | 1. 点击后，加载动画出现。 <br> 2. 列表刷新，显示第二页的数据。 <br> 3. 分页组件的页码“2”高亮。 <br> 4. 第二页的数据与第一页**不重复**（例如，最后一条记录摘要不同）。 <br> 5. “上一页”按钮变为可用状态。 | P0 |
| **TC_AH_009** | 验证点击“详情”跳转及数据一致性 | 1. 页面已加载，且列表中有数据。 | 1. 在列表中找到一条记录，记下其审批ID和标题。 <br> 2. 点击该记录操作列的“详情”按钮。 | 1. 页面发生跳转（或弹出详情层）。 <br> 2. 新页面/层的URL或标题包含步骤1中记下的审批ID。 <br> 3. 详情页中展示的标题与步骤1中记下的标题一致。 | P0 |
| **TC_AH_010** | 验证“导出”功能及文件内容 | 1. 页面已加载。 <br> 2. 设置一些筛选条件（如：时间范围、状态）。 | 1. 点击“导出”按钮。 <br> 2. 等待浏览器下载文件完成。 <br> 3. 打开导出的文件（如Excel）。 | 1. 浏览器正常触发文件下载，无错误。 <br> 2. 文件中的数据列数、列名与列表表头一致。 <br> 3. 文件中的数据行数与列表下方显示的“共X条”一致。 <br> 4. 文件中的数据内容（特别是时间、状态）与当前列表视图中显示的数据完全一致。 | P1 |
| **TC_AH_011** | 验证发起人搜索框的联想与选择 | 1. 页面已加载。 | 1. 在发起人搜索框（`#initiatorSearch`）中输入部分字符，如“张”。 <br> 2. 观察联想列表。 <br> 3. 从联想列表中选择一个选项（如“张伟”）。 <br> 4. 点击“查询”按钮。 | 1. 输入后，显示包含“张”的所有发起人联想列表。 <br> 2. 选择后，搜索框中显示“张伟”。 <br> 3. 查询结果仅包含发起人为“张伟”的记录。 | P1 |
| **TC_AH_012** | 验证查询无结果时的空状态展示 | 1. 页面已加载。 | 1. 设置一个极端的筛选组合，使其无法匹配任何记录（如：选择一个不存在的发起人，并指定一个很久远的过去时间）。 <br> 2. 点击“查询”按钮。 | 1. 列表（`#approvalListTable`）无数据行。 <br> 2. 显示空状态提示区域（`#emptyState`），提示“暂无数据”或类似信息。 <br> 3. 分页组件显示“共0条”。 | P1 |
| **TC_AH_013** | 验证页码跳转的输入校验 | 1. 页面已加载，且总页数 > 1。 | 1. 在分页组件的页码输入框中，输入非数字字符（如“abc”），并回车。 <br> 2. 输入大于实际总页数的数字（如999），并回车。 | 1. 输入非数字时，系统进行拦截，不执行跳转，或提示错误。 <br> 2. 输入超出范围的页码时，系统应跳转到最后一页，或提示页码无效。 | P1 |
| **TC_AH_014** | 验证自定义时间范围的边界与无效输入 | 1. 页面已加载。 | 1. 通过日期选择器，设置结束日期早于开始日期（如：开始“2024-01-20”，结束“2024-01-10”）。 <br> 2. 尝试点击“查询”按钮。 | 1. 日期选择器应禁止选择早于开始日期的结束日期，或在选择时给予提示。 <br> 2. 如果允许输入，点击“查询”时，系统应提示错误（如“结束日期不能早于开始日期”），且不发起查询请求。 | P2 |
| **TC_AH_015** | 验证网络异常时的提示与恢复 | 1. 打开浏览器开发者工具，切换到“网络”面板。 | 1. 在页面已加载状态下，修改筛选条件并点击“查询”。 <br> 2. 在请求发出后，通过开发者工具将网络状态设置为“Offline”或拦截相关API请求（`/api/approval/list`）并返回错误（如500）。 <br> 3. 观察页面变化。 | 1. 页面不应白屏或卡死。 <br> 2. 应出现友好的错误提示信息（如“网络异常，请稍后重试”）。 <br> 3. 提示信息可关闭，页面恢复为异常前的状态（或允许重试）。 | P2 |

## 3. 测试数据准备清单（供执行时参考）
- **基础数据池**：准备 >100条审批记录。
- **状态覆盖**：`待审批`、`审批中`、`已通过`、`已驳回` 各 ≥20条。
- **发起人设计**：
    - `张伟`：10条记录（跨多种状态和时间）。
    - `张伟明`：5条记录。
    - `王伟`：1条记录（用于筛选后可能无结果的验证）。
- **类型覆盖**：`请假审批`、`报销审批`、`采购审批` 等类型数据。
- **组合数据**：至少设计2条高价值特定记录（如“张伟”在“2024-01-15”发起的“已通过”的“报销审批”）。

## 4. 用例与页面上下文验证点映射
- **V-01, V-02** -> **TC_AH_001**
- **V-03, V-04** -> **TC_AH_003, TC_AH_004, TC_AH_005, TC_AH_006**
- **V-05** -> **TC_AH_012**
- **V-06** -> **TC_AH_002**
- **V-07, V-08** -> **TC_AH_008**
- **V-09** -> **TC_AH_003** (按钮状态)
- **V-10** -> **TC_AH_010**
- **V-11** -> **TC_AH_009**
- **V-12** -> **TC_AH_015**