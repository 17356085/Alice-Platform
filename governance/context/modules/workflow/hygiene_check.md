好的，我将根据提供的规则，对 **workflow** 模块下的 **approval-chain** 页面进行治理文档完整性检查。

**检查路径假设：** `governance/context/projects/{id}/` （由于未指定项目ID，我将以一个通用路径进行说明）

---

### **治理文档完整性检查报告**

**检查目标：**
- 项目ID: `my-project` (示例)
- 模块: `workflow`
- 页面: `approval-chain`

**检查结果：**

| 文件路径 | 状态 | 优先级 | 说明 |
| :--- | :--- | :--- | :--- |
| `governance/context/projects/my-project/project.yaml` | **[缺失]** | P0 | 项目核心配置文件，定义全局上下文、元数据和依赖关系。缺失此文件将导致项目治理结构不完整。 |
| `governance/context/projects/my-project/workflow/MODULE_CONTEXT.md` | **[缺失]** | P0 | `workflow` 模块的核心上下文文档，描述其业务目标、边界、核心实体和与其他模块的关系。缺失此文件将导致模块间协作和开发理解困难。 |
| `governance/context/projects/my-project/workflow/approval-chain/PAGE_CONTEXT.md` | **[缺失]** | P1 | `approval-chain` 页面的专属上下文文档，详细描述其用户流程、业务规则、状态机、数据模型及UI交互规范。缺失此文件会导致页面实现和维护缺乏明确依据。 |

---

### **缺失文件清单与建议**

**结论：** **关键治理文档完全缺失。** 当前检查路径下未发现任何要求的标准文档。

**优先处理建议 (按优先级排序)：**

1.  **【P0 - 立即创建】`project.yaml`**
    *   **作用：** 作为整个项目的“宪法”，定义项目名称、描述、负责人、模块列表、技术栈、环境配置等全局信息。所有模块和页面的文档应由此衍生和关联。
    *   **行动：** 必须首先创建此文件，为后续所有文档建立基础和索引。

2.  **【P0 - 立即创建】`workflow/MODULE_CONTEXT.md`**
    *   **作用：** 阐明 `workflow` 模块存在的业务价值、它需要解决的核心问题（如流程自动化）、它所管理的核心实体（如流程定义、任务实例）、以及它与其他模块（如用户权限、通知）的接口约定。
    *   **行动：** 在项目结构中创建 `workflow` 目录，并编写此模块级上下文文档。这是理解整个模块的入口。

3.  **【P1 - 紧随其后创建】`workflow/approval-chain/PAGE_CONTEXT.md`**
    *   **作用：** 细化到具体页面，明确 `approval-chain`（审批链）功能的业务规则。例如：审批节点的定义（审批人、审批条件）、审批流程的流转逻辑、超时处理、驳回/加签/转办等操作的规范、以及前端界面需要展示的关键信息。
    *   **行动：** 在 `workflow` 目录下创建 `approval-chain` 子目录，并编写此页面级上下文文档。这是开发、测试和产品验收的直接依据。

**后续步骤：**
完成以上文档的创建后，建议进一步检查 `workflow` 模块下的其他页面以及整个项目中其他模块的文档完整性，以形成完整的治理文档体系。