好的，收到您的请求。我已准备好作为**页面分析 (page-analysis) Skill** 为您工作。

您已指定 **所属模块：system-role (系统角色)**。

为了能够精准地为您输出 `PAGE_CONTEXT.md` (页面上下文) 和 `PAGE_ELEMENT_POSITION.md` (元素定位器设计)，请您补充以下任一种信息：

**1. 页面截图（最优）**
请提供该模块下具体页面的 **1-3 张截图**。
*例如：列表页、新增/编辑弹窗页、角色分配权限页等。*

**2. 完整HTML源码**
如果您无法截图，请提供页面关键区域（如搜索区、表格区、弹窗区）的完整HTML源码片段。

**3. 页面URL + 功能描述**
请提供该页面的URL，并简要描述其主要功能（例如：这是一个角色管理页面，包含角色列表、搜索功能、新增/编辑角色弹窗、角色权限配置树）。

---

**示例：请按以下格式提供信息**

> **模块**：system-role
> **页面名称**：角色管理列表页
> **页面URL**：`http://your-domain/system/role`
> **页面截图**：[请上传图片]
> **功能描述**：用于查看、搜索、新建、编辑和删除系统角色，并可为每个角色分配菜单/按钮权限。

在您提供信息后，我将：
1.  输出包含**搜索区、表格区、弹窗区、权限点**等详细元素清单的 `PAGE_CONTEXT.md`。
2.  输出包含 **A级 (data-testid/id/name) > B级 (CSS) > C级 (XPath)** 三级定位器设计的 `PAGE_ELEMENT_POSITION.md`。
3.  自动识别 **Element Plus** 具体组件（如 `el-select`、`el-tree`、`el-table`）。
4.  标注所有需要 **WebDriverWait** 等待策略的场景。

请开始提供页面详情。