好的，作为Web端测试工程师，我将基于您提供的Page Object代码、测试脚本和页面上下文，进行风险建模并产出业务场景模型。

---

## RISK_MODEL — lab / water-indicator

> **版本**: 1.0 | **最后更新**: 2026-06-18 | **风险数量**: 9 (P0: 0, P1: 4, P2: 5)

### 1. 业务风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| WATER-INDICATOR-BIZ-001 | **数据准确性风险** – 页面展示的设计指标（如pH阈值、COD限值）与实际配置不符，误导化验员依此进行水质判定。 | P1 | 自动化验证关键指标（至少5条）的阈值与API返回一致；建立上游配置变更的回归触发机制。 | 未覆盖（test_wi_02仅取第2列指标名称，未校验具体值） |
| WATER-INDICATOR-BIZ-002 | **数据不完整风险** – 指标缺失（如缺少关键指标“总磷”），导致化验员缺乏参考标准。 | P2 | 配置一个确定的指标列表基线与页面渲染结果做比对。 | 未覆盖（现有用例仅断言行数>0，未对比预期集） |
| WATER-INDICATOR-BIZ-003 | **业务含义误解风险** – 表头或备注描述不清（如“规则”列含义模糊），用户误读标准。 | P2 | UI/文案可测性审查；自动化检查表头是否与PRD一致。 | test_wi_01检查表头包含5个字段，但未精确匹配字段顺序和完整集合 |

### 2. 权限风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| WATER-INDICATOR-PERM-001 | **越权访问风险** – 无化验室权限的用户（如工艺操作员）通过URL直接访问 `#/lab/water/indicator`，看到敏感指标数据（如企业水质控制标准）。 | P1 | 添加路由守卫自动化测试：使用无权限的session访问页面，断言跳转或被拒绝。 | 未覆盖（现有用例均使用具有权限的Session） |
| WATER-INDICATOR-PERM-002 | **角色数据隔离风险** – “指标名称”列为权限敏感字段，部分角色（如外包人员）不应看到完整指标列表，但页面可能暴露所有22行数据。 | P1 | 模拟低权限角色登录，检查指标名称列内容是否被截断/隐藏，或行数是否减少。 | 未覆盖 |

### 3. 数据风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| WATER-INDICATOR-DATA-001 | **空数据状态风险** – 数据库无配置或初始化时，页面应显示友好的空状态提示（如“暂无数据”），而非空白表格或崩溃。 | P2 | 准备空数据环境（例如测试专用租户），验证`get_empty_text()`返回非空且包含“暂无”等中文。 | 未覆盖（现有用例仅在`row_count>0`不成立时检查empty_text，但未设计独立空数据场景） |
| WATER-INDICATOR-DATA-002 | **边界值风险** – 阈值列可能出现极端值（如`-9999`、`99999`、`0`）、浮点精度问题（如`6.5-8.5`显示为`6.500000-8.500000`）。 | P2 | 在专用测试数据中植入边界值，验证页面显示格式正确（保留1位小数、无多余0）。 | 未覆盖 |
| WATER-INDICATOR-DATA-003 | **数据注入风险** – 后端如果未做转义，指标名称或备注可能包含XSS payload（如`<script>alert(1)</script>`），页面渲染时被浏览器执行。 | P1 | 自动化验证：将包含`<`、`>`、`"`的指标名称写入数据库后（通过API或DB），检查页面是否已将特殊字符实体编码显示。 | 未覆盖 |

### 4. 接口风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| WATER-INDICATOR-API-001 | **接口超时/失败风险** – GET指标列表接口超时或返回500，页面应显示错误提示或重试按钮，而非无限loading。 | P1 | 使用mock服务模拟接口超时/500，验证页面显示友好错误提示，且不阻塞其他页面功能。 | 未覆盖 |
| WATER-INDICATOR-API-002 | **异常数据格式风险** – 后端返回不符合定义的JSON结构（如`threshold`字段为对象而非字符串），页面表格渲染错乱或JS报错。 | P2 | 自动化测试中增加对接口返回的schema校验，或通过mock注入异常数据验证前端容错。 | 未覆盖 |
| WATER-INDICATOR-API-003 | **Token过期风险** – 用户长时停留页面后Token失效，发起刷新请求时失败，页面显示无数据而非引导重新登录。 | P2 | 设计Session过期场景：先登录等待超过token有效期，再操作（刷新页面），验证页面跳转到登录页或提示重新登录。 | 未覆盖 |

### 5. UI/UX风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| WATER-INDICATOR-UI-001 | **加载状态显示风险** – 页面加载缓慢时，`el-loading`或骨架屏未显示，用户误以为页面空白。 | P2 | 自动化验证：等待页面初始化完成前，检查`el-loading`是否已显示（通过`is_element_exist`）。 | 未覆盖 |
| WATER-INDICATOR-UI-002 | **跨分辨率布局风险** – 在1920x1080下正常，但在1366x768或4K缩放下表格溢出或列宽异常，导致部分字段不可见。 | P2 | 在无头模式下切换视口尺寸，检查表格所有列的文字是否完整显示（不被截断）。 | 未覆盖 |

### 6. 性能风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| WATER-INDICATOR-PERF-001 | **大数据量（>500行）渲染性能风险** – 尽管当前22行，未来可能扩展上千行，前端虚表滚动卡顿。 | P2 | 使用mock大数据集（500行）验证页面加载时间<3s，滚动流畅。 | 未覆盖 |
| WATER-INDICATOR-PERF-002 | **高频切换页面风险** – 用户频繁在气体/水质设计指标页间切换，页面未正确销毁/重建导致内存泄漏。 | P2 | 编写循环navigate切换测试，监控浏览器内存是否持续增长（可结合`window.performance`或浏览器DevTools协议）。 | 未覆盖 |

---

## BUSINESS_SCENARIOS — lab / water-indicator

> **版本**: 1.0 | **最后更新**: 2026-06-18 | **关联RISK_MODEL版本**: 1.0

### 1. 业务目标 (Business Goal)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WTR-001 | 业务目标 | **核心目标**：为化验室人员提供水质分析设计指标的参考依据，确保分析结果判定有标可依。 | 化验员, 质量管理员 | water-indicator (仅此页) | - | ✅ |
| BS-WTR-002 | 业务目标 | **次要目标**：质量管理人员可快速查阅当前水质控制的预设标准，用于审核化验数据的合规性。 | 质量管理员, 部门主管 | water-indicator | - | ✅ |
| BS-WTR-003 | 业务目标 | **非目标**：本页面不提供指标的创建、修改、删除能力，不管理权限分配，不提供历史对比。 | 所有角色 | - | - | ✅ |
| BS-WTR-004 | 业务目标 | **扩展目标（待确认）**：可能支持导出指标清单供外部审计。 | 审计员, 管理员 | 未知导出组件 | - | ❓ |

### 2. 角色与旅程 (Roles & Journeys)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WTR-101 | 角色旅程 | **化验员旅程**：登录系统 → 左侧菜单点击“化验室取样”→“水质分析”→“水质分析设计指标” → 页面展示22行指标 → 化验员对照当前水样数据逐一比对阈值 → 确认或调整分析结论 → 无任何操作直接离开页面。 | 化验员 | lab-dashboard, water-indicator | - | ✅ |
| BS-WTR-102 | 角色旅程 | **质量管理员旅程**：登录系统 → 进入同一页面 → 快速浏览所有指标阈值是否合理（例如检查COD上限是否调整） → 如有疑问联系系统管理员 → 同一会话可能多次查看。 | 质量管理员 | water-indicator | - | ✅ |
| BS-WTR-103 | 角色旅程 | **无权限用户旅程**：工艺操作员尝试通过收藏夹或URL直接访问 `#/lab/water/indicator` → 页面拦截并提示无权限，或被重定向到首页。 | 工艺操作员 | water-indicator (无权) | WATER-INDICATOR-PERM-001 | ✅ |
| BS-WTR-104 | 角色旅程 | **外包人员旅程**：仅分配“外包查看”角色的用户登录后 → 页面正常打开 → 但“指标名称”列内容被脱敏（例如“COD”显示为“***”），或仅显示部分指标（如10行）。 | 外包查看员 | water-indicator | WATER-INDICATOR-PERM-002 | ⚠️ |

### 3. 业务流程 (Workflows)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WTR-201 | 业务流程 | **Happy Path**：用户正常登录 → 导航到水质分析设计指标页面 → 等待加载完成 → 22行数据正确显示（表头、数据、阈值等） → 用户自行解读后关闭页面。 | 化验员, 质量管理员 | lab-dashboard, water-indicator | - | ✅ |
| BS-WTR-202 | 业务流程 | **Alternative Path – 空数据**：系统初次部署或测试环境无指标配置 → 用户导航至该页面 → 表格显示空状态提示“暂无数据” → 用户理解当前无参考标准，联系管理员配置。 | 化验员 | water-indicator | WATER-INDICATOR-DATA-001 | ✅ |
| BS-WTR-203 | 业务流程 | **Alternative Path – 接口超时**：网络波动导致GET请求超时 → 页面显示loading超过10秒后 → 弹出“加载失败，请重试”的轻提示（或el-alert） → 用户手动刷新页面重试。 | 化验员 | water-indicator | WATER-INDICATOR-API-001 | ⚠️ |
| BS-WTR-204 | 业务流程 | **异常中断 – Token过期**：用户打开页面后离开岗位一段时间（超过token有效期） → 页面之前数据已加载，用户再次点击→页面发起后台自动刷新请求 → 收到401 → 前端跳转至登录页（或弹出登录对话框） → 用户重新登录后回到该页面。 | 化验员 | water-indicator, login-page | WATER-INDICATOR-API-003 | ⚠️ |

### 4. 业务规则 (Business Rules)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WTR-301 | 业务规则 | **规则1 – 指标唯一性**：同一分类下，指标名称必须全局唯一（不可出现两个“pH”行）。此规则由后端维护，页面展示时重复指标应被标记为异常（如高亮红色）。 | 系统管理员 | 后端配置页, water-indicator | WATER-INDICATOR-BIZ-001 | ❓ |
| BS-WTR-302 | 业务规则 | **规则2 – 阈值格式**：阈值列必须遵循格式 `<下限>-<上限>` 或 `≤<值>` 或 `≥<值>`，不允许非法字符（如中文“到”）。后端应校验，前端可做冗余显示格式化。 | 系统管理员 | 后端配置页 | WATER-INDICATOR-DATA-002 | ⚠️ |
| BS-WTR-303 | 业务规则 | **规则3 – 角色权限隔离**：角色 `OBSERVER_EXTERNAL` 只能看到分类为“物理指标”和“化学指标”的行，其余行隐藏；“指标名称”列内容替换为“***”。 | 外包查看员 | water-indicator | WATER-INDICATOR-PERM-002 | ⚠️ |
| BS-WTR-304 | 业务规则 | **规则4 – 数据版本控制（假设）**：指标数据有生效日期与版本号，页面只展示当前生效的版本。历史版本在专门历史记录页查看。 | 质量管理员 | water-indicator, history-page | - | ❓ |

### 5. 数据流 (Data Flow)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WTR-401 | 数据流 | **数据来源**：指标数据由系统管理员在 `#/admin/water-indicator-config` (假设页面) 进行增删改配置，存储在后端数据库 `indicator_definition` 表中。 | 系统管理员 | admin-water-indicator-config | - | ❓ |
| BS-WTR-402 | 数据流 | **数据变换**：前端从API GET `/api/lab/water/indicators` 获取原始数据，无客户端变换，直接渲染到表格。 | 无 | water-indicator | - | ✅ |
| BS-WTR-403 | 数据流 | **数据消费**：化验员根据本页面看到的阈值，在水质分析录入页 `#/lab/water/analysis` 输入允许范围内的实测值；本页面数据不向下游传递，仅作参考。 | 化验员 | water-indicator, water-analysis-new | - | ✅ |
| BS-WTR-404 | 数据流 | **跨页面一致性要求**：若管理员在配置页修改了COD的阈值，则化验员再次访问water-indicator页时应立即看到更新（要求页面设置合理缓存策略TTL或强制刷新）。 | 系统管理员, 化验员 | admin-water-indicator-config, water-indicator | WATER-INDICATOR-BIZ-001 | ⚠️ |

### 6. 业务风险→场景映射 (Risk-to-Scenario Mapping)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WTR-501 | 风险→场景 | **数据准确性场景**：验证页面显示的COD阈值 `≤100` 与API返回 `100` 一致，且显示正确。 | 化验员 | water-indicator | WATER-INDICATOR-BIZ-001 | ✅ |
| BS-WTR-502 | 风险→场景 | **越权访问场景**：使用工艺操作员session访问本页面，验证被重定向至首页或显示无权限。 | 工艺操作员 | water-indicator | WATER-INDICATOR-PERM-001 | ✅ |
| BS-WTR-503 | 风险→场景 | **权限隔离场景**：使用外包查看员session访问本页面，验证指标名称列脱敏，且仅显示部分行（符合规则3）。 | 外包查看员 | water-indicator | WATER-INDICATOR-PERM-002 | ⚠️ |
| BS-WTR-504 | 风险→场景 | **空数据场景**：清理测试库指标数据后访问页面，验证空状态提示出现。 | 化验员 | water-indicator | WATER-INDICATOR-DATA-001 | ✅ |
| BS-WTR-505 | 风险→场景 | **XSS注入场景**：在测试DB中插入包含 `<s>` <bold> 的指标名称，验证页面显示实体编码（如 `&lt;s&gt;`）。 | 系统管理员 | water-indicator | WATER-INDICATOR-DATA-003 | ✅ |
| BS-WTR-506 | 风险→场景 | **接口超时场景**：使用mock让接口响应延迟15秒，验证页面10秒内显示超时提示，并可重试。 | 化验员 | water-indicator | WATER-INDICATOR-API-001 | ⚠️ |
| BS-WTR-507 | 风险→场景 | **Token过期场景**：登录后等待超过token有效期（如30分钟），触发页面刷新，验证跳转登录页。 | 化验员 | water-indicator | WATER-INDICATOR-API-003 | ⚠️ |

---

### 缺失覆盖标注

| 维度 | 缺失场景描述 | 建议覆盖优先级 | 备注 |
|------|--------------|----------------|------|
| 角色旅程 | 管理员配置指标后验证页面实时更新（流通过程） | P1 | 当前无跨页面端到端测试 |
| 业务流程 | 500接口错误时页面提示（非超时） | P2 | mock错误响应 |
| 业务规则 | 重复指标名称时页面异常展示 | P2 | 依赖后端环境准备 |
| 数据流 | 页面数据缓存策略（是否使用localStorage/etag） | P2 | 需确认实现后补充 |

---

以上两份文档已覆盖风险建模的全部6个维度，并关联现有自动化覆盖与缺失场景。BUSINESS_SCENARIOS.md为 `testcase-design` 提供了业务输入，确保后续测试设计从业务目标出发，而非从UI操作出发。