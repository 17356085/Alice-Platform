好的，收到您的输入。基于 `gas-compare` 页面的 Page Object 代码、测试脚本和页面上下文，我已掌握页面的真实结构、交互逻辑与现有测试覆盖范围。

我将启动 **skill: risk-modeling**，并进行 **P2-5** 的增强，产出 `RISK_MODEL.md` 和 `BUSINESS_SCENARIOS.md`。

先进行 **业务场景分析**，再基于场景产出风险模型。

---

### BUSINESS_SCENARIOS.md — 气体分析对比

> **依赖**: PAGE_CONTEXT.md, test_gas_compare.py（分析测试覆盖范围）
> **输入**: lab/gas-compare 页面（双日期选择+查询/重置+对比结果表格）

#### 1. 业务目标 (Business Goal)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-GC-001 | 业务目标 | **核心目标：质检员/工程师选择日期范围，对比不同时间点的气体分析数据，以验证气体质量是否符合标准，或追踪气体组分的变化趋势。** | 质检员, 质量工程师 | gas-compare | RISK-GC-001, RISK-GC-003 | ✅ |
| BS-GC-002 | 业务目标 | **次要目标：在无数据情况下，系统友好提示“暂无数据”，而非抛出页面错误。** | 质检员, 质量工程师 | gas-compare | RISK-GC-004 | ✅ |
| BS-GC-003 | 业务目标 | **非目标：此页面不负责创建或修改气体分析记录，也不支持单个样本的详细查看（详情可能跳转到 gas-record 页面）。** | 质检员, 质量工程师 | gas-compare | - | ⚠️ 推断 |

#### 2. 角色与旅程 (Roles & Journeys)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-GC-004 | 角色旅程 | **质检员旅程**：质检员登录 → 导航至“气体分析对比” → 选择日期范围（如上周） → 点击“查询” → 查看对比表格，确认各项指标偏差是否在允许范围内 → 若发现异常，导出数据或跳转详情。 | 质检员 | login, gas-compare | RISK-GC-002 | ✅ |
| BS-GC-005 | 角色旅程 | **质量工程师旅程**：质量工程师登录 → 查询特定批次或设备的长期气体分析趋势 → 设置较宽日期范围（如过去三个月） → 分析表格数据 → 拒绝或确认质量警报。 | 质量工程师 | login, gas-compare, alarm-monitor | RISK-GC-006 | ⚠️ 推断 |
| BS-GC-006 | 角色旅程 | **多角色协作场景**：质检员发现偏差超标的数据，将对比结果截图或导出，作为附件提交给质量工程师审核。此流程不在本页面内完成，但本页面是数据源。 | 质检员, 质量工程师 | gas-compare, work-order | - | ⚠️ 推断 |
| BS-GC-007 | 角色旅程 | **零数据旅程**：新设备或新批次尚无气体分析记录，质检员查询后，看到的是空表格。系统应正常加载，而非报错。 | 质检员 | gas-compare | RISK-GC-008 | ⚠️ 推断 |

#### 3. 业务流程 (Workflows)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-GC-008 | 业务流程 | **Happy Path - 正常范围查询**: 质检员输入有效的日期范围（开始 < 结束，且范围内有数据），点击查询，系统显示对比表格。 | 质检员 | gas-compare | RISK-GC-001, RISK-GC-006 | ✅ |
| BS-GC-009 | 业务流程 | **Alternative Path - 空返回查询**: 质检员输入一个确定无数据的日期范围，点击查询，系统显示空表格，并提示“暂无数据”。 | 质检员 | gas-compare | RISK-GC-004, RISK-GC-008 | ✅ |
| BS-GC-010 | 业务流程 | **Alternative Path - 无效日期输入**: 质检员输入开始日期 > 结束日期，点击查询，系统应如何处理？(可能前端校验提示、或后端返回空/错误)。 | 质检员 | gas-compare | RISK-GC-005, RISK-GC-009 | ⚠️ 推断 |
| BS-GC-011 | 业务流程 | **Alternative Path - 查询后重置**: 质检员查询了A日期范围后，又输入B日期范围并查询，然后点击重置，表格应清空或恢复到初始状态（全量或空）。 | 质检员 | gas-compare | RISK-GC-003, RISK-GC-009 | ⚠️ 推断 |
| BS-GC-012 | 业务流程 | **异常恢复 - 网络中断**: 查询请求发出后，网络中断。系统应显示超时或网络错误提示，用户在恢复后可以重新查询，不应出现页面挂起。 | 质检员 | gas-compare | RISK-GC-002, RISK-GC-007 | ⚠️ 推断 |

#### 4. 业务规则 (Business Rules)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-GC-013 | 业务规则 | **时间范围校验规则**：开始日期不能大于结束日期。如果违反，应在前端或后端给出清晰的错误提示。 | 质检员 | gas-compare | RISK-GC-005 | ⚠️ 推断 |
| BS-GC-014 | 业务规则 | **数据关联规则**：对比表格展示的数据，是基于选中日期范围内，所有批次的气体分析记录的汇总。 | 质检员 | gas-compare | RISK-GC-006 | ⚠️ 推断 |
| BS-GC-015 | 业务规则 | **表格排序规则**：表格数据通常按日期升序排列。最旧的数据在最上方，最新的在最下方。 | 质检员 | gas-compare | RISK-GC-009 | ⚠️ 推断 |
| BS-GC-016 | 业务规则 | **无数据规则**：如果查询结果为空，系统必须展示“暂无数据”状态，不能显示空白页或报错。 | 质检员 | gas-compare | RISK-GC-008 | ⚠️ 推断 |

#### 5. 数据流 (Data Flow)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-GC-017 | 数据流 | **数据来源**：对比表格的数据来源于 `gas-analysis` 模块中的 `analysis-record` 页面创建的原始记录。 | 质检员 | gas-record, gas-compare | RISK-GC-006 | ⚠️ 推断 |
| BS-GC-018 | 数据流 | **数据变换**：本页面接受两个日期参数，向后端接口 `GET /api/v1/gas-analysis/compare` 发起请求，后端根据需要对比的字段（如标准值、实测值、偏差）进行聚合计算，返回 JSON 数据。 | 质检员 | gas-compare | RISK-GC-006 | ⚠️ 推断 |
| BS-GC-019 | 数据流 | **数据消费**：本页面的对比结果是只读展示。数据可能被“导出”功能消费（但当前 PO 未实现）。 | 质检员 | gas-compare | RISK-GC-003 | ⚠️ 推断 |

#### 6. 业务风险→场景映射 (Risk-to-Scenario Mapping)

> **详情见 `RISK_MODEL.md` 中的关联场景列。**

---

### RISK_MODEL.md — 气体分析对比

> **依赖**: BUSINESS_SCENARIOS.md, PAGE_CONTEXT.md, test_gas_compare.py
> **标记说明**: 
> - **P0**: 阻塞上线，必须修复。
> - **P1**: 严重影响核心体验，必须测试。
> - **P2**: 边缘场景，酌情测试。
> - **覆盖状态**: 关联 `test_gas_compare.py` 中的用例。

#### 1. 业务风险
| 风险ID | 风险描述 | 风险等级 | 关联场景 | 缓解措施/建议 | 现有自动化覆盖 |
|--------|----------|----------|----------|---------------|----------------|
| RISK-GC-001 | **误操作风险**：用户选择了错误的日期范围，导致看到了不相关的对比数据，影响业务决策。 | **P2** | BS-GC-001, BS-GC-008 | 在搜索框附近增加提示文本“支持按日期范围筛选”，并确保日期选择器有清晰的界面反馈。 | ❌ 未覆盖 |
| RISK-GC-002 | **网络中断风险**：查询接口请求过程中网络中断，系统未正确处理超时或报错，导致页面卡死或白屏。 | **P1** | BS-GC-005, BS-GC-012 | ① 前端需实现 `axios/fetch` 的 timeout 处理。 ② 添加 loading 状态和错误提示的 UI 组件。 | ❌ 未覆盖 |
| RISK-GC-003 | **数据一致性风险**：用户查询A日期后，查询B日期，然后点击重置。重置后的表格状态不符合预期（如显示A日期的旧数据，而非重置到全量/空状态）。 | **P1** | BS-GC-003, BS-GC-011, BS-GC-019 | ① 明确“重置”的业务含义：是清空输入框时清空表格，还是清空输入框但表格保持上次结果？ ② 与PM确认后，添加对应测试用例。 ③ 前端需在重置后重新调用接口（如果需要清空表格）。 | ❌ 未覆盖 |

#### 2. 权限风险
| 风险ID | 风险描述 | 风险等级 | 关联场景 | 缓解措施/建议 | 现有自动化覆盖 |
|--------|----------|----------|----------|---------------|----------------|
| RISK-GC-004 | **权限越界风险**：不同角色的质检员，不应看到其他车间的敏感气体分析数据。但页面没有权限过滤，导致数据泄露。 | **P1** | BS-GC-002, BS-GC-009 | ① 后端接口必须校验 `车间ID` 或 `用户组` 权限，只返回当前用户有权限的批次数据。 ② 前端可增加不可见的数据过滤提示。 | ❌ 未覆盖 |
| RISK-GC-005 | **越权访问风险**：普通用户可以通过直接输入 URL `#/lab/gas/compare` 进入页面，即使其在侧边栏不可见。 | **P2** | BS-GC-010 | ① 前端路由守卫应校验用户权限。 ② 如果用户无权限，应重定向到404或无权限页面，而非白屏。 | ❌ 未覆盖 |

#### 3. 数据风险
| 风险ID | 风险描述 | 风险等级 | 关联场景 | 缓解措施/建议 | 现有自动化覆盖 |
|--------|----------|----------|----------|---------------|----------------|
| RISK-GC-006 | **大数据量风险**：选择跨度很大的日期范围（如一年）后，后端返回大量数据（>1000行），导致前端表格加载缓慢或崩溃。 | **P1** | BS-GC-005, BS-GC-008, BS-GC-014, BS-GC-018 | ① 强制后端增加分页。 ② 前端表格使用虚拟滚动 (Virtual Scrolling)。 ③ 在前端查询限制，提示“日期范围超过30天，请缩短范围”。 | ❌ 未覆盖 |
| RISK-GC-007 | **特殊字符风险**：气体名称或批次号中包含特殊字符（如 '; DROP TABLE），开启接口作为查询参数时，存在 SQL 注入风险。 | **P0** | BS-GC-012 | **必须**：后端接口对所有输入参数（日期、批次号等）进行参数化查询，杜绝字符串拼接 SQL。 | ❌ 未覆盖 |
| RISK-GC-008 | **边界值风险 - 空数据**：日期范围内无数据，系统应友好提示，而非直接崩溃。 | **P2** | BS-GC-007, BS-GC-009, BS-GC-016 | 已通过代码 `is_page_loaded()` 和 空状态检测 `el-table__empty-text` 实现。现有测试 `test_gc_02_date_search` 覆盖了此场景。 | ✅ 已覆盖 |

#### 4. 接口风险
| 风险ID | 风险描述 | 风险等级 | 关联场景 | 缓解措施/建议 | 现有自动化覆盖 |
|--------|----------|----------|----------|---------------|----------------|
| RISK-GC-009 | **参数校验风险**：接口未对日期格式进行校验，导致前端传入非法格式（如 `2026/06/12` 而不是 `2026-06-12`）时，后端返回 500 错误。 | **P1** | BS-GC-010, BS-GC-011, BS-GC-015 | ① 前端 `el-date-picker` 强制 `value-format="yyyy-MM-dd"`。 ② 后端增加统一的日期参数校验 (如 `@DateTimeFormat`)。 | ❌ 未覆盖 |
| RISK-GC-010 | **接口超时/失败时的前端表现**：查询接口返回 504 或 500 时，前端应捕获异常并显示友好错误信息，而不是一直转圈。 | **P1** | BS-GC-012 | ① 封装 `request.js` 拦截器处理 HTTP 错误。 ② 增加全局的 `ElMessage.error` 弹窗。 | ❌ 未覆盖 |

#### 5. UI/UX风险
| 风险ID | 风险描述 | 风险等级 | 关联场景 | 缓解措施/建议 | 现有自动化覆盖 |
|--------|----------|----------|----------|---------------|----------------|
| RISK-GC-011 | **性能感受 - Loading**：查询时，页面没有明确的加载指示器（如骨架屏或旋转图标），用户不知道系统是否在响应。 | **P2** | BS-GC-008, BS-GC-012 | 确保 `_wait_loading_gone()` 中检测的加载动画在查询期间是可见的。 | ❌ 未覆盖 |
| RISK-GC-012 | **错误信息不友好**：当查询失败时，浏览器控制台可能打印了错误，但用户界面上没有任何提示。当前代码无错误处理。 | **P1** | BS-GC-002, BS-GC-010, BS-GC-012 | **必须**：在 `click_query()` 和 `set_start_date()` 等方法中，增加 try-except 捕获异常，并调用 `driver.save_screenshot()` 用于调试，同时在前端提示。 | ❌ 未覆盖 |

#### 6. 性能风险
| 风险ID | 风险描述 | 风险等级 | 关联场景 | 缓解措施/建议 | 现有自动化覆盖 |
|--------|----------|----------|----------|---------------|----------------|
| RISK-GC-013 | **频繁搜索**：用户在短时间内（秒级）疯狂点击“查询”按钮，导致后端收到大量重复请求，可能造成数据库压力。 | **P2** | BS-GC-008 | ① 按钮在点击后应变为 `loading` 状态（禁用点击），直到请求完成或超时。 ② 前端实现防抖 (debounce) 或节流 (throttle)。 | ❌ 未覆盖 |

---

### 总结与下一步

- **高风险项**: **RISK-GC-007 (SQL注入)** 是唯一一个 P0 风险，必须由开发修复。**RISK-GC-012 (无错误处理)** 是 P1 风险，需要开发增加前端错误提示机制。
- **自动化覆盖缺口**: 当前的 `test_gas_compare.py` 仅覆盖了 UI 显示和一次最基本的日期搜索。绝大多数 P0/P1 风险（操作、权限、数据、接口、性能）**均未被自动化覆盖**。
- **产出物定位**: `BUSINESS_SCENARIOS.md` 将作为 `testcase-design` 的输入，指导生成覆盖这些风险场景的测试用例。
- **下一步建议**: 对于 P1 风险 `RISK-GC-003 (重置逻辑)` 和 `RISK-GC-006 (大数据量)`，建议与产品经理或后端开发确认具体业务规则后再进行测试设计。