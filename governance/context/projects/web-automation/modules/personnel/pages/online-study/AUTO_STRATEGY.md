好的，我将基于您提供的 `PAGE_CONTEXT.md` 和上下文中的 `TECH_ANALYSIS.md`（片段）生成 `AUTO_STRATEGY.md`。由于缺少 `TEST_CASES.md`，我会根据页面典型业务场景推断 P0-P2 用例并标注原因。

---

```markdown
# 自动化策略：在线学习 (Online Study)

## 模块：personnel | 页面：online-study

---

## 1. 自动化覆盖矩阵

> 基于 PAGE_CONTEXT.md 推断的典型测试用例。请根据实际 TEST_CASES.md 调整优先级。

| 用例编号 (推断) | 标题 | 优先级 | 是否自动化 | 理由 |
|-----------------|------|--------|-----------|------|
| TC-OS-001 | 页面正常加载 — 显示标题、搜索区、表格、分页 | P0 | ✅ | 基础冒烟，验证权限和路由是否正确 |
| TC-OS-002 | 按课程名称搜索 | P0 | ✅ | 核心功能，搜索输入框定位器稳定（`input[placeholder*='课程名称']`） |
| TC-OS-003 | 按课程分类下拉搜索 | P0 | ✅ | 下拉框需展开选择，涉及 `el-select-dropdown`，定位链路上有风险但与搜索组合 |
| TC-OS-004 | 按状态下拉搜索 | P0 | ✅ | 同上 |
| TC-OS-005 | 按创建日期范围搜索 | P1 | ✅ | 日期范围组件定位器 `el-date-editor.el-range-editor` 稳定性 B，可自动 |
| TC-OS-006 | 组合条件搜索（名称 + 分类 + 状态 + 日期） | P0 | ✅ | 搜素核心，验证多条件联合过滤 |
| TC-OS-007 | 表格行展示：课程名称、分类、老师、学员数、状态等 | P0 | ✅ | 验证表格列数据渲染正确（通过获取单元格文本对比） |
| TC-OS-008 | 分页 — 切换每页条数 | P1 | ✅ | 下拉选择分页条数，组件稳定 |
| TC-OS-009 | 分页 — 点击页码 | P1 | ✅ | 点击 `el-pagination` 的页码按钮 |
| TC-OS-010 | 分页 — 总记录数显示正确 | P1 | ✅ | 获取 `total` 文本 |
| TC-OS-011 | 空数据状态显示“暂无课程数据” | P2 | ❌ | 需造空数据环境，维护成本高；执行频率低，建议手动验证 |
| TC-OS-012 | 加载中显示骨架屏 | P2 | ❌ | 动画验证不稳定，建议手工检查 |
| TC-OS-013 | 新建课程弹窗 — 打开；必填项校验（名称+分类+老师） | P0 | ✅ | 弹窗打开逻辑稳定；必填校验可通过提交后检查错误提示实现 |
| TC-OS-014 | 新建课程 — 填写完整信息并保存成功 | P0 | ✅ | 核心CRUD，弹窗内元素定位器基于 label 关联，稳定性 A |
| TC-OS-015 | 新建课程 — 取消保存 | P1 | ✅ | 点击取消按钮 |
| TC-OS-016 | 编辑课程 — 打开弹窗并回填数据 | P0 | ✅ | 表格行点击编辑，弹窗应展示旧数据，验证回填 |
| TC-OS-017 | 编辑课程 — 修改并保存 | P0 | ✅ | 同上 |
| TC-OS-018 | 删除课程 — 确认删除 | P0 | ✅ | 删除触发确认框（`el-message-box`），定位器稳定 |
| TC-OS-019 | 删除课程 — 取消删除 | P1 | ✅ | 取消操作 |
| TC-OS-020 | 上架/下架操作 | P1 | ✅ | 表格行内按钮，可能受 `v-if` 影响，需注意元素动态加载 |
| TC-OS-021 | 查看学员进度弹窗 | P1 | ✅ | 弹窗可能包含子表格，需独立 PageObject |
| TC-OS-022 | 权限控制 — 无编辑权限时编辑按钮隐藏 | P2 | ❌ | 需要切换角色或 mock 数据，维护成本高；建议安全测试覆盖 |
| TC-OS-023 | 上传封面图片 | P2 | ❌ | 文件上传操作依赖本地文件和浏览器文件选择框，自动化不稳定 |
| TC-OS-024 | 表单输入超长文本截断 | P2 | ❌ | 边界值验证只需手动一次 |
| TC-OS-025 | 左侧分类树单击筛选 | P1 | ✅ | 如果存在分类树，模拟树节点点击 |

**风险标注**：
- TC-OS-003、TC-OS-004：下拉选择需要展开 dropdown，存在点击穿透或动画延迟风险，建议封装 `select_option` 工具方法。
- TC-OS-005：日期范围选择器可能会因为月份切换导致不稳定，建议先清空再选择。
- TC-OS-016：编辑回填依赖弹窗打开后的数据加载，需要显式等待接口返回。

---

## 2. PageObject 拆分方案

| Page 类 | 职责 | 备注 |
|---------|------|------|
| `OnlineStudyListPage` | 搜索区、表格区、分页区的所有操作 | 主页面，包含分类树（如果有） |
| `CourseDialog` | 新建/编辑课程弹窗的 CRUD 操作 | `el-dialog`，独立于列表页 |
| `ProgressDialog` | 查看学员进度弹窗 | 可能包含子表格或分页 |
| `ConfirmBox` | 全局删除确认弹框（若存在非弹窗内确认框） | 复用 BasePage 的 `alert_confirm` 方法 |

**建议**：如果左侧分类树交互简单（仅点击节点触发筛选），可以将分类树方法放在 `OnlineStudyListPage` 中，不额外拆分。

---

## 3. 公共组件复用分析

### 可以复用的 BasePage 方法
| BasePage 方法 | 适用场景 | 示例参数 |
|--------------|---------|---------|
| `find` | 所有元素定位 | |
| `click` | 查询、重置、编辑、删除等按钮 | |
| `input` | 课程名称输入框、表单输入 | `input(placeholder="课程名称", value)` |
| `select_by_label` | 下拉选择（搜索结果依赖 label） | `select_by_label("课程分类", "AI")` |
| `select_by_placeholder` | 下拉选择（placeholder 方式） | `select_by_placeholder("课程分类", "AI")` |
| `click_by_text` | 按文本点击按钮 | `click_by_text("查询")` |
| `get_table_row_cell` | 获取表格某行某列文本 | |
| `get_pagination_total` | 获取总记录数 | |
| `switch_tab` | 如果页面有标签页 | |
| `alert_confirm` | 删除确认弹框 | `alert_confirm(accept=True)` |

### 需要扩展的 ElementPlusHelper 方法
| 方法 | 原因 | 说明 |
|------|------|------|
| `date_range_picker` | 日期范围选择器操作复杂，需封装清除、选开始、选结束 | 目前 ElementPlusHelper 可能只有单日期选择 |
| `upload_file` | 文件上传操作 | 需要 `send_keys` 到隐藏的 file input |
| `select_option_by_text` | 下拉选项可能不在 select 内部，需要弹出窗口后选 option | 当前 select 方法可能只支持 `el-select` 下拉，但有些 dropdown 是 `el-dropdown-menu` |
| `dialog_has_text` | 判断弹窗是否有特定文字（用于校验错误提示） | 弹窗内错误提示 `<div class="el-form-item__error">` |

---

## 4. 等待策略建议

### 页面特有的异步行为
| 行为 | 触发条件 | 建议等待策略 |
|------|---------|-------------|
| 表格数据加载 | 页面打开、搜索、分页点击 | 等待 `el-loading` 消失（`//*[contains(@class, 'el-loading-mask')]`）或等待表格行出现 |
| 下拉选项加载 | 点击 select 触发远程搜索 | 等待 option 列表可见（`//div[contains(@class, 'el-select-dropdown')]//li`) |
| 弹窗打开 | 点击新建/编辑/查看进度 | 等待 `el-dialog` 的 visible 属性为 true，或等待弹窗内表单元素出现 |
| 表单校验错误提示出现 | 提交空表单 | 等待 `.el-form-item__error` 可见 |
| 删除确认框出现 | 点击删除按钮 | 等待 `el-message-box` 可见 |
| 状态 change 后表格刷新 | 搜索/切换分页 | 等待 loading 掩码消失（与表格数据加载同一策略） |
| 上传文件后进度 | 文件选择后 | 等待上传进度条消失或上传完成提示 |

### 建议封装
在 `OnlineStudyListPage` 中添加 `wait_for_table_loaded(wait=10)` 方法，循环等待 loading 掩码消失并至少出现一条数据行。类似地，`wait_for_dialog_visible(dialog_class="el-dialog")` 方法。

---

## 5. ROI 分析

### 预估数据
- **手工执行一次完整冒烟（P0 用例）**：约 20 分钟（含环境准备、数据清理）
- **每日执行频率**：2 次（CI 每次提交 + 每晚定时）
- **每月执行次数**：60 次
- **每月手工耗时**：60 × 20 = 1,200 分钟 = 20 小时
- **自动化开发成本**：页面对话框 + 列表页 + 公共组件扩展 = 8 小时
- **单次自动化执行时间**：2 分钟
- **每月自动化维护成本**：约 2 小时（定位器微调、数据适配、失败排查）
- **每月自动化执行时间**：60 × 2 = 120 分钟 = 2 小时
- **每月节省时间**：(20 - 2 - 2) = 16 小时

### ROI 计算（3 个月）
- **收益**：每月节省 16 小时 × 3 个月 × 每小时成本（假设 ¥80）= ¥3,840
- **投入成本**：开发 8 小时 × ¥80 = ¥640
- **ROI**：(¥3,840 - ¥640) / ¥640 = 5.0 （即 3 个月回报 5 倍）

> 注：以上为理想估算。若维护成本因定位器不稳定上升，ROI 会下降。本页面下拉菜单较多，建议投入 1 小时额外封装稳定选择方法以降低维护成本。

---

## 6. 实施建议与风险

| 风险 | 缓解措施 |
|------|---------|
| `el-select` 选项可能动态远程加载 | 添加显式等待选项出现后再 `click` |
| 删除确认框可能由不同方式触发（`confirm` vs `message-box`） | 统一封装 `confirm_action` 方法 |
| 表格行内编辑/删除按钮可能因权限 `v-if` 隐藏 | 先判断按钮存在性，添加权限标记 fixture |
| 日期范围选择跨月场景 | 封装公共函数，先清除再选择，避免已有日期残留 |
| 上传文件组件需要文件路径 | 使用 `os.path.abspath` 获取测试资源文件绝对路径 |

---

> **⚠️ 说明**：本策略基于 PAGE_CONTEXT.md 推断的典型用例生成，未使用真实 `TEST_CASES.md`。建议与测试用例清单对齐后调整覆盖矩阵优先级和 ROI 计算。P0 用例共计约 12 个，已全部标记为自动化。
```