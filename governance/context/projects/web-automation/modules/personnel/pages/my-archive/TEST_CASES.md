好的，test-design-agent 收到指令。我将基于您提供的 `personnel/my-archive` 页面上下文及风险模型，并参考 `P2-5` 的业务场景分析思路，为您产出高质量的测试设计草案和详细测试用例表。

---

## 产出物 1: TEST_DESIGN.md

```markdown
# TEST_DESIGN: personnel / my-archive (我的档案)

> **版本**: 1.0 | **最后更新**: [当前日期] | **维护者**: test-design-agent
> **基于分析**: PAGE_CONTEXT.md, RISK_MODEL.md, MODULE_CONTEXT.md (personnel)
> **P2-5 业务场景**: (推断自页面功能) 员工信息维护与审计追溯

## 1. 测试范围与分层

| 测试维度 | 描述 | 风险关联 |
| :--- | :--- | :--- |
| **基础层 (UI操作覆盖)** | 页面加载、搜索、表格、CRUD、权限、异常 | 确保UI交互正确 |
| **价值层 (业务场景覆盖)** | 端到端业务流程、业务规则、角色协作、数据流 | 确保业务价值实现 |

## 2. 基础层测试场景 (UI操作覆盖)

### 2.1 页面加载与显示 (TCD-PM-01)
| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TCD-PM-01-01 | 验证“个人基本信息”Tab默认激活并正确加载 | P0 | 1. 使用在职员工账号登录 <br>2. 系统中有该员工的个人档案数据 | 1. 导航至“我的档案” <br>2. 观察默认激活的Tab | 1. 页面标题显示“我的档案” <br>2. `basic-info-tab` 高亮且 `el-tab-pane`内容区显示 <br>3. 表单中姓名、部门、职位等信息正确回显 |
| TCD-PM-01-02 | 验证页面无数据时的空状态显示 | P2 | 1. 使用新入职无档案变更记录的员工账号登录 | 1. 切换到“档案变更记录”Tab | 1. `change-table` 显示空状态文案“暂无数据”或占位图 <br>2. 筛选区、分页组件正常显示但不影响数据 |
| TCD-PM-01-03 | 权限受限：HR角色的访存限制 | P0 | 1. 使用HR角色账号登录 <br>2. 账号有查看个人基本信息的能力 | 1. 导航至“我的档案” <br>2. 检查页面元素 | 1. 页面正常加载 <br>2. `edit-basic-info-btn` 按钮**不可见或置灰**（若有此限制） |

> **自动化建议**: ✅ 适合自动化
> **自动化方式**: 使用 Page Object 模式，编写页面加载断言（标题、元素可见性、数据回显）。

### 2.2 搜索与筛选 (TCD-PM-02)
| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TCD-PM-02-01 | 按“变更类型”筛选 | P1 | 1. 员工账号下有多个类型的变更记录 | 1. 切换到“档案变更记录”Tab <br>2. 在 `change-type-select` 中选择“修改” <br>3. 点击 `search-btn` | 1. `change-table` 仅显示变更类型为“修改”的记录 <br>2. 分页数据总量更新为筛选后的记录数 |
| TCD-PM-02-02 | 按“变更日期范围”筛选 | P1 | 1. 员工账号下有指定日期范围内的变更记录 | 1. 切换到“档案变更记录”Tab <br>2. 在 `change-date-picker` 中选择一个日期范围 <br>3. 点击 `search-btn` | 1. 表格仅显示所选日期范围内的记录 |
| TCD-PM-02-03 | 组合筛选 | P2 | 1. 单条件筛选结果不唯一，组合条件能精确定位 | 1. 设置变更类型为“修改” <br>2. 设置日期范围为近一周 <br>3. 点击 `search-btn` | 表格仅显示在近一周内发生的“修改”记录 |
| TCD-PM-02-04 | 重置筛选 | P1 | 1. 当前有筛选条件在生效 | 1. 执行任意筛选后 <br>2. 点击 `reset-btn` | 1. 所有筛选条件恢复到默认值（类型: 全部/空，日期: 清空） <br>2. 表格恢复到未筛选状态（显示全部记录） |
| TCD-PM-02-05 | 无效/空搜索结果 | P2 | 1. 数据库中没有符合筛选条件的数据 | 1. 选择一个不存在任何变更记录的组合条件 <br>2. 点击 `search-btn` | 1. 表格显示“暂无数据” <br>2. 分页显示“共0条” |

> **自动化建议**: ✅ 适合自动化
> **自动化方式**: 使用数据驱动测试，准备多组前端筛选条件，断言表格行数和内容是否符合预期。

### 2.3 表格操作 (TCD-PM-03)
| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TCD-PM-03-01 | 分页-切换每页条数 | P1 | 1. 员工有超过10条变更记录 | 1. 切换到“档案变更记录”Tab <br>2. 改变 `pagination` 的 `page-size` 为20 | 1. 表格一页显示20条数据 <br>2. `pagination` 显示正确的分页信息 |
| TCD-PM-03-02 | 分页-翻页 | P0 | 1. 员工有超过当前页大小的变更记录 | 1. 点击分页的“下一页”按钮 | 1. 表格显示下一页数据 <br>2. 当前活动页码变为2 |
| TCD-PM-03-03 | 数据格式化校验 | P1 | 1. 表格中有某条变更记录 | 1. 检查表格中各列的数据格式 | 1. `col-change-time` 显示为“YYYY-MM-DD HH:mm:ss”格式 <br>2. `col-operator` 显示为操作人的真实姓名，而非ID |

> **自动化建议**: ✅ 适合自动化
> **自动化方式**: 组合 Page Object 元素定位与数据断言。

### 2.4 新增/编辑操作 (TCD-PM-04)
| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TCD-PM-04-01 | 基本信息-正常编辑保存 | P0 | 1. 使用在职员工账号登录 <br>2. 点击 `edit-basic-info-btn` 打开编辑弹窗 | 1. 修改姓名、部门、职位、联系方式 <br>2. 输入合法数据 <br>3. 点击 `dialog-save-btn` | 1. 提示操作成功 <br>2. 弹窗关闭 <br>3. 基本信息表单更新为修改后的值 <br>4. 档案变更记录中新增一条“修改”记录 |
| TCD-PM-04-02 | 编辑-表单必填项校验 | P0 | 1. 打开“编辑基本信息”弹窗 | 1. 清空“姓名”输入框 <br>2. 点击 `dialog-save-btn` | 1. 表单校验未通过 <br>2. `dialog-name-input` 下方显示错误提示“姓名不能为空” |
| TCD-PM-04-03 | 编辑-表单数据格式校验 | P0 | 1. 打开“编辑基本信息”弹窗 | 1. 输入不合法的手机号（如`123456`） <br>2. 点击 `dialog-save-btn` | 1. 表单校验未通过 <br>2. `dialog-phone-input` 下方显示“请输入正确的手机号” |
| TCD-PM-04-04 | 编辑-取消操作 | P2 | 1. 打开编辑弹窗并修改了某些数据 | 1. 点击 `dialog-cancel-btn` | 1. 弹窗关闭 <br>2. 基本信息页面数据不变，未发生实际更新 |
| TCD-PM-04-05 | 权限：编辑非授权字段 | P0 | 1. 使用普通员工角色登录 <br>2. 前端已限制限制 `dialog-department-select` 中的可选值 | 1. 打开编辑基本信息弹窗 <br>2. 尝试在 `dialog-department-select` 中选择一个不在当前角色授权列表中的部门 | 1. `dialog-department-select` 选项中没有该部门 <br>2. 若通过接口请求修改，后端应返回“无权操作” |

> **自动化建议**: ✅ 适合自动化
> **自动化方式**: 使用数据驱动测试，验证各类输入校验。

### 2.5 删除操作 (TCD-PM-05)
*（根据`PAGE_CONTEXT.md`，此页面无删除功能，故不设计测试用例）*

### 2.6 异常场景 (TCD-PM-06)
| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TCD-PM-06-01 | 网络异常：保存时断网 | P1 | 1. 打开编辑弹窗并输入数据 <br>2. 模拟网络断开 | 1. 点击 `dialog-save-btn` | 1. 前端提示“网络异常，请稍后重试” <br>2. 保存请求未被服务端处理 <br>3. 再次联网刷新页面，数据未变更 |
| TCD-PM-06-02 | 接口超时：查询变更记录接口超时 | P1 | 1. 切换到“档案变更记录”Tab <br>2. 模拟 `getChangeRecords` 接口超时 | 1. 观察页面表现 | 1. 页面加载状态显示（如骨架屏或loading图标） <br>2. 超时后页面提示“请求超时，请刷新重试” <br>3. 不会导致页面白屏或崩溃 |
| TCD-PM-06-03 | 接口异常：保存接口返回500 | P1 | 1. 打开编辑弹窗并输入数据 <br>2. 后端模拟500错误 | 1. 点击 `dialog-save-btn` | 1. 前端弹出错误提示（如“操作失败，请联系管理员”） <br>2. 弹窗未关闭，数据未保存 |
| TCD-PM-06-04 | 会话超时 | P0 | 1. 登录后长时间未操作，Token过期 | 1. 点击 `edit-basic-info-btn` | 1. 页面跳转到登录页 <br>2. 清除本地登录状态 |
| TCD-PM-06-05 | 并发编辑：多窗口编辑同一条数据 | P2 | 1. 在窗口A打开编辑弹窗 <br>2. 在窗口B也打开编辑弹窗（数据未变，但后端有版本号） | 1. 在窗口A点击保存 <br>2. 在窗口B点击保存 | 1. 窗口A保存成功 <br>2. 窗口B保存失败，提示“数据已被修改，请刷新后重试” |

> **自动化建议**: ➡️ 部分自动化，部分不适合
> **自动化方式**: 使用 `pytest-mock` 或 `responses` 库模拟接口超时/500错误。网络断开、会话超时需按情况考虑半自动化（结合 Selenium + 网络模拟工具，如 `mitmproxy`）。
> **不建议自动化的场景**: 多窗口并发编辑（涉及复杂的时间同步问题，建议手动测试）。

## 3. 价值层测试场景 (业务场景验证)

*（以下场景基于业务目标、角色旅程和工作流推断，旨在测试P2-5的完整性）*

| 编号 | 业务场景ID | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TCD-PM-BS-01 | BS-PM-01 | **业务目标验证**: 员工成功维护个人信息并完成审计追溯 | P0 | 1. 在职员工账号 <br>2. 部门/职位无变更限制 | 1. 登录→查看基本信息 <br>2. 修改手机号并保存 <br>3. 切换到“档案变更记录”Tab <br>4. 筛选变更类型为“修改”，并按更新时间排序 | 1. 基本信息Tab更新为新的手机号 <br>2. 变更记录Tab中最新的那条记录的`col-change-field`为“手机号”，`col-operator`为当前用户 |
| TCD-PM-BS-02 | BS-PM-02 | **状态机验证**: 修改密码后强制注销 | P2 | 1. 在职员工账号 <br>2. 已知当前密码 | 1. 点击“修改密码” <br>2. 输入旧密码、新密码、确认新密码 <br>3. 点击确认修改 <br>4. **未**手动退出，直接尝试导航到其他页面 | 1. 成功提示“密码修改成功，请重新登录” <br>2. 页面跳转到登录页 <br>3. 旧的登录Token失效 |
| TCD-PM-BS-03 | BS-PM-03 | **角色协作验证**: 普通员工无法编辑非授权字段 | P0 | 1. 普通员工角色账号 <br>2. 后端已处理越权请求 | 1. 打开编辑基本档案弹窗 <br>2. 修改“部门”字段（该字段为HR专属权限） <br>3. 点击保存 <br>4. 使用抓包工具或浏览器控制台，手动修改`dialog-department-select`值为非授权部门ID并发送保存请求 | 1. 前端校验：部门选择器不显示非授权部门 <br>2. 接口校验：后端返回403或“无权操作该字段” <br>3. 数据库记录未修改 |
| TCD-PM-BS-04 | BS-PM-04 | **数据流完整性**: 档案变更记录数据的精准度 | P1 | 1. 有至少5条不同日期、不同类型的变更记录 | 1. 按“变更类型”筛选“修改” <br>2. 数据行数等于数据库中类型为“修改”的记录数 <br>3. 点击每页条数调整为“全部” | 1. 表格展示全部符合条件的数据 <br>2. 每条记录都显示正确的字段、操作时间、操作人 <br>3. 分页组件底部显示“共X条” |

> **自动化建议**: ✅ 适合自动化
> **自动化方式**: **BS-01** (E2E测试), **BS-03** (数据驱动 + mock), **BS-04** (数据驱动)。 **BS-02** (修改密码注销流程，适合自动化，但需注意测试账号状态）。

## 4. 测试策略与约束

- **自动化优先级**: P0 业务场景 > P0 UI 操作 > P1 业务场景 > P1 UI 操作
- **手工测试**: TCD-PM-06-05 (并发编辑)
- **不建议自动化场景**:
    - **TCD-PM-06-01 (网络断开)**：需要依赖外部网络模拟工具，环境搭建成本高。
    - **UI 视觉风格/布局细节**：像素级比对不适合自动化，适合人工视觉审查。
    - **偶发性的、原因不明的崩溃**：不适合自动化覆盖。

## 5. 测试数据需求

| 数据对象 | 用途 | 来源 |
| :--- | :--- | :--- |
| 在职员工账号 | UI操作测试、E2E场景 | 测试环境预置 |
| 新入职无记录员工账号 | 空状态测试 | 测试环境预置 |
| 普通员工角色账号 | 角色权限测试 | RBAC系统分配 |
| HR角色账号 | 角色权限测试（断言无编辑按钮） | RBAC系统分配 |
| 离线员工账号 | 异常场景测试 | 后台手动禁用 |
| 真实的部门/职位列表 | 业务规则测试 | 后端API预置 |
| 合法的/非法的手机号、邮箱 | 表单校验测试 | 测试数据预置 |

---
```

---

## 产出物 2: TEST_CASES.md

```markdown
# TEST_CASES: personnel / my-archive (我的档案)

**基于**: TEST_DESIGN.md (v1.0) | **版本**: 1.0 | **最后更新**: [当前日期]

---

## 1. 基础层测试用例

### 1.1 页面加载与显示

| 用例编号 | 用例标题 | 所属模块 | 所属页面 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 自动化 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-PM-001 | 基本信息Tab默认激活 | personnel | my-archive | P0 | 1. 使用在职员工账号登录 <br>2. 有档案数据 | 1. 导航至我的档案 <br>2. 观察页面 | - | 1. 页面标题“我的档案” <br>2. `basic-info-tab` 高亮 <br>3. 表单显示姓名、部门、职位等 | | ✅ 已自动化 |
| TC-PM-002 | 空状态显示 | personnel | my-archive | P2 | 1. 新入职员工账号 | 1. 导航至我的档案 <br>2. 切换到“档案变更记录”Tab | - | 1. `change-table` 显示“暂无数据” | | ✅ 已自动化 |

### 1.2 搜索与筛选

| 用例编号 | 用例标题 | 所属模块 | 所属页面 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 自动化 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-PM-003 | 按“修改”类型筛选 | personnel | my-archive | P1 | 1. 员工下有“新增”、“修改”、“删除”变更记录 | 1. 切换到“档案变更记录”Tab <br>2. 选择“修改”类型 <br>3. 点击“查询” | 变更类型: "修改" | 1. 表格仅显示“修改”类型记录 <br>2. 数据总量正确 | | ✅ 已自动化 |
| TC-PM-004 | 按日期范围筛选 | personnel | my-archive | P1 | 1. 有不同日期变更记录 | 1. 切换到“档案变更记录”Tab <br>2. 选择日期范围 <br>3. 点击“查询” | 日期范围: 上周一 ~ 本周一 | 1. 表格显示该日期范围记录 | | ✅ 已自动化 |

### 1.3 编辑操作

| 用例编号 | 用例标题 | 所属模块 | 所属页面 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 自动化 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-PM-005 | 编辑基本信息-成功保存 | personnel | my-archive | P0 | 1. 在职员工账号 <br>2. 打开编辑弹窗 | 1. 输入新的姓名：`testuser_new` <br>2. 选择部门：`测试部` <br>3. 输入手机号：`13800138000` <br>4. 点击“确定” | name: `testuser_new` <br>department: `测试部` <br>phone: `13800138000` | 1. 提示“保存成功” <br>2. 弹窗关闭 <br>3. 基本信息更新 <br>4. 变更记录新增“修改”记录 | | ✅ 已自动化 |
| TC-PM-006 | 编辑基本信息-必填项校验 | personnel | my-archive | P0 | 1. 打开编辑弹窗 | 1. 清空姓名输入框 <br>2. 点击“确定” | name: `` (空) | 1. `dialog-name-input` 下方提示“姓名不能为空” | | ✅ 已自动化 |

### 1.4 异常场景

| 用例编号 | 用例标题 | 所属模块 | 所属页面 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 自动化 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-PM-007 | 接口超时-变更记录查询 | personnel | my-archive | P1 | 1. 接口 `getChangeRecords` 超时 | 1. 切换到“档案变更记录”Tab | - | 1. 显示加载状态 <br>2. 超时后提示“请求超时” | | 🔄 待开发 |
| TC-PM-008 | 会话超时-跳转登录页 | personnel | my-archive | P0 | 1. Token 过期 | 1. 点击 `edit-basic-info-btn` | - | 1. 跳转到登录页 <br>2. 清除本地状态 | | 🔄 待开发 |

## 2. 价值层测试用例 (业务场景验证)

| 用例编号 | 用例标题 | 业务场景ID | 所属模块 | 所属页面 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 自动化 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-PM-BS-001 | 员工个人档案修改并审计追溯 | BS-PM-01 | personnel | my-archive | P0 | 1. 在职员工 <br>2. 有编辑权限 | 1. 登录 → 查看基本信息 <br>2. 修改手机号为 `13912345678` <br>3. 切换到“档案变更记录” <br>4. 按“修改”类型筛选 | phone: `13912345678` | 1. 基本信息更新 <br>2. 变更记录最新1条字段为“手机号” <br>3. 操作人为当前用户 | | ✅ 已自动化 |
| TC-PM-BS-002 | 越权编辑部门字段被拦截 | BS-PM-03 | personnel | my-archive | P0 | 1. 普通员工角色 <br>2. 后端拦截 | 1. 打开编辑弹窗 <br>2. 前端选择非授权部门 -> 无法选择 <br>3. 用开发者工具修改请求 -> 接口返回403 | department_id: `hr_authorized_id` | 1. 前端选择器不显示 <br>2. 接口返回403 <br>3. 数据库未改变 | | 🔄 待开发 |
| TC-PM-BS-003 | 修改密码后强制重新登录 | BS-PM-02 | personnel | my-archive | P2 | 1. 在职员工 <br>2. 已知旧密码 | 1. 点击“修改密码” <br>2. 输入旧/新密码并确认 <br>3. 点击保存 <br>4. 尝试导航到其他页面 | - | 1. 提示“密码修改成功” <br>2. 跳转到登录页 <br>3. 旧Token失效 | | 🔄 待开发 |
| TC-PM-BS-004 | 数据流完整性验证 | BS-PM-04 | personnel | my-archive | P1 | 1. 有5条不同日期的变更记录 | 1. 按“修改”类型筛选 <br>2. 查看每页条数“全部” <br>3. 检查每条记录 | - | 1. 条数正确 <br>2. 每条记录精确 <br>3. 分页显示“共5条” | | 🔄 待开发 |

---

## 3. 自动化优先级建议

| 优先级 | 用例 | 自动化工具/方式 | 说明 |
| :--- | :--- | :--- | :--- |
| **P0** | TC-PM-001, TC-PM-005, TC-PM-006, TC-PM-BS-001, TC-PM-BS-002 | Selenium + BasePage | 核心P0业务场景，全自动覆盖 |
| **P1** | TC-PM-003, TC-PM-004, TC-PM-007, TC-PM-008, TC-PM-BS-004 | Selenium + Mock (for 超时) | 核心操作覆盖，部分结合mock |
| **P2** | TC-PM-002 | Selenium + 数据驱动 | 边缘/异常场景 |

---
```