你提到了模块 `aitest-platform`，但我需要更多信息才能继续。根据 Skill `frontend/vue-component-generator` 的要求，生成 Vue 组件需要以下输入文件：

### 需要的输入

| 文件 | 说明 |
|------|------|
| **PROPS_INTERFACE.yaml** | 组件的 Props / Events 接口定义 |
| **COMPONENT_SPEC.md** | 组件的状态和行为规范 |
| **COMPONENT_TREE.md** | 父组件的上下文（可选但推荐） |

### 请告诉我

1. **你要生成哪个组件？**（例如：`UserTable`、`LoginForm`、`EquipmentDialog` 等）
2. **上述输入文件在哪里？** 或者你是否需要我先帮你创建接口定义和规格文档？

如果你已经有这些文件，直接把内容贴出来或告诉路径，我就可以开始生成 `<script setup lang="ts">` 的 Vue 3 SFC 组件。