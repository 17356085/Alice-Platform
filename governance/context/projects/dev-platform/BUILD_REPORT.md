根据工具执行结果，前端构建/后端打包任务执行失败，错误信息为：

**`string indices must be integers, not 'str'`**

这表示在构建脚本中，某个地方试图用字符串索引访问一个字符串（而不是预期的字典/列表），导致类型错误。

## 构建失败报告

### 错误分析
- **错误类型**: `TypeError` — 字符串索引使用了字符串而非整数
- **原因**: 构建脚本中某处将字符串当作字典/列表访问

### 建议修复方向
1. 检查 `vite.config.ts` 或 `pyproject.toml` 中是否有 JSON/YAML 解析逻辑
2. 检查 npm/pip 脚本中是否有 `os.environ` 或 `process.env` 读取后未做类型转换
3. 检查是否有 `json.loads()` 调用但传入了字符串本身而非 JSON 文本

请提供更详细的构建脚本内容以便定位具体问题行。