我理解您想对 `system-role` 模块进行风险建模和业务场景分析。但这个技能需要明确的输入（`PAGE_CONTEXT.md` 和 `MODULE_CONTEXT.md`），目前您仅提供了模块名。为了产出符合 P2-5 标准的 `RISK_MODEL.md` 和 `BUSINESS_SCENARIOS.md`，我需要补充以下信息：

---

## 请提供：

### 1. 页面上下文（PAGE_CONTEXT）
至少包含：
- 页面名称（如：角色管理列表页、角色编辑页）
- 页面功能摘要
- 页面包含的关键元素（如表格、搜索框、新增/编辑/删除按钮、权限分配树）
- 页面访问路径

### 2. 模块上下文（MODULE_CONTEXT）
至少包含：
- 模块业务描述（如：系统角色管理用于定义角色及权限，关联用户）
- 模块与其他模块的交互（如：用户管理、权限管理）
- 已知的历史缺陷或风险点（可选）

---

## 根据现有参考上下文（EP-007、ENV-001），我可推测的初步信息：

| 条目 | 内容 |
|------|------|
| 已知坑位 | `el-tag` 颜色动态计算（EP-007），影响角色状态显示定位 |
| 已知环境问题 | RBAC权限测试种子数据缺失（ENV-001），影响 `system-role` 权限相关自动化测试 |
| 推断的风险 | 权限分配组件（el-tree）的勾选状态通过 Teleport 渲染？需确认 |
| 推断的业务场景 | 角色 CRUD、角色分配权限、角色关联用户、角色状态禁用/启用 |

但这些推测需要您进一步确认。如果您能提供上述上下文，我将立即输出：
- **`RISK_MODEL.md`**：覆盖业务、权限、数据、接口、UI/UX、性能 6 个维度
- **`BUSINESS_SCENARIOS.md`**：从业务目标→角色→流程→规则→数据→风险映射完整建模

请提供必要的上下文，或告诉我您希望我基于假设先输出一个示例草稿（标注置信度为⚠️推断）？