好的，遵照您的指示。我现在将扮演风险建模专家（risk-modeling Skill），基于提供的上下文，为 `system-role` 模块的 `role-list` 页面进行全面的风险建模和业务场景分析。

**输入：**
- 模块：`system-role`
- 页面：`role-list`

**参考上下文：**
- `[EP-007]`：el-tag 颜色由 JS 动态计算，不可用于定位
- `[FP-012]`：跨页面 E2E 测试覆盖严重不足
- `[ENV-001]`：RBAC 权限测试种子数据缺失
- `system-role` 模块通常涉及：角色CRUD、权限树分配、角色与用户关联

**推断的业务上下文 (`MODULE_CONTEXT.md` 内容推断):**
- `system-role` 模块目标是管理 RBAC (基于角色的访问控制) 中的角色定义。
- 核心操作包括：创建角色、编辑角色权限、删除角色、查看角色列表。
- 角色与用户、角色与权限（菜单/按钮）之间存在多对多关系。
- 权限通常通过一个 `el-tree` 组件展示和选择。

---

现在，我将输出两个文件：

1.  `RISK_MODEL.md`
2.  `BUSINESS_SCENARIOS.md`

---

### RISK_MODEL.md

```markdown
# 风险模型：角色列表页 (role-list)

**模块:** system-role
**页面:** role-list
**分析日期:** 2024-05-24

## 1. 业务风险 (Business Risks)

| ID | 风险描述 | 等级 | 缓解措施 | 关联自动化覆盖 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **BR-001** | **误删关键角色**: 删除“超级管理员”或“管理员”角色，导致系统无人可管理。 | **P0** | 1. 前端限制：不允许删除预置关键角色（如`admin`, `super_admin`）。<br>2. 后端校验：删除接口检查角色是否被关联到用户或系统保护。<br>3. 删除确认弹窗：明确提示“此操作将永久删除该角色及其所有权限配置”。 | 未覆盖 | 现有E2E测试集中在前端UI，缺后端级保护验证。 |
| **BR-002** | **角色权限变更未生效**: 修改角色权限树后，已登录的该角色用户权限未实时刷新，导致越权或功能异常。 | **P1** | 1. 后端需在权限变更后，通知相关用户session失效或触发token刷新。<br>2. 前端应有提示：“权限已更新，建议相关用户重新登录”。 | 未覆盖 | 跨页面E2E缺失 ([FP-012])。 |
| **BR-003** | **角色名称重复**: 创建新角色时，未校验角色名称唯一性，导致数据混乱。 | **P1** | 后端接口确保唯一性索引，创建时返回明确的重复错误提示。 | 未覆盖 |  |
| **BR-004** | **角色用户强删**: 删除一个仍被“用户-角色”关联引用的角色，导致关联用户失去所有权限。 | **P0** | 1. 后端删除前检查角色是否被引用。若被引用，返回明确错误信息。<br>2. 前端交互优化：提供“查看关联用户”的链接，让管理员决定是否先解绑。 | 未覆盖 |  |
| **BR-005** | **批量操作不可逆**: 批量删除多个角色时，缺少二次确认或撤销机制。 | **P2** | 添加批量删除的二次确认对话框，并记录操作日志。 | 未覆盖 |  |

## 2. 权限风险 (Permission Risks)

| ID | 风险描述 | 等级 | 缓解措施 | 关联自动化覆盖 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **PR-001** | **未授权访问**: **普通用户**通过直接输入URL (例如 `/system/role`) 访问角色列表页。 | **P0** | 1. 前端路由守卫对所有敏感页面进行鉴权。<br>2. 后端API接口 (如 `/api/roles`) 对所有请求进行权限校验，返回403。 | 未覆盖 ([ENV-001]) | 种子数据缺失导致自动化无法验证。 |
| **PR-002** | **越权操作**: 普通角色管理员（如`role_editor`）可以编辑/删除比自己权限等级更高的角色（如`super_admin`）。 | **P0** | 后端在更新或删除角色时，比较当前用户角色等级与目标角色等级，禁止低等级用户操作高等级角色。 | 未覆盖 | 这是RBAC的核心安全逻辑。 |
| **PR-003** | **API层无鉴权**: 前端隐藏了`删除`按钮，但用户仍可直接调用`DELETE /api/roles/{id}`接口。 | **P0** | 所有非查询API必须通过Spring Security或类似框架校验权限标识（如 `system:role:delete`）。 | 未覆盖 |  |
| **PR-004** | **按钮级权限错乱**: 用户A无“创建角色”权限，但页面仍显示“新建”按钮。 | **P1** | 按钮权限指令 (`v-permission`) 正确配置，后端返回的权限树准确。 | 部分覆盖 (EP-007) | 按钮显示与否可能依赖计算的tag样式，需用文本匹配代替。 |

## 3. 数据风险 (Data Risks)

| ID | 风险描述 | 等级 | 缓解措施 | 关联自动化覆盖 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **DR-001** | **角色名边界**: 角色名输入为空、超过长度限制(如255字符)、包含特殊字符。(SQL注入、XSS) | **P1** | 前后端都进行白名单校验，后端使用参数化查询。 | 未覆盖 |  |
| **DR-002** | **权限树数据异常**: 保存角色时，权限树选择的节点（如菜单ID）在后端找不到，导致角色权限格式错误。 | **P1** | 后端参数校验时，检查提交的所有权限ID是否真实存在。 | 未覆盖 |  |
| **DR-003** | **并发删除**: 管理员A和管理员B同时尝试删除同一个角色。 | **P2** | 后端使用乐观锁（如版本号）或悲观锁（如`select ... for update`）处理并发。 | 未覆盖 |  |
| **DR-004** | **查询接口无限流**: 角色列表没有分页或搜索参数未校验，导致一次性返回上千条数据，前端崩溃或接口超时。 | **P1** | 后端对列表接口强制分页，并对搜索参数进行长度和内容限制。 | 未覆盖 |  |

## 4. 接口风险 (API Risks)

| ID | 风险描述 | 等级 | 缓解措施 | 关联自动化覆盖 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **IR-001** | **接口超时/失败**: 获取角色列表接口因网络或后端问题超时，前端无任何提示，页面一直显示loading。 | **P1** | 1. 前端集成请求拦截器，处理HTTP错误状态码（如500， 504）。<br>2. 添加超时处理逻辑，显示统一的“网络异常，请稍后重试”错误页面或Toast。 | 未覆盖 |  |
| **IR-002** | **接口返回格式异常**: 后端接口返回的JSON结构发生改变（如`data.roles`变为`data.roleList`），前端解析失败。 | **P0** | 1. 前后端一致接口契约（如Swagger/OpenAPI）。<br>2. 前端可以对关键接口的响应结构进行类型校验或兼容处理。 | 未覆盖 |  |
| **IR-003** | **Token过期处理**: 用户长时间停留页面后操作，Token已过期，操作（如删除）失败，但页面未自动跳转登录页。 | **P1** | 1. 401拦截器自动跳转到登录页。<br>2. 提示“登录已过期，请重新登录”。 | 未覆盖 |  |

## 5. UI/UX 风险 (UI/UX Risks)

| ID | 风险描述 | 等级 | 缓解措施 | 关联自动化覆盖 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **UX-001** | **表格内容错位**: 角色名、描述字段过长时，表格列宽固定，内容截断或错位。 | **P2** | 使用 `el-tooltip` 或 `show-overflow-tooltip` 属性。 | 未覆盖 |  |
| **UX-002** | **弹窗错位/溢出**: 在分辨率较低的显示器上，“新建/编辑角色”弹窗内容过多，出现滚动条或错位。 | **P2** | 确保弹窗内容高度自适应或设置最大高度并允许内部滚动。 | 未覆盖 |  |
| **UX-003** | **加载状态缺失**: 分页、搜索、删除等操作无加载指示器，用户无法感知操作正在执行。 | **P2** | 所有异步请求时，按钮或列表区域显示 `v-loading` 效果。 | 未覆盖 |  |
| **UX-004** | **“确认操作”弹窗文本不清晰**: “确定要删除该角色吗？”没有说明角色是否被用户引用。 | **P2** | 弹窗文本应包含关键信息，如“该角色下有5个关联用户，删除后这些用户将失去该角色所有权限。确定删除吗？” | 未覆盖 |  |

## 6. 性能风险 (Performance Risks)

| ID | 风险描述 | 等级 | 缓解措施 | 关联自动化覆盖 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **PRF-001** | **权限树加载慢**: 编辑角色时，加载完整的、有几百个节点的权限树，导致页面卡顿。 | **P1** | 1. 权限树延迟加载（懒加载）。<br>2. 使用虚拟滚动优化 `el-tree`。 | 未覆盖 |  |
| **PRF-002** | **列表查询慢**: 随着角色数量增长，`/api/roles` 接口响应变慢。 | **P1** | 后端对查询SQL增加索引，并考虑使用Redis缓存。 | 未覆盖 |  |
| **PRF-003** | **频繁搜索**: 用户在搜索框中快速输入，导致短时间内发送大量请求。 | **P2** | 前端加入防抖 (debounce) 功能，如300ms内只发送最后一次请求。 | 未覆盖 |  |

---

### BUSINESS_SCENARIOS.md

```markdown
# 业务场景模型：角色列表页 (role-list)

**模块:** system-role
**页面:** role-list
**源文件:** MODULE_CONTEXT.md (inferred), RISK_MODEL.md
**分析日期:** 2024-05-24

## 1. 业务目标 (Business Goal)

- **核心业务目标**：允许授权的系统管理员对应用内的角色（即权限集合）进行全生命周期管理，包括查看、创建、编辑、删除，以支持动态的组织结构和权限控制需求。
- **次要业务目标**：
    - 快速搜索和定位特定角色，进行权限调整。
    - 了解每个角色的基本信息（如描述、创建时间）和关联用户数。
- **非目标**：
    - 不负责管理单个用户的权限（此功能在`user-list`页面）。
    - 不负责定义菜单或按钮（此功能在`system-menu`页面）。

## 2. 角色与旅程 (Roles & Journeys)

| 角色 | 职责边界 | 典型业务旅程（从进入到离开） | 置信度 |
| :--- | :--- | :--- | :--- |
| **超级管理员** | 拥有所有权限，可创建/编辑/删除所有角色。 | 1. 登录系统。 <br>2. 访问“系统管理 -> 角色管理”。 <br>3. 为满足新业务需求，点击“新建角色”。 <br>4. 填写角色名、描述，从全量权限树中勾选“订单管理”、“用户管理”等面向特定部门的权限。 <br>5. 点击“确认”，新建成功，角色出现在列表中。 <br>6. 在列表中找到刚创建的角色，点击“编辑”，为其增加“报表查看”权限。 <br>7. 退出页面。 | ✅ |
| **角色管理员** (如 `role_admin`) | 可创建、编辑和删除非管理性质的角色，但无权操作“测试管理员”、“超级管理员”预置角色。 | 1. 登录系统。 <br>2. 访问“系统管理 -> 角色管理”。 <br>3. 搜索“超级管理员”，发现“编辑”和“删除”按钮是灰色不可点击状态。 <br>4. 新建一个名为“实习生”的角色，并分配基础查看权限。 <br>5. 完成操作，退出。 | ⚠️ (推断) |
| **普通用户** | 无角色管理权限，访问该页面应失败。 | 1. 登录系统。 <br>2. 在浏览器地址栏输入 `/system/role`。 <br>3. 路由守卫或后端api返回403，页面提示“您没有访问此资源的权限”。 | ✅ |

## 3. 业务流程 (Workflows)

| 流程ID | 名称 | 描述 | 关联页面 | 置信度 |
| :--- | :--- | :--- | :--- | :--- |
| **WF-001** | **新建角色完整流程 (Happy Path)** | 1. [start] 管理员点击“新建角色”按钮。<br>2. [dialog] `新建角色`弹窗出现，展示角色名、角色描述、权限树。<br>3. [input] 输入唯一角色名 `intern_role` 和描述。<br>4. [select] 在权限树中勾选菜单。<br>5. [submit] 点击“确认”。<br>6. [api] 调用 `POST /api/roles`。<br>7. [end] 弹窗关闭，列表刷新，可看到新角色。 | `role-list`, `role-dialog` (本页弹窗) | ✅ |
| **WF-002** | **编辑角色权限 (Alternative Path 1)** | 1. [start] 管理员在列表中找到角色，点击“编辑”。<br>2. [dialog] `编辑角色`弹窗出现，加载已勾选的权限树。<br>3. [modify] 管理员修改权限（如去掉“导出”权限，增加“导入”权限）。<br>4. [submit] 点击“确认”。<br>5. [api] 调用 `PUT /api/roles/{id}`。<br>6. [end] 弹窗关闭，列表刷新。 | `role-list`, `role-dialog` | ✅ |
| **WF-003** | **删除预置角色 (异常流程)** | 1. [start] 管理员尝试删除名为 `super_admin` 的角色。<br>2. [confirm] 前端弹出确认弹窗：“此操作将永久删除角色及其所有权限配置”。<br>3. [confirm-yes] 管理员点击“确定”。<br>4. [api] 调用 `DELETE /api/roles/super_admin`。<br>5. [fail] 后端返回400错误：`不能删除系统预置角色`。<br>6. [ui] 前端显示错误消息。 | `role-list`, `confirm-dialog` | ✅ |
| **WF-004** | **删除仍有用户引用的角色 (异常流程)** | 1. [start] 管理员尝试删除一个被用户引用的角色 `editor`。<br>2. [confirm] 确认弹窗出现。<br>3. [confirm-yes] 管理员点击“确定”。<br>4. [api] 调用 `DELETE /api/roles/editor`。<br>5. [fail] 后端返回409冲突：`该角色仍有3个关联用户，无法删除。`<br>6. [ui] 前端显示错误提示。 | `role-list`, `confirm-dialog` | ⚠️ |
| **WF-005** | **无权限用户访问页面 (安全性流程)** | 1. [start] 普通用户登录。<br>2. [url] 用户直接在地址栏输入 `/system/role`。<br>3. [auth] 前端路由守卫校验权限。<br>4. [deny] 请求被拒绝，跳转到403页面或首页。 | `role-list` | ✅ |

## 4. 业务规则 (Business Rules)

| 规则ID | 规则类型 | 规则描述 | 触发条件 | 置信度 |
| :--- | :--- | :--- | :--- | :--- |
| **R-001** | 业务校验 | **角色名称唯一**：系统不允许创建两个同名的角色（大小写不敏感）。 | 创建或更新角色时。 | ✅ |
| **R-002** | 数据完整性 | **禁止删除预置角色**：角色表中标记为`system`的角色，如`admin`和`super_admin`，不能通过API删除。 | 调用删除API时。 | ✅ |
| **R-003** | 数据完整性 | **删除需先解绑关联用户**：角色`A`若被任何`用户-角色表`引用，系统必须阻止删除`A`操作，并返回关联用户数量。 | 调用删除API时。 | ⚠️ |
| **R-004** | 权限校验 | **低等级角色不能操作高等级角色**：如果用户角色等级为1，则不能编辑或删除等级为0的角色。 | 调用编辑或删除API时。 | ✅ |
| **R-005** | 状态流转 | **角色状态流转：禁用 | 启用**：角色可以切换状态，被禁用的角色，其关联用户将失去该角色赋予的权限。 | 管理员在角色详情页或编辑页修改状态时。 | ⚠️ |
| **R-006** | 计算规则 | **权限继承**：如果角色勾选了`用户管理`菜单，则自动继承其下所有按钮权限（如：新建、编辑、删除）。 | 保存角色时，后端根据勾选的菜单节点计算最终权限列表。 | ✅ |

## 5. 数据流 (Data Flow)

| 数据项 | 数据来源 (页面/模块) | 在此页面的操作 (变换) | 数据消费 (下游页面/模块) | 跨页面一致性要求 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `角色列表` | 后端接口 `GET /api/roles` | 展示、分页、搜索、排序。 | `user-list` (用户管理页面) 的下拉选择框 (角色关联)。 | 角色如果在`role-list`被重命名或删除，`user-list`下拉列表必须同步更新。 | ✅ |
| `角色权限树` | 后端接口 `GET /api/menus/tree` | 展示、勾选、全选/取消。 | `POST /api/roles` (创建时保存) 或 `PUT /api/roles/{id}` (编辑时保存)。 | 权限树的结构由`system-menu`模块维护。管理员修改`menu`后，`role-list`的权限树必须实时反映最新结构。 | ✅ |
| `角色ID & 选择结果` | `role-list`页面提交的数据 | 发送至后端 `POST/PUT` 接口。 | `user-role` 关联表 (后端写入)。 | 角色ID是真-持久化的。 | ✅ |

## 6. 业务风险→场景映射 (Risk-to-Scenario Mapping)

| 业务场景ID (此处新建) | 场景描述 | 覆盖的业务流程 | 覆盖的业务规则 | 关联风险ID | 自动化用例覆盖? | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BS-ROLE-001** | **验证超级管理员角色无法被删除** | WF-003 | R-002 | BR-001, BR-004, PR-001, PR-003 | ❌ | ✅ |
| **BS-ROLE-002** | **验证删除一个被引用的角色接口正确返回错误码** | WF-004 | R-003 | BR-001, BR-004 | ❌ | ⚠️ |
| **BS-ROLE-003** | **验证低等级角色管理员无法编辑高等级角色** | WF-001, WF-002 | R-004 | PR-002, PR-003 | ❌ | ✅ |
| **BS-ROLE-004** | **验证无权限用户通过URL直接访问页面会被拦截** | WF-005 | - | PR-001 | ❌ (依赖环境种子数据) | ✅ |
| **BS-ROLE-005** | **验证创建角色时，角色名重复时前端获得正确错误提示** | WF-001 | R-001 | BR-003 | ❌ | ✅ |
| **BS-ROLE-006** | **验证角色权限更新后，后端安全机制是否有效（新权限立即生效）** | WF-002 | - | BR-002 | ❌ (跨页面E2E) | ⚠️ |
| **BS-ROLE-007** | **验证接口返回格式异常（如属性名错误）时，前端能优雅处理而不崩溃** | 所有流程 | - | IR-002 | ❌ | ✅ |
| **BS-ROLE-008** | **验证大量角色数据（分页场景）正确渲染，且接口性能可接受** | 列表加载 | - | PRF-002 | ❌ | ⚠️ |

**缺失场景标注**：
- **BS-ROLE-006 (权限生效)**：需要 `role-list` + `user-list` + 登录模块的跨页面E2E，当前无覆盖 ([FP-012])。
- **BS-ROLE-003 (越权)**：需要 `role-list` + 多个用户账号的鉴权测试，依赖种子数据 ([ENV-001])。
- **BS-ROLE-007 (接口异常)**：属于鲁棒性测试，需要Mock后端，当前无覆盖。
```