好的，作为Web端测试工程师，我将基于您提供的完整上下文（包括Page Object代码、现有测试脚本、页面结构分析和风险模型），为“水质分析设计指标”这个**只读展示页**设计完整的测试方案。

首先，我会注意到这是一个**非常纯粹的只读页面**：无搜索、无分页、无CRUD（新增、编辑、删除）。因此，在遵循 `testcase-design Skill` 框架时，我会根据页面特性进行适配，跳过不存在的测试维度（如“搜索与筛选”、“新增操作”等），并将**业务数据验证**和**权限安全**作为核心测试维度。

### 产出物 1: TEST_DESIGN.md

```markdown
# TEST_DESIGN — lab / water-indicator

> **版本**: 1.0 | **最后更新**: 2026-06-18 | **页面类型**: 只读展示表格
> **相关风险**: RISK_MODEL.md (共9项风险)

## 测试策略
此页面为纯展示型页面，无用户交互操作。测试重点将从UI操作转向**业务数据准确性、权限控制、数据安全**和**状态展示**。核心采用 **数据驱动** + **权限安全** 的测试策略。自动化优先覆盖数据验证（回归价值高）和权限验证（安全基线），手工测试覆盖文案/UX异常等可读性检查。

## 测试维度与场景

### 维度 1: 页面加载与展示状态
基础层测试，验证页面在不同数据状态下的正确呈现。

| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 | 对应风险 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-DIS-001 | **正常加载**：验证22行数据完整展示 | P0 | 数据库有配置的22条水质指标数据 | 1. 使用具有权限的账号登录 <br> 2. 导航至“化验室取样→水质分析→水质分析设计指标” <br> 3. 等待页面加载完成 | 1. 页面URL为 `#/lab/water/indicator` <br> 2. 加载动画消失 <br> 3. 表格显示22行数据 <br> 4. 无异常报错 | WATER-INDICATOR-BIZ-002 |
| TC-DIS-002 | **空数据状态**：验证友好提示 | P2 | 数据库未配置任何水质指标数据（或使用空租户账号） | 1. 使用具有权限的专用空数据账号登录 <br> 2. 导航至目标页面 <br> 3. 等待页面加载完成 | 1. 页面正常加载，无JS报错 <br> 2. 表格内显示“暂无数据”或类似友好提示 <br> 3. 页面整体布局无异常 | WATER-INDICATOR-DATA-001 |
| TC-DIS-003 | **加载中状态**：验证骨架屏/loading组件消失（已隐含在正常用例流程中，作为可复现性检查） | P2 | 网络环境较差（可通过浏览器开发者工具限速模拟） | 1. 打开开发者工具，选择“网络”标签，切换到Slow 3G <br> 2. 导航至目标页面 | 1. 在数据完全加载前，表格区域显示el-loading或骨架屏 <br> 2. 数据加载完成后，loading状态消失，数据正常展示 | 无 |

### 维度 2: 业务数据准确性验证（核心）
基于业务场景（P2-5: BUSINESS_SCENARIOS精神，从风险模型提炼）进行数据层的精确验证。这是自动化回归的核心价值所在。

| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 | 对应风险 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-DATA-001 | **关键指标阈值验证**：自动化验证5个核心指标数据与API一致 | **P0** | 1. 数据库中存在ID为 `INDICATOR_ID` (如 `1, 2, 3, 4, 5`) 的明确预期数据 <br> 2. 通过API获取这5个指标的JSON数据作为基准 | 1. 获取页面第2列（指标名称）和第6列（阈值）数据 <br> 2. 将页面数据与API获取的基准值进行逐行比对 | 1. 页面显示的指标名称和阈值与API返回的基准值一致 <br> 2. 例如：pH 对应 `6.5-8.5`，COD 对应 `≤ 100` | WATER-INDICATOR-BIZ-001 |
| TC-DATA-002 | **列完整性验证**：验证所有列数据均存在且非空 | P2 | 数据库有完整的22行数据 | 1. 获取第2列（指标名称）数据 <br> 2. 获取第6列（阈值）数据 <br> 3. 获取第4列（单位）数据 | 1. 指标名称列不为空，且包含的指标数量与数据库一致（如 pH, COD, 氨氮, 总磷, SS等） <br> 2. 阈值列不为空，无空白或格式错误 <br> 3. 单位列不为空，如 mg/L, NTU, °C等 | WATER-INDICATOR-BIZ-002 |
| TC-DATA-003 | **边界值/格式化验证**：验证阈值列的正确显示格式 | P2 | 1. 数据库中存在值为 `0` 的指标 <br> 2. 存在值为 `-9999` 或 `99999` 的极端指标 <br> 3. 存在浮点精度值 `6.5` 的指标 | 1. 获取第6列（阈值）的全部数据 <br> 2. 在页面上找到这些特殊值对应的行 | 1. 值为 `0` 的指标显示为 `0`，而非空 <br> 2. 极端值 `-9999` 和 `99999` 正常展示，不会截断或显示为科学计数法 <br> 3. 浮点值显示为 `6.5` 而非 `6.500000` | WATER-INDICATOR-DATA-002 |

### 维度 3: 权限与安全验证

| 编号 | 测试场景 | 优先级 | 前置条件 | 测试步骤 | 预期结果 | 对应风险 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-AUTH-001 | **越权访问验证**：无权限用户通过URL访问，确认被拦截 | **P1** | 1. 准备一个“工艺操作员”角色账号（无`化验室取样`菜单权限） <br> 2. 此账号已登录系统 | 1. 使用此无权限账号登录 <br> 2. 在浏览器地址栏直接输入路由 `#/lab/water/indicator` 并回车 <br> 3. 观察浏览器行为 | 1. 页面跳转到403无权限页面或 <br> 2. 被重定向到首页/仪表板，并弹出“无权限访问”提示 <br> 3. 不显示表格内容 | WATER-INDICATOR-PERM-001 |
| TC-AUTH-002 | **角色数据隔离验证**：低权限角色（如外包人员）数据范围验证 | **P1** | 1. 数据库中有22条指标（包含部分敏感指标，如“总氰化物”） <br> 2. 外包人员账号仅被授权查看前15条非敏感指标 | 1. 使用外包人员角色账号登录 <br> 2. 导航至目标页面 <br> 3. 获取第2列（指标名称）数据 | 1. 指标名称列仅显示前15条非敏感指标（如不包含“总氰化物”） <br> 2. 表格总行数显示15行 | WATER-INDICATOR-PERM-002 |
| TC-AUTH-003 | **XSS注入验证**：验证字段的HTML转义 | **P1** | 1. 通过API或数据库修改，在某个指标名称中加入XSS payload（如 `<Script>alert('XSS')</Script>`） <br> 2. 注意清除缓存 | 1. 使用有权限账号登录 <br> 2. 导航至目标页面 <br> 3. 找到包含XSS payload的指标名称行 <br> 4. 检查页面元素 | 1. 页面上看到的文本是 `<Script>alert('XSS')</Script>` 的字符串 <br> 2. 页面**没有**弹出 `alert` 弹窗 <br> 3. 查看页面HTML源码（或通过开发者工具检查元素），确认 `<>` 已被转义为 `&lt;` 和 `&gt;` | WATER-INDICATOR-DATA-003 |

---

## 自动化优先级建议

| 测试场景编号 | 自动化优先级 | 理由 | 建议 |
| :--- | :--- | :--- | :--- |
| **TC-DIS-001** | 高 | 核心回归检查点，判断页面是否可访问。 | **必须自动化**，作为冒烟测试用例。 |
| **TC-DIS-002** | 高 | 数据库状态变化时（如租户初始化/数据清空），确保页面不崩溃。 | **建议自动化**，可作为独立的环境初始化测试。 |
| **TC-DATA-001** | **最高** | 这是业务正确性的**唯一红线**，每次部署都需要回归，手动执行成本高、易出错。 | **建议自动化**，作为核心回归用例，使用API获取基准数据。这也是用户建议的第1点。 |
| **TC-DATA-002** | 高 | 验证数据完整性，防护数据库迁移或配置错误。 | **建议自动化**。 |
| **TC-DATA-003** | 中 | 边界值测试，验证UI格式化逻辑。若UI版本不频繁更新，可手工验一次；若频繁，则自动化。 | **建议手工 + 脚本化数据准备**。即用脚本准备好数据，手工执行1-2次（验证UI渲染逻辑）。这也是用户建议的第4点。 |
| **TC-AUTH-001** | 最高 | 安全基线，不可手工依赖。 | **必须自动化**，作为持续集成的安全门禁。 |
| **TC-AUTH-002** | 高 | 角色权限变化后的回归验证。 | **建议自动化**，但前提是维护好不同角色的测试账号。 |
| **TC-AUTH-003** | 中 | 安全防御测试，升级第三方库或后端框架后回归。 | **建议手工 + 自动化脚本**。自动化脚本可以修改数据库并检查页面源码是否转义，但首次手工验证更稳妥。这也是用户建议的第3点。 |

---

### 产出物 2: TEST_CASES.md

```markdown
# TEST_CASES — lab / water-indicator

> **版本**: 1.0 | **最后更新**: 2026-06-18 | **总用例数**: 8 (P0: 2, P1: 4, P2: 2)

| 用例编号 | 用例标题 | 所属模块 | 所属页面 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 自动化 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TC-DIS-001 | **正常加载-22行数据展示** | lab | water-indicator | P0 | 1. 数据库有22条水质指标数据 <br> 2. 具有化验室权限的账号已登录 | 1. 导航至“化验室取样→水质分析→水质分析设计指标” <br> 2. 等待 `wait_page_ready` 和 `_wait_loading_gone` 完成 <br> 3. 检查页面URL是否包含 `#/lab/water/indicator` <br> 4. 使用 `get_table_row_count()` 获取行数 | 无独立数据准备，使用默认环境。 | 1. URL为 `#/lab/water/indicator` <br> 2. 无loading动画 <br> 3. `row_count` 等于22 | ✅ 已自动化 |
| TC-DIS-002 | **空数据状态-暂无数据提示** | lab | water-indicator | P1 | 1. 准备一个`test_tenant_empty`的空租户账号（数据库无水质指标） <br> 2. 使用此账号登录系统 | 1. 使用空租户账号登录 <br> 2. 导航至目标页面 <br> 3. 等待页面完全加载 <br> 4. 使用 `get_table_row_count()` 获取行数 <br> 5. 使用 `get_empty_text()` 获取空状态文本 <br> 6. 检查页面控制台有无JS报错 | 空租户账号 | 1. `row_count` 等于0 <br> 2. `empty_text` 不为空，且包含“暂无数据” <br> 3. 控制台无JS Error | 🔄 待开发 |
| TC-DATA-001 | **关键指标阈值数据一致性校验** | lab | water-indicator | P0 | 1. 从接口 `GET /api/lab/water/indicators` 获取**预期JSON数据** <br> 2. 确定5个核心指标（如 pH, COD, 氨氮, 总磷, SS）的ID | 1. 登录并访问目标页面 <br> 2. 使用 `get_column_data(2)` 和 `get_column_data(6)` 获取“指标名称”列和“阈值”列数据 <br> 3. 从JSON数据中提取上述5个核心指标的“名称”和“阈值” <br> 4. 将页面数据与JSON数据逐行比对 | 核心指标ID: `[1,2,3,4,5]` | 1. 页面上的“指标名称”与JSON中的完全一致 <br> 2. 页面上的“阈值”与JSON中的完全一致 <br> 3. 例如：pH的阈值为 `6.5-8.5`，COD的阈值为 `≤ 100` | ✅ 已自动化 |
| TC-DATA-002 | **表格列完整性-必填列无空值** | lab | water-indicator | P2 | 数据库有完整的22条水质指标数据 | 1. 访问目标页面 <br> 2. 获取“指标名称”、“单位”、“阈值”三列的数据 <br> 3. 遍历每一行，检查这三列的值是否为空字符串 `""` | 默认环境 | 所有行的“指标名称”、“单位”、“阈值”均不为空字符串 | 🔄 待开发 |
| TC-DATA-003 | **阈值边界值/格式化显示** | lab | water-indicator | P2 | 1. 通过API将某个指标的阈值更新为 `-9999` <br> 2. 将另一个指标的阈值更新为 `0` <br> 3. 刷新页面缓存 | 1. 登录并访问目标页面 <br> 2. 使用 `get_column_data(2)` 找到包含“-9999”指标的行 <br> 3. 查看该行的第6列（阈值）文本 <br> 4. 重复步骤2-3检查 `0` 的指标 | 注入阈值：`-9999` 和 `0` | 1. 页面显示的`-9999`格式正确，非科学计数法 <br> 2. 页面显示的`0`正确，非空白 | ❌ 不适合（手工执行） |
| TC-AUTH-001 | **越权访问-URL直接访问** | lab | water-indicator | P1 | 1. 准备“工艺操作员”角色的账号（无化验室权限） <br> 2. 此账号已登录 | 1. 使用操作员账号登录系统 <br> 2. 在浏览器地址栏输入 `#/lab/water/indicator` <br> 3. 按回车 <br> 4. 检查页面跳转 | 操作员账号 | 1. 页面跳转至403页面或首页 <br> 2. 显示“无权限访问”提示 <br> 3. 表格内容不可见 | ✅ 必须自动化 |
| TC-AUTH-002 | **角色数据隔离-外包人员数据范围** | lab | water-indicator | P1 | 1. 准备“外包人员”账号（仅能看到前15条指标） <br> 2. 此账号已登录 | 1. 使用外包人员账号登录 <br> 2. 导航至目标页面 <br> 3. 使用 `get_table_row_count()` 获取行数 <br> 4. 使用 `get_column_data(2)` 获取指标名称 | 外包人员账号 | 1. 行数等于15 <br> 2. 指标名称列表不包含“总氰化物” | 🔄 待开发 |
| TC-AUTH-003 | **XSS注入防护验证** | lab | water-indicator | P1 | 1. 通过API将某个指标的“指标名称”更新为包含XSS payload的字符串 <br> 2. 清除该用户浏览器缓存 | 1. 使用有权限账号登录 <br> 2. 访问目标页面 <br> 3. 使用 `get_column_data(2)` 找到包含 `<Script>` 的行 <br> 4. 手动（或通过Selenium脚本）检查该行HTML源码 | 注入payload: `<Script>alert('XSS')</Script>` | 1. 页面展示文本为 `<Script>alert('XSS')</Script>` <br> 2. **无**JavaScript弹窗 <br> 3. 页面源码中`<`和`>`被转义为`&lt;`和`&gt;` | 🔄 待开发（自动化脚本准备数据 + 手工首次验证） |
```