好的，收到您的任务。作为 `test-design` 分类下的 `risk-modeling` Agent，我将基于您提供的 `PAGE_CONTEXT.md` 和 `MODULE_CONTEXT.md` 上下文，为您输出 `RISK_MODEL.md` 和 `BUSINESS_SCENARIOS.md`。

---

## OUTPUT: RISK_MODEL.md

> **文件**: `RISK_MODEL.md`
> **版本**: 1.0
> **模块**: personnel
> **页面**: visitor

### 访客管理页面风险模型

#### 风险总览

| 风险ID | 风险类型 | 风险描述 | 等级 | 缓解措施 | 自动化覆盖状态 |
|--------|----------|----------|------|----------|----------------|
| RISK-VISITOR-001 | 业务风险 | **误删访客记录**：操作员在非“待访”状态下删除访客，导致历史追溯链断裂，影响审计。 | P0 | 实施二次确认弹窗，明确警告“删除后无法恢复”；后端对非“待访”状态记录禁止物理删除（逻辑删除）。 | ❌ 未覆盖 |
| RISK-VISITOR-002 | 业务风险 | **强制离场功能滥用**：操作员对“待访”状态访客误操作“强制离场”，导致状态错乱。 | P1 | 前端按钮“强制离场”只对“在访”状态显示；后端校验状态逻辑，拒绝非“在访”状态的强制离场请求。 | ❌ 未覆盖 |
| RISK-VISITOR-003 | 业务风险 | **未登记直接放行**：访客未经系统登记被保安直接放行，造成安全漏洞和事后追溯困难。 | P1 | 门禁系统与访客系统联动；保安若未经系统登记放行，需事后补录并触发审批。 | ❌ 未覆盖（非UI测试范畴，需集成测试） |
| RISK-VISITOR-004 | 权限风险 | **越权查看/编辑/删除**：普通用户通过URL直接访问编辑接口、批量操作接口，或看到非本人权限范围内的访客记录。 | P0 | 后端所有API接口进行权限校验（RBAC）；前端隐藏无权限的按钮（如“编辑”、“删除”、“导入”）。 | ❌ 未覆盖（需API级权限测试） |
| RISK-VISITOR-005 | 权限风险 | **数据隔离失效**：不同部门/园区的管理员能看到对方管辖范围的访客记录。 | P1 | 后端查询逻辑需增加租户/园区/部门数据隔离条件。 | ❌ 未覆盖 |
| RISK-VISITOR-006 | 数据风险 | **输入边界溢出**：`访客姓名`、`单位`字段超长，`手机号`格式非法（如少于11位、含非数字字符）。 | P1 | 后端API对字符串长度、手机号正则进行严格校验；前端`el-input`设置`maxlength`属性，手机号增加`formatter`。 | ❌ 未覆盖 |
| RISK-VISITOR-007 | 数据风险 | **并发操作**：两个操作员同时编辑同一条访客记录（如一人编辑信息，另一人强制离场），导致数据覆盖或状态不一致。 | P1 | 后端增加乐观锁（`version`字段）或悲观锁；前端提示“数据已被他人修改，请刷新”。 | ❌ 未覆盖 |
| RISK-VISITOR-008 | 数据风险 | **数据逻辑删除与级联问题**：删除访客后，关联的访客通行码、历史记录未作废，导致历史追溯出现空引用或错误。 | P0 | 删除操作需清理与该访客记录关联的临时通行码、门禁记录；历史报表中标记为“已删除”。 | ❌ 未覆盖 |
| RISK-VISITOR-009 | 接口风险 | **批量导入失败处理不当**：CSV/Excel文件格式错误、编码问题导致部分或全部导入失败，页面无明确错误提示。 | P1 | 导入接口返回成功/失败列表；前端展示导入结果详情弹窗，明确错误行数及原因。 | ❌ 未覆盖 |
| RISK-VISITOR-010 | 接口风险 | **Token过期，API请求被拒**：用户长时间操作页面，Token过期后，后端返回401，前端无友好提示（直接白屏或报错）。 | P1 | 前端封装全局HTTP拦截器，对401响应自动跳转登录页并提示“登录已过期，请重新登录”。 | ✅ 已覆盖（BasePage通用逻辑） |
| RISK-VISITOR-011 | 接口风险 | **接口返回空数据/异常数据**：后端接口返回`null`、`undefined`或不符合预期的字段类型，导致前端表格渲染报错。 | P1 | 前端对接口返回数据进行类型校验和空值处理（如`?.`操作符）；增加Sentry/错误日志上报。 | ❌ 未覆盖 |
| RISK-VISITOR-012 | UI/UX风险 | **二次确认弹窗文案不清**：删除弹窗未明确说明删除后果（物理删除/逻辑删除），导致用户误解。 | P2 | 弹窗文案应明确“确认删除该访客记录？此操作不可恢复。” | ❌ 未覆盖 |
| RISK-VISITOR-013 | UI/UX风险 | **搜索无结果提示不明确**：搜索后无数据，页面直接显示空表格，用户不知道是搜索条件有误还是真的没有数据。 | P2 | 空数据状态下增加插画及提示文字“暂无符合条件的访客记录，请调整搜索条件”。 | ❌ 未覆盖 |
| RISK-VISITOR-014 | 性能风险 | **大数据量下表格渲染卡顿**：当访客记录超过万条时，一次性加载到前端表格导致浏览器卡死。 | P1 | 后端接口支持分页查询；前端`el-table`启用虚拟滚动（`el-table-v2`）或懒加载。 | ❌ 未覆盖 |
| RISK-VISITOR-015 | 性能风险 | **频繁搜索导致接口压力**：用户在输入框快速输入时，触发大量无效搜索请求。 | P2 | 搜索按钮增加500ms防抖；或使用`el-input`的`debounce`属性。 | ❌ 未覆盖 |

**结论**：存在 `3` 个 **P0** 风险（误删、权限、数据级联），需优先在测试设计中覆盖。当前自动化用例对该页面高风险场景的覆盖率接近 `0%`。

---

## OUTPUT: BUSINESS_SCENARIOS.md

> **文件**: `BUSINESS_SCENARIOS.md`
> **版本**: 1.0
> **模块**: personnel
> **页面**: visitor
> **依赖输入**: RISK_MODEL.md

### 1. 业务目标

| 目标类型 | 描述 | 置信度 |
|----------|------|--------|
| **核心业务目标** | 管理来访人员信息，确保外部人员进出公司的安全性与可追溯性。 | ✅ |
| **次要业务目标** | 为保安、前台等角色提供快速查询与核验工具，提升通行效率。 | ✅ |
| **非目标** | 此页面不处理办公室内部人员的日常考勤、不处理车辆通行证管理。 | ✅ |

### 2. 角色与旅程

| 角色 | 职责边界 | 典型旅程 | 置信度 |
|------|----------|----------|--------|
| **行政前台** | 管理内部员工及访客来访预约，进行访客的新增、登记、编辑、删除。 | 1. 接到访客预约电话 → 2. 新增访客记录 → 3. 引导访客入内 → 4. 管理访客离场。 | ✅ |
| **保安** | 核实访客身份，确保访客有相应的被访预约。 | 1. 访客到访 → 2. 在系统中搜索访客姓名/电话 → 3. 核对身份与预约信息 → 4. 放行并更新状态为“在访”。 | ✅ |
| **安全管理员** | 审计访客记录，查看访客报表，对违规访客进行强制离场操作。 | 1. 收到门禁报警（访客逗留超时）→ 2. 搜索查询该访客 → 3. 点击“强制离场”按钮 → 4. 查看操作日志。 | ✅ |
| **员工（被访人）** | 系统内部人员，是访客的访问对象，无需直接登录该系统。 | （该角色不操作此页面，仅被引用） | ⚠️ |
| **财务人员** | （无关） | 无旅程。 | ❓ |

### 3. 业务流程

| 流程ID | 流程类型 | 流程描述 | 涉及角色 | 关联风险 |
|--------|----------|----------|----------|----------|
| FW-001 | **Happy Path** | **访客预约到访**： <br>1. 前台/被访人发起，登记访客信息 → <br>2. 系统生成预约记录（状态: 待访） → <br>3. 访客到访，保安核对身份 → <br>4. 保安更新状态为“在访” → <br>5. 访客离场，保安/前台更新状态为“已离场”。 | 前台，保安 | RISK-VISITOR-004 |
| FW-002 | **Alternative Path** | **访客未预约直接到访**： <br>1. 访客未提前登记，直接到前台 → <br>2. 前台电话联系被访人确认 → <br>3. 前台手动新增访客记录 → <br>4. 进入FW-001的第3步。 | 前台，保安 | RISK-VISITOR-003 |
| FW-003 | **Alternative Path** | **访客违规逗留，强制离场**： <br>1. 访客计划离场但未办理离场手续 → <br>2. 安全管理员发现或系统超时报警 → <br>3. 安全管理员在系统中搜索到该“在访”状态的访客 → <br>4. 点击“强制离场”按钮 → <br>5. 系统更新状态为“已离场”，并记录操作人。 | 安全管理员 | RISK-VISITOR-002 |
| FW-004 | **异常中断流程** | **访客信息录入错误，需要删除并重新录入**：<br>1. 前台在“待访”状态下发现信息录入错误 → <br>2. 点击删除按钮 → <br>3. 二次确认弹窗 → <br>4. 点“确定”删除（逻辑删除） → <br>5. 重新执行FW-001。 | 前台 | RISK-VISITOR-001 |

### 4. 业务规则

| 规则ID | 规则类型 | 具体规则描述 | 置信度 |
|--------|----------|-------------|--------|
| BR-001 | 数据校验 | **手机号格式**：访客手机号必须为11位数字，且不能重复（同一个手机号可作为不同访客，但需人证核验）。 | ✅ |
| BR-002 | 数据校验 | **访客姓名**：访客姓名不能为空，不得包含特殊字符（如“`@#$%^&*”），长度限制为 1-50 个字符。 | ✅ |
| BR-003 | 状态流转 | **状态只许正向流转**：访客状态只能从“待访” → “在访” → “已离场”，不允许回退（如“已离场”不可再变回“待访”）。 | ✅ |
| BR-004 | 业务约束 | **未登记的访客不能放行**：保安不能对系统中不存在的记录手动“放行”，必须在系统中创建记录后才能执行“放行”操作。 | ✅ |
| BR-005 | 操作约束 | **强制离场的触发条件**：“强制离场”操作只对“在访”状态的访客生效；对“待访”和“已离场”状态，该操作按钮不展示或置灰。 | ✅ |

### 5. 数据流

| 数据流方向 | 描述 | 涉及页面/模块 | 置信度 |
|------------|------|---------------|--------|
| **数据来源（创建）** | 访客数据在此页面通过“新增访客”或“批量导入”功能创建。 | 访客管理页面 | ✅ |
| **数据变换（编辑/更新）** | 用户在列表中更新访客信息、改变访客在“在访/已离场”间的状态。 | 访客管理页面 | ✅ |
| **数据消费（下游）** | 1. **门禁系统**：访客被标记为“在访”后，生成临时通行码（或关联人脸识别）。<br>2. **监控系统/门禁日志**：记录访客进出记录。<br>3. **报表系统**：日/月访客报表使用此数据。 | 门禁模块，监控模块，报表模块 | ⚠️ |
| **数据一致性** | **删除时**：删除访客记录（逻辑删除）时，需同步回收该访客的所有未过期通行码，确保门禁系统安全。 | 访客管理页面，门禁系统 | ✅ |

### 6. 业务风险→场景映射

| 场景ID | 业务场景描述 | 关联风险 | 覆盖状态 |
|--------|-------------|----------|----------|
| BS-001 | 前台新增访客并成功走完“待访 → 在访 → 已离场”全流程。 | RISK-VISITOR-004 | 🔴 未覆盖 |
| BS-002 | 操作员在访客“待访”状态下，错误地单击“删除”按钮，验证二次确认弹窗及删除后的逻辑处理（通行码回收）。 | RISK-VISITOR-001, RISK-VISITOR-008 | 🔴 未覆盖 |
| BS-003 | 安全管理员对一个“在访”状态的访客执行“强制离场”操作，验证状态变更及日志记录。 | RISK-VISITOR-002 | 🔴 未覆盖 |
| BS-004 | 操作员尝试对“待访”或“已离场”访客执行“强制离场”，验证按钮是否隐藏或接口是否拒绝。 | RISK-VISITOR-002 | 🔴 未覆盖 |
| BS-005 | 验证访客手机号填写非11位数字（如10位、12位、含字母）或为空时的新增/导入失败情况。 | RISK-VISITOR-006 | 🔴 未覆盖 |
| BS-006 | 用户A编辑访客信息后，用户B也尝试编辑同一条记录（并发场景），验证后端锁机制（乐观锁）的提示。 | RISK-VISITOR-007 | 🔴 未覆盖 |
| BS-007 | 普通角色（如保安）通过URL直接访问删除/编辑接口的场景，验证后端权限校验。 | RISK-VISITOR-004 | 🔴 未覆盖 |
| BS-008 | 查询无结果时（如输入不存在的访客姓名），验证页面是否显示空状态提示。 | RISK-VISITOR-013 | 🔴 未覆盖 |

> **结论**: 总计 `8` 个关键业务场景，**当前自动化覆盖率为 0%**。所有业务场景均需作为测试设计的输入进行覆盖。