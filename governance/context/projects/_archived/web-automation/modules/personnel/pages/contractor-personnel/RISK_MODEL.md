好的，我将为你输出承包商人员管理页面的风险模型和业务场景模型。从页面上下文、代码和已知缺陷出发，我们将深入挖掘业务、权限、数据等维度的潜在问题。

---

## RISK_MODEL.md

```markdown
# RISK_MODEL — personnel / contractor-personnel

## 1. 业务风险

| 风险ID | 风险描述 | 等级 | 缓解措施 / 备注 |
|--------|----------|------|-----------------|
| RISK-BIZ-001 | 误操作导致人员数据丢失（如：误删关键承包商人员，导致现场无人可用） | P0 | 1. 前端删除操作必须二次确认（el-message-box）；2. 后端实现逻辑删除或归档，而非物理删除；3. 操作日志可追溯 |
| RISK-BIZ-002 | 人员状态（入场/退场）与现场实际情况不一致（状态流转错误） | P1 | 1. 启停用操作需明确含义（停用=退场？）；2. 状态变更记录需保存时间戳和操作人；3. 现有测试未覆盖状态变更的上下游一致性子 |
| RISK-BIZ-003 | 非法的退场操作：强制退场但未关联退场手续或设备归还 | P1 | 1. 退场时若关联工单或设备，应提示关联项并限制退场；2. 业务规则需要明确确认 |
| RISK-BIZ-004 | 同身份证号重复录入（一名承包商人员在系统中出现两次） | P0 | 1. 后端API对身份证号做唯一性校验；2. 前端在保存时应有友好提示；3. 测试需验证重复创建场景 |

## 2. 权限风险

| 风险ID | 风险描述 | 等级 | 缓解措施 / 备注 |
|--------|----------|------|-----------------|
| RISK-PERM-001 | `personnel:contractor:person:create` 缺失但无权限用户通过直接调用API新增人员 | P0 | 1. 后端必须校验所有写操作的权限；2. 测试需验证调用API越权场景 |
| RISK-PERM-002 | `personnel:contractor:person:edit` 缺失但用户界面可见编辑按钮（权限配置错误） | P1 | 1. 权限配置需前后端联动；2. 前端按钮应基于权限标识判断 `v-if` |
| RISK-PERM-003 | 承包商管理员看到所有承包商人员，而非仅其所属单位人员 | P0 | 1. 后端查询接口必须添加 `unitId` 过滤；2. 测试需创建不同单位数据验证数据隔离 |
| RISK-PERM-004 | 新增/编辑人员时的“所属承包商”下拉列表可见所有单位（数据越权） | P1 | 1. 下拉列表数据接口应返回安全数据；2. admin角色能看到所有，但承包商管理员只能看到本单位 |

## 3. 数据风险

| 风险ID | 风险描述 | 等级 | 缓解措施 / 备注 |
|--------|----------|------|-----------------|
| RISK-DATA-001 | 新增表单必填项（如姓名、身份证）为空提交 | P1 | 1. 前端增加 `required` 规则校验；2. 后端必须做空值校验 |
| RISK-DATA-002 | 超长字符串输入（姓名/岗位/手机号超过字段长度） | P1 | 1. 后端字段需限制长度（如 name VARCHAR(50)）；2. 前端也需做 maxlength 限制或提示 |
| RISK-DATA-003 | SQL注入：搜索框输入 `' OR 1=1 --` | P1 | 1. 后端必须使用参数化查询；2. 建议对特殊字符做转义或过滤 |
| RISK-DATA-004 | 身份证号格式校验缺失：允许非法身份证号或非18位 | P1 | 1. 后端必须做身份证格式+校验位验证；2. 前端也推荐做基础格式校验 |
| RISK-DATA-005 | 手机号格式校验缺失：允许非法手机号 | P1 | 1. 后端做正则校验；2. 前端做 input type=tel + 正则 |
| RISK-DATA-006 | 并发操作：两个管理员同时编辑同一个人员，后者覆盖前者 | P1 | 1. 后端使用乐观锁（版本号）或时间戳冲突检测；2. 冲突时提示用户刷新重试 |
| RISK-DATA-007 | 数据删除后，关联的业务工单/考勤记录产生孤儿数据 | P0 | 1. 删除必须检查是否被引用，如有引用则拒绝删除或提示；2. 使用软删除，保留人员记录但标记为不可用 |
| RISK-DATA-008 | 分页、搜索大数据量时的空数据、全量数据、模糊搜索性能 | P1 | 1. 后端接口需支持分页、LIKE查询索引优化；2. 前端等待loading动画必须及时关闭（关联 EP-003） |

## 4. 接口风险

| 风险ID | 风险描述 | 等级 | 缓解措施 / 备注 |
|--------|----------|------|-----------------|
| RISK-API-001 | 搜索接口超时/500错误时，前端无loading或错误提示 | P1 | 1. 需增加前端loading超时处理，显示“请求失败，请重试”；2. 测试需Mock超时场景 |
| RISK-API-002 | 接口返回字段变更造成前端的属性名不匹配 | P1 | 1. 前后端需约定API契约（Swagger）；2. 测试中使用schema校验（如pytest-json-schema） |
| RISK-API-003 | Token过期或未认证时请求API，返回401但前端未重定向到登录页 | P1 | 1. API拦截器需统一处理401；2. 前端axios响应拦截需跳转登录 |
| RISK-API-004 | 新增/编辑接口幂等性缺失：重复请求造成重复数据（如网络重发） | P2 | 1. 前端按钮在提交后置为 `disabled`；2. 后端增加业务唯一键约束或幂等令牌 |
| RISK-API-005 | 分页接口返回空数组（[]）时，loading遮罩未正常清除（EP-003） | P1 | 1. 等待逻辑需兼容 `空数组` 场景；2. 监听 “暂无数据” 提示的出现 |

## 5. UI/UX 风险

| 风险ID | 风险描述 | 等级 | 缓解措施 / 备注 |
|--------|----------|------|-----------------|
| RISK-UI-001 | 在复杂图表、公共搜索区加载时，元素被loading遮罩遮挡而不可点击 | P1 | 1. 等待loading消失后再定位操作；2. 详见 EP-003 |
| RISK-UI-002 | 搜索后表格刷新导致已存在的el-select下拉框元素失效（stale element） | P1 | 1. 每次操作前重新定位元素，避免缓存旧的 `WebElement` |
| RISK-UI-003 | 弹窗（新增/编辑）中各表单元素定位困难（嵌套在 `el-dialog` + `el-form-item` 中） | P1 | 1. 封装 `_get_dialog_form_item` 方法按 label 文本定位；2. 自动化测试需依赖唯一 label 值 |
| RISK-UI-004 | 不同角色或权限下，页面按钮（新增/编辑/删除）可见性不一致导致定位失败 | P1 | 1. 可预先判断按钮的存在性，不存在则跳过 |
| RISK-UI-005 | Toast提示（成功/失败）一闪而过，自动化校验困难 | P2 | 1. 使用 `pytest` 日志记录；2. 可加入 `time.sleep(1)` 手动等待捕获 |

## 6. 性能风险

| 风险ID | 风险描述 | 等级 | 缓解措施 / 备注 |
|--------|----------|------|-----------------|
| RISK-PERF-001 | 表格一次加载数千条数据时，UI渲染卡顿或假死 | P1 | 1. 确保后端默认分页条数不超过100；2. 前端可使用虚拟滚动（如 `el-table-v2`） |
| RISK-PERF-002 | 频繁点击搜索按钮（如每1秒点一次）造成大量请求堆积 | P2 | 1. 前端对搜索按钮做节流（throttle）或防抖（debounce）；2. 测试考虑按钮连续点击场景 |

## 配置映射

| 配置项 | 值 | 备注 |
|--------|-----|------|
| 风险总数量 | 22 | |
| P0 | 4 | RISK-BIZ-001, RISK-BIZ-004, RISK-PERM-001, RISK-PERM-003 |
| P1 | 16 | |
| P2 | 2 | |
| 现有用例覆盖高风险场景 | 见下方说明 | 当前测试仅覆盖搜索、分页、新增闭环，未覆盖权限、并发、数据边界等 |

### 现有测试覆盖说明
- 当前 `test_contractor_personnel.py` 主要测试：页面展示（CP-001）、分页（CP-002）、搜索（CP-003）、新增闭环（CP-00*）。
- **未覆盖场景**：权限/数据越权、并发编辑、错误格式数据（非法身份证/手机号）、超长字符串、SQL注入、接口容错（超时/500/空数组）等。
- **高风险未覆盖**：P0级别的越权、唯一性校验和P1级别的表单校验、数据边界等都缺乏自动化测试。

---

## BUSINESS_SCENARIOS.md

```markdown
# BUSINESS_SCENARIOS — personnel / contractor-personnel

## 1. 业务目标 (Business Goal)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-P-001 | 业务目标 | **核心目标**：企业管理人员确保承包商内所有作业人员的资质、身份、进场状态得到记录和追踪，保障现场安全合规。 | admin， contractor_admin | contractor-personnel | RISK-BIZ-001 | ✅ 已确认 |
| BS-P-002 | 业务目标 | **次要目标**：实现对人员信息（姓名、身份证、单位、联系方式）的快速检索和验证，应对现场查验、应急联系等需求。 | admin， contractor_admin | contractor-personnel | RISK-BIZ-004 | ✅ 已确认 |
| BS-P-003 | 业务目标 | **非目标**：本页面不处理人员的考勤、工时、工资以及培训记录等更广泛的人力资源管理。 | - | - | - | ✅ 已确认 |

## 2. 角色与旅程 (Roles & Journeys)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-P-010 | 角色与旅程 | **Admin旅程**：1. 登录 → 2. 导航到承包商人员 → 3. 查看/搜索全部单位人员列表 → 4. 新增/编辑任意单位人员 → 5. 对人员进行停用/删除 → 6. 完成管理后退出。旅程涵盖全量数据管理。 | admin | personnel， contractor-personnel | RISK-PERM-003 | ✅ 已确认 |
| BS-P-011 | 角色与旅程 | **承包商管理员旅程**：1. 登录 → 2. 导航到承包商人员 → 3. 仅查看/搜索本单位人员列表 → 4. 新增/编辑本单位人员（需选本单位） → 5. 放弃（或不允许）删除本单位的“关键人员” → 6. 登出。旅程强调数据隔离。 | contractor_admin | personnel， contractor-personnel | RISK-PERM-003 | ✅ 已确认 |
| BS-P-012 | 角色与旅程 | **合作场景**：admin将某人员从“A单位”调至“B单位”。1. admin删除“A单位”该人员记录 → 2. 人员数据归档（软删除） → 3. contractor_admin（B单位）新增该人员（复用身份证信息）。需要检验系统对身份证的去重逻辑是否允许这种跨单位操作。 | admin， contractor_admin | contractor-personnel | RISK-BIZ-004 | ⚠️ 推断 |

## 3. 业务流程 (Workflows)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-P-020 | 业务流程 | **Happy Path: 新增一个承包商人员**：点击[新增] → 弹窗 → 填写姓名、身份证号、选择所属单位、录入岗位/电话 → 点击[保存] → 系统保存 → 弹窗自动关闭 → 表格刷新并展示新行 → 页面显示操作成功toast。 | admin， contractor_admin | contractor-personnel | RISK-BIZ-001， RISK-DATA-001 | ✅ 已确认 |
| BS-P-021 | 业务流程 | **Alternative Path: 新增时必填校验失败**：点击[新增] → 弹窗 → 不填写姓名或身份证 → 点击[保存] → 浏览器或后端提示“姓名不能为空” → 表格不刷新 → 表单可修正后再次保存。 | admin， contractor_admin | contractor-personnel | RISK-DATA-001， RISK-DATA-002 | ✅ 已确认 |
| BS-P-022 | 业务流程 | **Alternative Path: 新增时身份证号重复**：输入一个已存在人员的身份证号 → 点击[保存] → 后端返回业务错误（如“身份证号已存在”） → 前端表单展示错误提示 → 表格不刷新。 | admin， contractor_admin | contractor-personnel | RISK-BIZ-004 | ✅ 已确认 |
| BS-P-023 | 业务流程 | **Abnormal Path: 网络中断/服务器500**：操作中网络断开 → 点击[保存]后，toast显示“请求失败”或页面不跳转 → 用户需手动刷新并查数据最终状态。 | admin， contractor_admin | contractor-personnel | RISK-API-001 | ⚠️ 推断 |
| BS-P-024 | 业务流程 | **Abnormal Path: 删除已被引用的承包商人员**：某人员已关联正在执行的施工任务，点击[删除] → 前端向接口发起删除 → 后端检查引用，返回失败信息 → 前端toast提示“该人员存在关联任务，无法删除”。 | admin | contractor-personnel | RISK-DATA-007 | ⚠️ 推断 |
| BS-P-025 | 业务流程 | **异常中断及恢复流程：Token过期**：用户在编辑弹窗填写数据时Token过期 → 点击[保存]后，API返回401 → 前端axios拦截器弹出“登录过期”并跳转登录页 → 用户登录后需要重新定位到页面，已填写的表单数据可能丢失。 | admin， contractor_admin | login， contractor-personnel | RISK-API-003 | ⚠️ 推断 |

## 4. 业务规则 (Business Rules)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-P-030 | 业务规则 | **人员身份证号唯一性校验**：系统中每个身份证号只能对应一个记录。新增或编辑时，若输入已存在的身份证号，后端返回“身份证号已存在”错误。 | admin， contractor_admin | contractor-personnel | RISK-BIZ-004 | ✅ 已确认 |
| BS-P-031 | 业务规则 | **入场状态与业务流程联动**：人员被“停用”时，其入场状态自动变为“退场”，并可能触发相关的退场审批或设备归还流程。 | admin | contractor-personnel | RISK-BIZ-002 | ⚠️ 推断 |
| BS-P-032 | 业务规则 | **数据可见性隔离**：`contractor_admin` 只能看到和编辑本单位的承包商人员。查询API需要自动加上单位ID过滤。 | contractor_admin | contractor-personnel | RISK-PERM-003 | ✅ 已确认 |
| BS-P-033 | 业务规则 | **必须选择“所属承包商单位”**：新增人员时，“所属承包商单位”是必填项，且下拉选项需根据用户角色（admin/contractor_admin）进行过滤。 | admin， contractor_admin | contractor-personnel | RISK-PERM-004 | ✅ 已确认 |
| BS-P-034 | 业务规则 | **关键人员不允许删除**：如果当前人员正处于“入场”状态或有关联的工单/任务，则禁止删除操作。 | admin | contractor-personnel | RISK-DATA-007 | ⚠️ 推断 |

## 5. 数据流 (Data Flow)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-P-040 | 数据流 | **数据来源**：新增人员的初始数据源是“新增弹窗”表单。**数据转换**：前端提交JSON；后端处理后存入 `personnel` 表。**数据消费**：数据在“人员管理”模块的列表、详情页被消费。 | admin， contractor_admin | contractor-personnel | - | ✅ 已确认 |
| BS-P-041 | 数据流 | **数据来源**：搜索/列表页的数据由后端API实时提供（`/api/personnel/contractor/list`）。**数据转换**：后端查询根据当前用户的单位ID做数据过滤（数据隔离）。**数据消费**：前端渲染表格、处理分页。 | admin， contractor_admin | contractor-personnel | RISK-PERM-003 | ✅ 已确认 |
| BS-P-042 | 数据流 | **跨页面数据一致性要求**：如果人员被删除或者状态被停用，该系统人员考勤页面、门禁通行记录等相关数据需要同步标记为“无效”或“已退场”，以确保数据完整性。 | admin， contractor_admin | contractor-personnel， attendance， access-control | RISK-DATA-007 | ⚠️ 推断 |

## 6. 业务风险→场景映射 (Risk-to-Scenario Mapping)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-P-100 | 风险映射 | **场景：admin误删在岗人员。** 模拟在岗人员被强制删除。业务流程：admin登录→定位到该人员→点击[删除]按钮→确认弹窗→提交删除接口。 | admin | contractor-personnel | RISK-BIZ-001 | ✅ 已确认 |
| BS-P-101 | 风险映射 | **场景：contractor_admin越权查看非本单位人员。** 模拟承包商管理员通过直接修改API参数或手动输入URL ID等方式，尝试访问不属于本单位的其他人员信息。 | contractor_admin | contractor-personnel | RISK-PERM-003 | ✅ 已确认 |
| BS-P-102 | 风险映射 | **场景：重复身份证号尝试新增。** 使用相同的身份证号连续两次调用新增API。业务流程：自动化直接调用新增接口→保存第一次→再调用第二次→检查响应状态码及错误信息。 | admin | contractor-personnel | RISK-BIZ-004 | ✅ 已确认 |
| BS-P-103 | 风险映射 | **场景：接口返回空数组时，loading遮罩状态。** 触发一个没有数据的搜索条件，检查前端是否正常显示“暂无数据”并且loading动画消失。 | admin， contractor_admin | contractor-personnel | RISK-API-005 | ⚠️ 推断 |

---

### 未覆盖的高风险场景列表
基于上述模型，以下高风险场景当前测试脚本未覆盖：
1.  **数据校验**（P1）：超长/含特殊字符的姓名、手机号、身份证的非数字字符等。
2.  **权限测试**（P0）：越权操作（REST API直接调用）、权限配置异常导致的越权。
3.  **并发测试**（P1）：两个管理员同时编辑并提交同一人员的冲突场景。
4.  **接口容错**（P1+）：后端返回500、超时、空数组等情况下的前端表现。
5.  **跨页面E2E**（P0的FP-012）：删除人员后，考勤/门禁系统是否同步更新的完整E2E流程。
```