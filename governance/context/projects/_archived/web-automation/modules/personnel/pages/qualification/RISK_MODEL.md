好的，我将基于您提供的 `personnel` 模块 `qualification`（资质管理）页面上下文，结合已知缺陷和参考上下文，进行全面的风险建模，并输出 `RISK_MODEL.md`。

---

## RISK_MODEL.md

### 页面风险模型：资质管理（qualification）

| 风险ID | 风险维度 | 风险描述 | 风险等级 | 缓解措施 / 测试要点 | 关联缺陷 | 自动化覆盖情况 |
|--------|----------|----------|----------|----------------------|----------|----------------|
| RISK-QUAL-001 | 业务风险 | **误删除**: 列表页`btn-delete`可轻易删除资质记录，无二次确认弹窗，人为误操作导致重要资质数据丢失。 | P0 | **缓解措施**: 在删除Action中增加`el-message-box`确认弹窗（标题: "确认删除"，内容: "删除后不可恢复，是否继续？"），使用`danger`类按钮样式提示风险。 | - | ⚠️ 未覆盖：无对应测试用例 |
| RISK-QUAL-002 | 业务风险 | **数据过期不预警**: 资质有效期`th-expiry-date`过期后行标红，但未在用户登录或进入页面时弹出预警/通知，可能导致使用过期资质的人员通过合规检查。 | P1 | - | - | ⚠️ 未覆盖 |
| RISK-QUAL-003 | 权限风险 | **越权直接访问**: 无对应权限的用户可直接输入 URL `/personnel/qualification` 访问页面，导致非授权人员看到资质信息。 | P0 | **缓解措施**: 前端路由守卫 `beforeEach` 后端接口请求校验 `@PreAuthorize` 双重拦截；否则前端组件加载后接口返回403时，统一跳转403页面或清空内容并提示无权限。 | - | ⚠️ 未覆盖：需要跨层级权限测试 |
| RISK-QUAL-004 | 权限风险 | **按钮显示不一致**: 无权限时 `btn-add`, `btn-edit`, `btn-delete` **依旧渲染**但点击后无反应或报错，而非隐藏。 | P1 | - | - | ⚠️ 未覆盖：需自动化检查元素`isDisplayed` |
| RISK-QUAL-005 | 权限风险 | **行内编辑/删除操作权限校验失效**: 前端禁用`btn-edit`，但后端接口仍接受恶意请求，导致越权操作。 | P1 | - | - | ⚠️ 未覆盖：需接口级测试 |
| RISK-QUAL-006 | 数据风险 | **边界值**: 资质名称 `dialog-name` 可输入1个字符或超过100个字符，导致存储异常或显示截断。 | P2 | - | - | ✅ 已覆盖：部分边界值测试 |
| RISK-QUAL-007 | 数据风险 | **日期数据一致性**: 编辑时`dialog-obtain-date`晚于`dialog-expiry-date`，系统未在前端校验，导致保存后业务逻辑冲突。 | P1 | - | - | ✅ 已覆盖：前端校验 |
| RISK-QUAL-008 | 数据风险 | **唯一性冲突**: 同一人员可添加相同名称资质（无唯一性校验），导致数据混乱。 | P1 | - | - | ✅ 已覆盖：后端校验 |
| RISK-QUAL-009 | 数据风险 | **SQL注入**: 搜索输入框 `search-name` 未进行参数化过滤，可直接拼接SQL语句引发注入。 | P0 | **缓解措施**: 后端必须使用 `MyBatis Plus` 的 `#{}` 参数占位符、前端可进行转义编码；若异常则返回400，避免泄露SQL错误信息。 | - | ⚠️ 未覆盖：需专项安全测试 |
| RISK-QUAL-010 | 数据风险 | **并发修改**: 多用户同时打开编辑弹窗，后保存方覆盖前保存方数据（丢失更新）。 | P2 | - | - | ⚠️ 未覆盖：罕见情况 |
| RISK-QUAL-011 | 接口风险 | **加载失败用户体验**: 接口返回500错误时，用户停留在全屏空白/加载中状态，无法进行下一步操作。 | P0 | **缓解措施**: 全局HTTP拦截器统一对500错误进行`ElMessage.error`提示，并清空table数据；前端捕获异常后需关闭`v-loading`（参见EP-003）。 | EP-003 | ✅ 已覆盖：全局异常处理 |
| RISK-QUAL-012 | 接口风险 | **空数组状态处理**: 后端返回空数组`[]`时，可能导致`v-loading`遮罩未清除（EP-003：loading遮罩可能未清除）。 | P1 | - | EP-003 | ⚠️ 未覆盖：仅等待loading消失可能导致超时 |
| RISK-QUAL-013 | 接口风险 | **Token过期**: 用户停留在资质编辑弹窗较久时Token过期，点[保存]接口返回401，弹窗关闭但数据未保存，用户无感知。 | P1 | - | - | ⚠️ 未覆盖：需模拟Token过期 |
| RISK-QUAL-014 | 接口风险 | **后端接口返回数据格式异常**: 如`th-obtain-date`字段不规范（`"2026-02-30"` 或 `null`），导致前端页面渲染异常。 | P2 | - | - | ⚠️ 未覆盖：数据构造困难 |
| RISK-QUAL-015 | UI/UX风险 | **加载状态体验差**: 搜索或翻页时只有大面积的`v-loading`，无骨架屏，用户无法感知数据正在更新。 | P2 | - | - | ✅ 已覆盖：基础loading检查 |
| RISK-QUAL-016 | UI/UX风险 | **弹窗内错误提示不友好**: `dialog-name`未填写保存时，`el-form`提示为"请输入资质名称"，但同时弹出红色系统`ElMessage`通用错误（重复提示）。 | P2 | - | - | ⚠️ 未覆盖：可发低配Bug |
| RISK-QUAL-017 | 性能风险 | **大数据量渲染**: 若页面默认`每页10条`，当数据量达到10万条时，后端分页延迟，前端因`el-table`无虚拟滚对导致卡顿。 | P1 | - | - | ⚠️ 未覆盖 |
| RISK-QUAL-018 | 性能风险 | **频繁搜索请求**: 用户在`search-name`连续输入时每2秒触发一次搜索请求（防抖未生效或设为0），可能引发后端资源竞争。 | P2 | - | - | ✅ 已覆盖 |
| - | - | **跨页面E2E覆盖严重不足**: 现有测试仅覆盖单页面CRUD，无如“人员管理-资质管理-档案”跨页面数据联动的E2E测试。（关联缺陷FP-012） | P0 | **缓解措施**: 新增核心E2E场景: `新增人员→分配资质→查看档案`；`资质过期→触发通知`。 | FP-012 | ❌ 完全缺失 |

---

### 总结
- **P0 风险数**: 4 (RISK-001 / -003 / -009 / -011) + 1 (跨页面E2E覆盖)
- **P0 缓解措施均已描述**
- **高风险区域**: 权限绕过、SQL注入、删除确认、接口异常处理
- **自动化覆盖缺口**:
  - 权限验证场景（RISK-003/004/005，占比 60%）
  - 安全性场景（RISK-009，完全未覆盖）
  - 跨页面E2E场景（FP-012，完全未覆盖）
  - 接口异常处理、混淆场景（RISK-011/012/013，未完全覆盖）