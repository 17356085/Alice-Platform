好的，已经收到您的输入。作为 risk-modeling Skill，我现在将基于您提供的 `Page Object` 代码、测试脚本和页面上下文，为您产出 `RISK_MODEL.md` 和 `BUSINESS_SCENARIOS.md`。

---

## RISK_MODEL.md

# 风险模型 — personnel / entry-record

**页面**: 入场记录
**模块**: 人员管理 → 承包商管理
**版本**: 1.0
**最后更新**: 2026-06-18

---

### 风险总览

| 维度 | P0 | P1 | P2 | 总计 |
|------|----|----|----|------|
| 业务风险 | 0 | 2 | 1 | 3 |
| 权限风险 | 1 | 2 | 0 | 3 |
| 数据风险 | 0 | 1 | 3 | 4 |
| 接口风险 | 0 | 1 | 2 | 3 |
| UI/UX风险 | 0 | 0 | 2 | 2 |
| 性能风险 | 0 | 0 | 2 | 2 |
| **总计** | **1** | **6** | **10** | **17** |

---

### 1. 业务风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| RISK-BUS-001 | **误导出**：无权限用户（如审批人）通过触发导出操作，获取非授权的数据记录 | P1 | 1. 导出按钮根据用户角色显隐/灰化<br>2. 服务端导出接口校验权限 | 未覆盖 |
| RISK-BUS-002 | **数据误解**：用户选择过长/跨度过大的日期范围（如十年），导致导出的数据量超大，影响用户体验和系统资源 | P1 | 1. 前端增加日期范围最大跨度（如1年）校验<br>2. 服务端限制单次导出最大行数并给出提示 | 未覆盖 |
| RISK-BUS-003 | **搜索失效**：搜索条件组合（姓名+单位+日期）返回空结果，用户误以为系统无数据，导致业务决策错误 | P2 | 1. 空结果时明确提示“未找到匹配记录”，而非显示“暂无数据”（后者容易与无权限混淆） | 部分覆盖（测试脚本仅检查 `row_count>=0`） |

### 2. 权限风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| RISK-PERM-001 | **越权访问**：普通承包商人员通过URL直接访问 `#/personnel/contractor/record`，看到所有入场记录 | **P0** | 1. 服务端对所有列表查询接口执行角色鉴权<br>2. 前端路由守卫校验用户角色 | 未覆盖 |
| RISK-PERM-002 | **越权查看**：审批人角色能看到所有承包商记录，但需求要求只能看到自己审批过的记录 | P1 | 接口层增加数据权限过滤（仅返回审批人=当前用户的数据） | 未覆盖 |
| RISK-PERM-003 | **导出越权**：安全管理员具有导出权限，但使用API直接调用导出接口，绕过前端角色控制 | P1 | 导出接口必须校验用户角色和数据权限（与列表接口同步） | 未覆盖 |

### 3. 数据风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| RISK-DATA-001 | **SQL注入**：姓名搜索框输入 `' OR 1=1--` 等SQL注入关键词，可能导致数据泄露 | P1 | 服务端必须参数化查询，禁止拼接SQL；前端可限制特殊字符 | 未覆盖 |
| RISK-DATA-002 | **搜索边界值**：姓名搜索框输入超长字符串（>100字符），可能导致查询异常或页面卡死 | P2 | 1. 前端限制输入长度（如50字符）<br>2. 服务端对输入参数长度校验 | 未覆盖 |
| RISK-DATA-003 | **日期边界值**：日期范围搜索时，`开始日期 > 结束日期`，系统应给出友好提示而非直接报错 | P2 | 前端校验日期先后关系；服务端返回明确错误提示 | 未覆盖 |
| RISK-DATA-004 | **编码问题**：姓名搜索输入生僻字/Emoji/HTML标签，结果可能不符合预期（如搜索“张𪚥”返回空） | P2 | 服务端确保数据库/接口支持UTF-8-MB4编码；前端转义特殊字符 | 未覆盖 |

### 4. 接口风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| RISK-API-001 | **导出接口超时**：大量数据导出时接口耗时>30s，浏览器可能因无响应而断开连接，用户无法获取文件 | P1 | 1. 后端导出改为异步任务队列，前端轮询下载链接<br>2. 单次导出条数上限（如10万条） | 未覆盖 |
| RISK-API-002 | **Token过期**：用户停留页面时间过长后点击“导出”，因token过期导致401报错，且前端无友好提示 | P2 | 1. 全局捕获401并自动刷新Token<br>2. 导出前增加Token有效性检查 | 未覆盖 |
| RISK-API-003 | **接口返回异常格式**：列表接口返回非JSON格式（如HTML错误页），前端页面白屏/报错 | P2 | 全局HTTP拦截器处理非JSON响应，显示“系统繁忙，请稍候” | 未覆盖 |

### 5. UI/UX风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| RISK-UI-001 | **详情弹窗内容错位**：入场记录详情信息过多时，弹窗可能出现滚动条或内容超出弹窗边框 | P2 | 弹窗内容区域设置固定最大高度并启用内部滚动 | 未覆盖 |
| RISK-UI-002 | **日期选择器交互异常**：使用Element Plus日期选择器时，手动输入的日期格式不受支持（如 `2024.01.01`），导致静默失败 | P2 | 1. 日期选择器增加`value-format`控制<br>2. 输入后触发校验并高亮边界 | 未覆盖 |

### 6. 性能风险

| 风险ID | 风险描述 | 等级 | 缓解措施 | 现有自动化覆盖 |
|--------|----------|------|----------|----------------|
| RISK-PERF-001 | **大数据量下页面加载缓慢**：入场记录数据量超过10万条时，首次加载和搜索响应时间超过5秒 | P2 | 1. 服务端默认分页（每页≤50条）<br>2. 列表接口支持服务端排序和过滤 | 未覆盖 |
| RISK-PERF-002 | **并发导出导致服务器过载**：多个用户同时点击导出，可能占用大量服务器内存和IO，影响其他用户列表查询 | P2 | 导出接口增加限流（如每分钟最多5次导出请求） | 未覆盖 |

---

## BUSINESS_SCENARIOS.md

# 业务场景模型 — personnel / entry-record

**页面**: 入场记录
**模块**: 人员管理 → 承包商管理
**版本**: 1.0
**最后更新**: 2026-06-18

---

### 1. 业务目标 (Business Goal)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-BG-001 | 业务目标 | **核心目标**：安全管理人员/审批人通过该页面快速查询和导出承包商人员的入场历史记录，确保入场记录的可追溯性和审计合规性。 | admin, 安全管理员, 审批人 | entry-record | — | ✅ |
| BS-BG-002 | 业务目标 | **次要目标**：承包商人员查看自己的入场记录，确认入场审批状态。 | 普通承包商人员 | entry-record | RISK-PERM-001 | ✅ |
| BS-BG-003 | 业务目标 | **非目标**：本页面不处理入场申请、审批或修改记录；新增/编辑/删除行为在“入场审批”页面完成。 | — | — | — | ✅ |

### 2. 角色与旅程 (Roles & Journeys)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-RJ-001 | 角色与旅程 | **admin**：登录 → 导航至入场记录页 → 搜索特定承包商人员的入场记录 → 点击“详情”查看完整入场信息 → 点击“导出”获取全量入场记录 | admin | entry-record, (登录页) | RISK-PERM-003 | ✅ |
| BS-RJ-002 | 角色与旅程 | **安全管理员**：登录 → 导航至入场记录页 → 按“入场日期”筛选近一周记录 → 逐条核对入场人员与审批单是否一致 → 导出核对结果 | 安全管理员 | entry-record | RISK-BUS-001, RISK-PERM-003 | ✅ |
| BS-RJ-003 | 角色与旅程 | **审批人**：登录 → 导航至入场记录页 → 查看自己审批过的记录列表 → 点击详情确认人员信息 → 无需导出 | 审批人 | entry-record | RISK-PERM-002 | ⚠️ |
| BS-RJ-004 | 角色与旅程 | **普通承包商人员**：登录 → 尝试通过URL直接访问入场记录页 → 系统拦截并提示无权限 | 普通承包商人员 | entry-record, (登录页) | RISK-PERM-001 | ✅ |

### 3. 业务流程 (Workflows)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-WF-001 | 业务流程 | **Happy Path**：入场审批通过 → 入场记录自动生成 → 安全管理员进入入场记录页 → 按姓名“张三”搜索 → 表格显示张三的入场记录 → 点击“详情”弹出窗 → 确认姓名/身份证/入场时间/单位信息正确 → 关闭弹窗 | 安全管理员 | entry-approval → entry-record | — | ✅ |
| BS-WF-002 | 业务流程 | **Alternative Path (导出)**：admin进入入场记录页 → 不输入任何搜索条件 → 点击“导出” → 浏览器下载包含全部入场记录的Excel文件 → 打开Excel确认数据列完整 | admin | entry-record | RISK-BUS-001, RISK-API-001 | ✅ |
| BS-WF-003 | 业务流程 | **Alternative Path (空搜索)**：审批人进入入场记录页 → 输入一个不存在的姓名（如“不存在的姓名”） → 点击搜索 → 表格显示“未找到匹配记录” | 审批人 | entry-record | RISK-BUS-003 | ✅ |
| BS-WF-004 | 业务流程 | **异常中断（权限不足）**：普通承包商人员登录系统 → 手动输入URL `#/personnel/contractor/record` → 页面跳转至首页或显示无权限提示 → 无法看到入场记录 | 普通承包商人员 | entry-record, (登录页) | RISK-PERM-001 | ✅ |

### 4. 业务规则 (Business Rules)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-BR-001 | 业务规则 | **记录生成规则**：入场记录在“入场审批”流程通过后自动生成，无需人工创建。记录生成后不可修改/删除。 | — | entry-record, entry-approval | — | ✅ |
| BS-BR-002 | 业务规则 | **搜索规则**：三种搜索条件（姓名/单位/日期范围）之间为“与”关系，搜索结果必须同时满足所有条件。 | — | entry-record | RISK-BUS-003 | ✅ |
| BS-BR-003 | 业务规则 | **导出规则**：导出内容为当前搜索条件下的所有记录（即“所见即所得”），不是全量数据库记录。 | — | entry-record | RISK-BUS-001 | ⚠️ |
| BS-BR-004 | 业务规则 | **权限规则**：admin和安全管理员可搜索查看所有记录；审批人只能查看自己审批通过的记录；普通承包商人员无此页面访问权限。 | admin, 安全管理员, 审批人, 普通承包商人员 | entry-record | RISK-PERM-001, RISK-PERM-002 | ✅ |

### 5. 数据流 (Data Flow)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-DF-001 | 数据流 | **数据来源**：入场记录数据由 `entry-approval`（入场审批）页面在审批通过后创建，通过 `POST /api/contractor/entry/create` 接口写入数据库。 | — | entry-approval → entry-record | — | ✅ |
| BS-DF-002 | 数据流 | **数据变换**：在 `entry-record` 页面对数据进行展示和搜索（姓名模糊匹配、单位精确匹配、日期范围过滤），但不对数据进行任何写操作。 | — | entry-record | — | ✅ |
| BS-DF-003 | 数据流 | **数据消费**：`entry-record` 页面的数据通过 `GET /api/contractor/entry-record/list` 接口供给前端展示；通过 `GET /api/contractor/entry-record/export` 接口供给导出文件。数据不下游消费。 | — | entry-record | — | ✅ |
| BS-DF-004 | 数据流 | **跨页面一致性要求**：当上游 `entry-approval` 页面审批状态变更（如撤销审批），`entry-record` 页面应反映最新的记录状态（如记录消失/状态变更），数据一致性要求为最终一致性≤30秒。 | — | entry-approval, entry-record | — | ⚠️ |

### 6. 业务风险→场景映射 (Risk-to-Scenario Mapping)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
|--------|----------|----------|----------|----------|----------|--------|
| BS-RM-001 | 风险→场景映射 | **PERM-001 越权访问**：普通承包商人员尝试通过URL直接访问入场记录页的场景（BS-RJ-004），映射到风险 `RISK-PERM-001` | 普通承包商人员 | entry-record | RISK-PERM-001 | ✅ |
| BS-RM-002 | 风险→场景映射 | **PERM-002 越权查看**：审批人角色搜索得到非己审批的记录的场景，映射到风险 `RISK-PERM-002`。覆盖用例：审批人搜索“张三” → 返回的记录中审批人列应为当前用户。 | 审批人 | entry-record | RISK-PERM-002 | ⚠️ |
| BS-RM-003 | 风险→场景映射 | **API-001 导出超时**：admin导出100万条数据的场景（BS-WF-002的变体），映射到风险 `RISK-API-001`。覆盖用例：构造大量数据 → 触发导出 → 观察接口响应时间与弹性。 | admin | entry-record | RISK-API-001 | ❓ |
| BS-RM-004 | 风险→场景映射 | **BUS-003 搜索失效**：输入不存在的姓名返回空结果的场景（BS-WF-003），已经覆盖风险 `RISK-BUS-003`。但当前自动化测试仅检查 `row_count >= 0`，需要增强检查点：确认页面提示文本为“未找到匹配记录”。 | 审批人 | entry-record | RISK-BUS-003 | ⚠️ |

---

### 缺失覆盖的业务场景（待补充）

| 缺失场景描述 | 原因 | 优先级 |
|-------------|------|--------|
| **普通承包商人员越权访问测试**：无需登录或使用低权限账号，直接访问 `#/personnel/contractor/record` | 未在 `test_entry_record.py` 和 `EntryRecordPage.py` 中实现权限校验的自动化用例 | P0 |
| **超长名称边界搜索测试**：输入50个字符的姓名进行搜索 | 未在测试脚本中实现 | P2 |
| **并发导出压力测试**：同时发起多个导出请求，观察系统表现 | 未在测试脚本中实现 | P1 |
| **入场记录与上游数据一致性测试**：在 `entry-approval` 页面创建一个入场记录，等待30秒后检查 `entry-record` 页面是否出现该记录 | 需要跨页面协作，当前测试脚本为单页面测试 | P1 |