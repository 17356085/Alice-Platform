好的，收到。模块: `personnel`，页面: `exam-record`。

根据 **Skill: page-analysis** 的规则，我无法基于仅有的模块和页面名称生成内容。分析需要**页面截图（1-3 张）**或**HTML源码**作为输入。

---

以下是基于您提供的元数据，并结合 **项目基座上下文** 和 **人员/人事模块**（常见业务场景）生成的**分析框架**。请您提供对应的**截图**，我将基于此框架填充具体内容。

下面是 `exam-record` 页面的**占位符分析**，这些表格在您提供截图后将变为精确内容。

## 📊 页面分析框架（占位符）

### 1. 页面整体结构 (预设)
假设此页面是标准的人员管理后台页面：
- **顶部**：面包屑导航 + 页面标题（“考试记录”）
- **主内容区**：
    - **搜索/筛选区**：顶部横条，包含多个筛选控件。
    - **操作按钮**：位于搜索区右侧或表格上方（如“导出”、“查看详情”等）。
    - **数据表格**：展示考试记录。
    - **分页**：表格底部。

### 2. 页面元素清单（根据常见业务预测，等待验证）

| 元素ID | 元素描述 | 控件类型 | 所在区域 | 备注（定位器思路/权限） |
| :--- | :--- | :--- | :--- | :--- |
| `search_person_name` | 搜索: 人员姓名 | `el-input` | 搜索区 | A级: placeholder="请输入姓名" |
| `search_exam_date` | 搜索: 考试日期范围 | `el-date-picker` | 搜索区 | A级: placeholder="选择日期范围" |
| `search_exam_type` | 搜索: 考试类型 | `el-select` | 搜索区 | A级: 下拉框标签“考试类型” |
| `search_status` | 搜索: 状态 | `el-select` | 搜索区 | A级: 下拉框标签“状态” |
| `btn_search` | 搜索按钮 | `el-button` | 搜索区 | A级: text() = "搜索" |
| `btn_reset` | 重置按钮 | `el-button` | 搜索区 | A级: text() = "重置" |
| `btn_export` | 导出按钮 | `el-button` | 表格操作区 | 可能受权限控制 |
| `table_col_name` | 表头: 人员姓名 | `el-table-column` | 表格区 | B级: `.el-table .cell` 含“姓名” |
| `table_col_type` | 表头: 考试类型 | `el-table-column` | 表格区 | 标签类型 |
| `table_col_score` | 表头: 成绩 | `el-table-column` | 表格区 | 数字类型 |
| `table_col_result` | 表头: 结果（通过/未通过） | `el-table-column` | 表格区 | 标签/状态类型 |
| `table_col_action` | 操作列（查看/编辑/删除） | `el-table-column` | 表格区 | 弹窗触发 |
| `pagination` | 分页组件 | `el-pagination` | 表格底部 | A级: class="el-pagination" |

### 3. 关键交互与弹窗（预测）

- **“查看详情”弹窗 (`dialog_detail`)**
    - 模态弹窗，标题为“考试记录详情”。
    - 表单字段：人员名称（只读）、考试名称、考试日期、成绩、详细报告（文本/附件）。
    - 按钮：确定（`text() = "确定"`）

- **“导出”功能**
    - 点击后可能触发下载或弹出“导出确认”弹窗。

- **权限点（待核实）**
    - `btn_export`（导出按钮）
    - `table_col_action` 中的“编辑”或“删除”按钮。

---

## 请提供截图

为了产出精确的 **PAGE_CONTEXT.md** 和 **PAGE_ELEMENT_POSITION.md**，请上传以下截图：
1. **页面完整截图**（需包含搜索区、表格区、分页区）
2. **搜索或新增/编辑弹窗的截图**（如果有）

上传截图后，我将立即展开详细分析。