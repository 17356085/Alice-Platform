好的，作为页面分析专家，我将基于您提供的代码事实源（Page Object代码、测试脚本、页面上下文），对“设备报警配置”页面进行全面的风险建模和业务场景分析。我将严格对齐代码实现，输出`RISK_MODEL.md`和`BUSINESS_SCENARIOS.md`。

---

### 产出文件1: `RISK_MODEL.md`

```markdown
---
source: pair
source_agent: risk-modeling
created: 2026-06-18
---

# 风险模型: 设备报警配置

- **页面**: 设备报警配置
- **所属模块**: 设备管理
- **风险分析依据**: 代码实现 (AlarmConfigPage.py, test_alarm_config.py) 与页面上下文 (PAGE_CONTEXT.md)

## 风险清单

### 1. 业务风险 (Business Risk)

| 风险ID | 风险描述 | 风险等级 | 缓解措施 | 关联用例 | 自动化覆盖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| BR-001 | **误操作删除核心报警配置**：用户可能无意中删除正在使用的报警配置，导致设备报警功能失效。删除操作无二次确认或软删除机制。 | P1 | 1. 实现二次确认弹窗（已实现）。<br>2. 添加软删除机制，允许恢复。<br>3. 记录操作日志。 | 无 | ❌ 未覆盖（弹窗交互已跳过） |
| BR-002 | **重复配置报警规则**：用户可为同一设备创建多条相同报警类型/级别的规则，导致报警重复或逻辑混乱。 | P2 | 在新增或保存时，校验同设备下是否已存在相同报警类型/级别的规则，并提示用户。 | 无 | ❌ 未覆盖 |
| BR-003 | **报警阈值配置不合理**：用户可能将触发值设置得过于敏感（频繁误报）或过于迟钝（无法及时报警）。 | P2 | 在业务上，提供合理的阈值范围或建议值。在UI上增加范围校验或提示。 | 无 | ❌ 未覆盖 |

### 2. 权限风险 (Permission Risk)

| 风险ID | 风险描述 | 风险等级 | 缓解措施 | 关联用例 | 自动化覆盖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| PR-001 | **低权限用户可看到不应显示的页面元素**：非admin用户可能无法看到“新增配置”按钮，但通过直接URL或接口仍可访问。 | P1 | 1. 后端接口增加权限校验。<br>2. 自动化测试覆盖admin用户的基线权限（`test_ac_04`）。 | AC-04 | ✅ 已覆盖（admin基线） |
| PR-002 | **低权限用户可看到所有数据**：不同角色（如设备运维人员）应仅能看到其负责设备的报警配置。 | P1 | 后端接口实现数据权限隔离，根据登录用户角色或部门返回数据。 | 无 | ❌ 未覆盖 |
| PR-003 | **用户可访问不存在的页面**：通过直接URL或hash路由访问一个不存在的页面ID，应返回404或重定向。 | P2 | 前端路由应增加404页面处理。 | 无 | ❌ 未覆盖 |

### 3. 数据风险 (Data Risk)

| 风险ID | 风险描述 | 风险等级 | 缓解措施 | 关联用例 | 自动化覆盖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| DR-001 | **搜索关键词边界值**：使用超长特殊字符串、SQL注入语句、XSS脚本等作为搜索关键词，可能导致后端报错或XSS攻击。 | P1 | 1. 后端接口对输入参数进行严格的长度和格式校验。<br>2. 前端对搜索内容做初步编码或过滤。<br>3. 自动化测试覆盖关键词搜索的边界值。 | 无 | ❌ 未覆盖 |
| DR-002 | **空数据处理**：表格数据为空时，页面应显示友好的空状态提示（`.el-table__empty-text`），而非持续loading或崩溃。 | P1 | 确保后端返回空列表时，前端表格正确处理。测试脚本`_wait_table_ready`已检查空状态。 | 无 | ✅ 已覆盖（测试脚本逻辑） |
| DR-003 | **并发编辑/删除**：两个用户同时尝试编辑或删除同一行配置，可能导致数据不一致。 | P2 | 后端接口应实现乐观锁机制（版本号）。如果前一个用户修改了数据，后一个用户操作时提示数据已更新，需刷新。 | 无 | ❌ 未覆盖 |
| DR-004 | **统计卡片与表格数据一致性**：统计卡片（总数、已启用等）与表格实际数据可能不一致，例如在添加/删除后未及时更新。 | P0 | **前端**：在弹窗操作（新增/编辑/删除）成功关闭后，调用刷新统计卡片接口。<br>**测试**：增加API测试，验证CRUD操作后，统计数据是否与数据库一致。`test_ac_05` 已覆盖基本校验。 | AC-05 | ✅ 部分覆盖（仅校验当前页数据） |

### 4. 接口风险 (API Risk)

| 风险ID | 风险描述 | 风险等级 | 缓解措施 | 关联用例 | 自动化覆盖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| AR-001 | **接口超时/失败**：搜索或加载表格时，网络慢或后端服务异常，页面无有效反馈（如Loading转圈、错误提示）。 | P1 | 1. 前端增加请求超时处理，显示“加载失败，请重试”提示。<br>2. 在`_wait_page_ready`中增加超时处理（已实现`TimeoutException`捕获，但无UI提示）。 | 无 | ❌ 未覆盖 |
| AR-002 | **Token过期**：用户长时间在页面上后Token过期，点击“新增”或“搜索”时，页面应跳转至登录页或刷新Token。 | P1 | 1. 实现全局的Token失效拦截器，统一跳转到登录页。<br>2. 自动化测试增加Token过期场景。 | 无 | ❌ 未覆盖 |
| AR-003 | **接口返回异常数据格式**：后端返回非预期的JSON结构或数据类型（如返回字符串而非数字），可能导致前端渲染失败。 | P1 | 1. 前端应增加数据容错或类型转换（如`_parse_stat`中的`int()`处理）<br>2. 增加UI自动化测试，监控常见异常数据场景。 | 无 | ✅ 部分覆盖（`_parse_stat` 有异常处理） |

### 5. UI/UX风险 (UI/UX Risk)

| 风险ID | 风险描述 | 风险等级 | 缓解措施 | 关联用例 | 自动化覆盖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| UX-001 | **fixed-right列导致DOM克隆**：`get_table_data()`方法已处理，但新增/编辑/删除操作按钮的点击仍可能定位到克隆的、不可见行，导致`ElementClickInterceptedException`。 | P1 | 1. 点击操作按钮前，强制滚动到目标行并等待动画完成。<br>2. 使用JavaScrpt点击（`element.click()` + `return self.driver.execute_script("arguments[0].click()", element)`）避开`is_displayed()`判断。 | 无 | ❌ 未覆盖 |
| UX-002 | **`filterable el-select` 弹窗坑**：由于此已知问题，所有与弹窗相关的UI自动化测试被跳过，导致大量核心功能（增删改）失去自动化覆盖。 | P0 | **短期**：采用API测试替换UI测试，如`test_ac_06_create`。<br>**长期**：推动开发修复`is_displayed()`问题，或使用`execute_script`绕开。 | 无 | ❌ 全部未覆盖（被跳过） |
| UX-003 | **弹窗内表单元素异步加载**：新增/编辑弹窗中的“设备名称”下拉选择框可能需要异步加载设备列表，加载完成前点击无效。 | P2 | 在操作弹窗表单元素前，先等待其加载完成（如等待下拉选项可点击）。 | 无 | ❌ 未覆盖 |

### 6. 性能风险 (Performance Risk)

| 风险ID | 风险描述 | 风险等级 | 缓解措施 | 关联用例 | 自动化覆盖 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| PF-001 | **大数据量下表格渲染慢**：当报警配置数量超过1000条时，表格渲染和操作（如分页、搜索）可能出现卡顿。 | P2 | 1. 后端接口实现分页及延迟加载。<br>2. 前端虚拟滚动。 | 无 | ❌ 未覆盖 |
| PF-002 | **频繁搜索无防抖**：用户快速点击“查询”按钮可能发送大量重复请求。 | P2 | 1. “查询”按钮应在请求发出后变为loading状态，禁止二次点击。<br>2. 实现输入框防抖。 | 无 | ❌ 未覆盖 |

---

## 风险汇总与覆盖状态

| 风险等级 | 总风险数 | 已覆盖 | 部分覆盖 | 未覆盖 |
| :--- | :--- | :--- | :--- | :--- |
| **P0** | 2 | 0 | 0 | 2 |
| **P1** | 8 | 2 | 2 | 4 |
| **P2** | 6 | 0 | 0 | 6 |
| **总计** | 16 | 2 | 2 | 12 |

### 未覆盖的高风险场景 (P0 & P1)

1.  **P0: UX-002 - 弹窗交互自动化缺失**：所有增删改核心功能无法通过UI自动化验证。
2.  **P1: DR-001 - 搜索输入边界值**：安全性与健壮性测试缺失。
3.  **P1: UX-001 - fixed-right列点击**：核心操作按钮的稳定性风险。
4.  **P1: AR-002 - Token过期处理**：核心用户会话生命周期管理风险。
```

---

### 产出文件2: `BUSINESS_SCENARIOS.md`

```markdown
---
source: pair
source_agent: risk-modeling
created: 2026-06-18
---

# 业务场景模型: 设备报警配置

- **页面**: 设备报警配置
- **所属模块**: 设备管理
- **分析依据**: 代码实现 (AlarmConfigPage.py, test_alarm_config.py) 与风险模型 (RISK_MODEL.md)

## 1. 业务目标 (Business Goal)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BS-EQP-ALM-001 | **核心业务目标** | **设备报警规则管理**：用户需要集中管理所有设备（如储罐、泵、压缩机）的报警规则，包括查看、新增、编辑、启用/禁用和删除报警配置，确保设备运行异常时能及时通知。 | 系统管理员, 设备运维人员 | 设备报警配置 | BR-001, BR-002, BR-003 | ✅ |
| BS-EQP-ALM-002 | **次要业务目标** | **报警统计概览**：用户通过顶部统计卡片快速了解报警规则的整体状态（总数、已启用、已禁用）和今日报警数量，以便进行宏观决策。 | 系统管理员, 设备运维人员 | 设备报警配置 | DR-004 | ✅ |
| BS-EQP-ALM-003 | **非目标** | 本页面不负责处理报警产生后的报警事件（如报警确认、报警记录查看）。那些功能应在“报警记录”或“报警处理”页面。 | N/A | N/A | N/A | ⚠️ |

## 2. 角色与旅程 (Roles & Journeys)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BS-EQP-ALM-004 | **角色：系统管理员** | **管理员旅程**：<br>1. 导航至设备报警配置。<br>2. 查看统计卡片，了解全局报警规则概况。<br>3. 新增一个报警规则，例如为“储罐A”添加“液位超限”报警，设置阈值为95%。<br>4. 搜索刚创建的规则，确认其在表格中。<br>5. 禁用该规则，验证状态变更。<br>6. 删除该过期规则。 | 系统管理员 | 设备报警配置 | PR-001, PR-002 | ⚠️ |
| BS-EQP-ALM-005 | **角色：设备运维人员** | **运维人员旅程**：<br>1. 导航至设备报警配置。<br>2. 通过“设备名称”搜索其负责的设备（如“泵房1号泵”）。<br>3. 查看该设备的报警配置详情。<br>4. 编辑“压力超高”报警的触发值，从10.0调整到12.0。 | 设备运维人员 | 设备报警配置 | PR-001, PR-002 | ❓ |

## 3. 业务流程 (Workflows)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BS-EQP-ALM-006 | **流程：新增报警配置 (Happy Path)** | 1. 点击“新增配置”按钮。<br>2. 弹窗打开，填写所有必填项（设备名称、报警类型、报警级别、触发值）。<br>3. 点击“确定”按钮。<br>4. 弹窗关闭，表格刷新，新规则出现在第一页。<br>5. 统计数据（总数）增加1。 | 系统管理员, 设备运维人员 | 设备报警配置 | BR-002, UX-002 | ⚠️ |
| BS-EQP-ALM-007 | **流程：编辑报警配置 (Alternative Path 1)** | 1. 在表格中找到目标行，点击“编辑”按钮。<br>2. 弹窗打开，预填充旧的配置信息。 <br>3. 修改“触发值”字段。<br>4. 点击“确定”按钮。<br>5. 弹窗关闭，表格刷新，该行的“触发值”更新。 | 系统管理员, 设备运维人员 | 设备报警配置 | DR-003, UX-002 | ⚠️ |
| BS-EQP-ALM-008 | **流程：删除报警配置 (Alternative Path 2)** | 1. 在表格中找到目标行，点击“删除”按钮。<br>2. 系统弹出二次确认弹窗（`el-message-box`）：“确认删除配置 [xxx] 吗？”<br>3. 用户点击“取消”：弹窗关闭，未执行删除。<br>4. 用户点击“确认”：弹窗关闭，该行从表格中移除，统计数据（总数）减少1。 | 系统管理员 | 设备报警配置 | BR-001, UX-002 | ⚠️ |
| BS-EQP-ALM-009 | **流程：搜索无结果 (异常流程)** | 1. 在搜索输入框输入一个不存在的关键词（如“不存在”）。<br>2. 点击“查询”按钮。<br>3. 表格显示空数据状态（`.el-table__empty-text`），提示“暂无数据”。<br>4. 统计数据保持不变。 | 所有角色 | 设备报警配置 | DR-002 | ✅ |

## 4. 业务规则 (Business Rules)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BS-EQP-ALM-010 | **业务规则 1: 唯一性规则** | 同一台设备（deviceId）下，不允许存在两条 报警类型（alarmType）+ 报警级别（alarmLevel） 完全相同的已启用报警规则。 | N/A | 设备报警配置 | BR-002 | ❓ |
| BS-EQP-ALM-011 | **业务规则 2: 状态互斥规则** | 一条报警配置的状态只能是“启用”或“禁用”，不允许同时存在。切换操作通过点击表格行的“启用/禁用”按钮完成。 | N/A | 设备报警配置 | N/A | ✅ |
| BS-EQP-ALM-012 | **业务规则 3: 关联删除规则** | 当一台设备被从“设备管理”中删除时，与该设备关联的所有报警配置应被级联删除。 | N/A | 设备报警配置, 设备管理 | N/A | ❓ |
| BS-EQP-ALM-013 | **业务规则 4: 数据级联规则** | 编辑报警配置时，其“设备名称”字段通常是不可修改的，因为它与设备挂钩。如需修改，应删除原规则并新建。 | N/A | 设备报警配置 | N/A | ❓ |

## 5. 数据流 (Data Flow)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BS-EQP-ALM-014 | **数据来源** | 表格中的报警配置数据来自后端 `/api/equipment/alarm-config/list` 接口。统计卡片数据来自 `/api/equipment/alarm-config/stats` 接口。 | N/A | 设备报警配置 | AR-001 | ✅ |
| BS-EQP-ALM-015 | **数据变换** | 本页面可通过“新增/编辑”操作向 `/api/equipment/alarm-config/save` 或 `/api/equipment/alarm-config/update` 接口发送请求，创建或修改报警规则数据。 | N/A | 设备报警配置 | UX-002 | ✅ |
| BS-EQP-ALM-016 | **数据消费** | 报警规则数据被“报警处理”或“报警记录”页面消费，用于判断设备状态是否超标。也可能会影响“设备详情”页面的展示。 | N/A | 报警记录, 设备详情 | N/A | ❓ |

## 6. 业务风险→场景映射 (Risk-to-Scenario Mapping)

| 场景ID | 业务维度 | 场景描述 | 涉及角色 | 涉及页面 | 关联风险 | 置信度 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BS-EQP-ALM-017 | **风险映射: 弹窗交互** | **P0风险 UX-002映射**：当前所有UI自动化测试均跳过弹窗交互，导致新增（BS-EQP-ALM-006）、编辑（BS-EQP-ALM-007）、删除（BS-EQP-ALM-008）三个核心流程完全无自动化覆盖。这是一个主要的测试缺口。 | 系统管理员, 设备运维人员 | 设备报警配置 | UX-002 | ✅ |
| BS-EQP-ALM-018 | **风险映射: 权限测试** | **P1风险 PR-001/002映射**：缺少对非admin权限用户（如设备运维人员）的权限基线测试，包括“是否能看到新增按钮”、“能否看到所有设备的数据”等场景。 | 设备运维人员 | 设备报警配置 | PR-001, PR-002 | ⚠️ |
| BS-EQP-ALM-019 | **风险映射: 并发删除** | **P2风险 DR-003映射**：缺少两个管理员同时删除同一行或编辑同一行的并发场景测试。该场景可能导致数据不一致。 | 系统管理员 | 设备报警配置 | DR-003 | ❓ |

---
**缺失覆盖的业务场景（需补充测试用例）**
1.  **权限测试**: 需创建“设备运维人员”账户，验证其权限与admin的差异。
2.  **并发测试**: 需测试并发编辑/删除报警配置的并发场景。
3.  **输入校验**: 需补充搜索关键词的SQL注入、XSS攻击测试场景。
4.  **Token过期**: 需模拟Token过期后，点击“新增”或“搜索”的场景。
5.  **API测试**: 需完善对CRUD接口的API测试，以绕过UI弹窗的阻塞。
```