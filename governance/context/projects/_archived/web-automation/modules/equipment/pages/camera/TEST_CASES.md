好的，作为**测试设计智能体 (Test Design Agent)**，我已收到 `PAGE_CONTEXT.md`、`RISK_MODEL.md` 以及 **PAIR_SEEDS.md**。我将遵循 `testcase-design` 技能的规范，以结对种子为优先，围绕其扩展，并为摄像头管理页面生成完整的测试设计方案。

以下是我的输出：`TEST_DESIGN.md`

---

### 文件: `TEST_DESIGN.md`

```markdown
---
version: 1.0
source: merged
source_agent: test-design
created: 2026-06-18T14:45:00+08:00
module: equipment
page: camera
seeds: SEED-001, SEED-002, SEED-003
description: "摄像头管理页面测试设计方案（基于结对种子合并）"
---

# TEST_DESIGN — 摄像头管理

> **设计策略:** 先场景，后步骤；先风险，后覆盖。以 PAIR_SEEDS 中的结对种子为核心，AI 围绕扩展并填补覆盖缺口。
> **结对种子合并状态:**
> - **SEED-001** (P0, negative): 直接采用，标记 `来源: pair`
> - **SEED-002** (P0, positive): 直接采用，标记 `来源: pair`
> - **SEED-003** (P1, boundary): 直接采用，标记 `来源: pair`
> - **AI 扩展**: 围绕种子补充缺失场景，标记 `来源: merged` 或 `来源: ai`

---

### 1. 页面加载与显示
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-01 | 正常加载页面，验证布局完整 | P0 | positive | ai | 用户有 `camera:view` 权限，数据库中有≥3条摄像头数据 | 1. 访问摄像头管理页面<br>2. 观察页面布局 | 页面正常渲染，包含：<br>1. 搜索/筛选区（名称输入框、分组下拉、状态下拉、搜索/重置按钮）<br>2. 数据表格（显示3行数据）<br>3. 添加摄像头按钮 |
| TD-CAM-02 | 无数据时空状态展示 | P1 | negative | ai | 数据库无摄像头数据，用户有 `camera:view` 权限 | 1. 访问摄像头管理页面 | 显示空状态占位图及提示文案“暂无摄像头数据”，表格区域显示“暂无数据” |
| TD-CAM-03 | 无权限用户访问页面 | P1 | negative | ai | 用户无 `camera:view` 权限 | 1. 直接访问摄像头管理页面 URL | 页面跳转至无权限提示页或首页，显示“您没有访问该页面的权限” |

### 2. 搜索与筛选
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-04 | 按摄像头名称精确搜索 | P1 | positive | ai | 数据库有摄像头“大门-01”、“仓库-02”等 | 1. 在名称输入框输入“大门-01”<br>2. 点击**搜索** | 表格仅显示“大门-01”的记录 |
| TD-CAM-05 | 按IP地址模糊搜索 | P1 | positive | ai | 数据库有摄像头IP包含“192.168.1” | 1. 在名称输入框输入“192.168.1”<br>2. 点击**搜索** | 表格显示所有IP包含“192.168.1”的摄像头 |
| TD-CAM-06 | 按所属分组筛选 | P1 | positive | ai | 数据库有“A区”和“B区”分组的摄像头 | 1. 在分组下拉框选择“A区”<br>2. 点击**搜索** | 表格仅显示“A区”分组的摄像头 |
| TD-CAM-07 | 按在线状态筛选 | P1 | positive | ai | 数据库有“在线”和“离线”的摄像头 | 1. 在状态下拉框选择“在线”<br>2. 点击**搜索** | 表格仅显示“在线”的摄像头 |
| TD-CAM-08 | 组合条件搜索 | P1 | positive | ai | 数据库有“A区”分组、“在线”状态的摄像头 | 1. 在分组下拉框选择“A区”<br>2. 在状态下拉框选择“在线”<br>3. 点击**搜索** | 表格仅显示同时满足“A区”分组和“在线”状态的摄像头 |
| TD-CAM-09 | 搜索后重置筛选条件（基于RISK-CAM-005） | P1 | boundary | merged | 已应用搜索或筛选条件 | 1. 执行一次搜索（如搜索“大门”）<br>2. 点击**重置** | 1. 所有搜索/筛选输入框被清空<br>2. 表格恢复显示全部数据 |

*(AI Note: TD-CAM-09 扩展自 SEED-003 的“重置”操作，但未完全覆盖种子场景，因此标记为 merged。)*

### 3. 表格操作
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-10 | 表格列排序 | P2 | positive | ai | 数据库有多个摄像头数据 | 1. 点击“名称”列的排序图标<br>2. 观察排序结果 | 列表按名称的升序/降序排列 |
| TD-CAM-11 | 分页-跳转到指定页 | P2 | positive | ai | 数据库有超过一页的摄像头数据（如20条） | 1. 在分页输入框输入“2”<br>2. 按回车或点击跳转按钮 | 表格显示第2页数据（第11-20条） |
| TD-CAM-12 | 分页-改变每页条数 | P2 | positive | ai | 数据库有超过20条数据 | 1. 在每页条数下拉框选择“50” | 表格显示50条数据，分页信息更新 |

### 4. 新增操作
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-13 | 成功添加摄像头 | P1 | positive | ai | 用户有 `camera:add` 权限 | 1. 点击**添加摄像头**<br>2. 在弹窗中填写所有必填项（名称、IP、所属分组）<br>3. 输入唯一名称和IP<br>4. 点击**确定** | 1. 弹窗关闭<br>2. 表格新增一条数据<br>3. 出现“添加成功”toast提示 |
| TD-CAM-14 | 添加摄像头-IP地址重复（RISK-CAM-002） | P1 | negative | merged | 用户有 `camera:add` 权限，数据库中已存在一个IP“192.168.1.100”的摄像头 | 1. 点击**添加摄像头**<br>2. 填写名称（唯一）<br>3. 在IP字段输入已存在的 IP “192.168.1.100”<br>4. 点击**确定** | 1. 弹窗不关闭<br>2. 出现“IP地址已存在”的提示 |

*(AI Note: TD-CAM-14 基于 RISK-CAM-002 生成，是核心业务风险验证点，标记为 merged。)*

### 5. 编辑操作
*(此页面上下文未明确提供编辑功能，假设存在。如无，此维度可为空，或仅保留一条注释)*

### 6. 删除操作
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-15 | 单条删除摄像头（RISK-CAM-001） | P0 | destructive | ai | 用户有 `camera:delete` 权限，列表中有数据 | 1. 点击某条数据的删除图标<br>2. 在确认弹窗中点击**确定** | 1. 该条数据从列表中移除<br>2. 出现“删除成功”toast提示 |
| TD-CAM-16 | 取消删除摄像头 | P1 | negative | ai | 用户有 `camera:delete` 权限 | 1. 点击某条数据的删除图标<br>2. 在确认弹窗中点击**取消** | 1. 确认弹窗关闭<br>2. 列表数据无变化 |

### 7. 权限测试
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-17 | 无添加权限用户，“添加摄像头”按钮不显示 (RISK-CAM-PERM-003) | P1 | negative | ai | 用户无 `camera:add` 权限 | 1. 访问摄像头管理页面 | 页面下方不显示“添加摄像头”按钮 |

### 8. 异常场景
| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-18 | 搜索接口超时 | P2 | negative | ai | 网络环境可模拟接口超时 | 1. 输入搜索条件<br>2. 触发搜索 | 页面显示“搜索超时，请稍后重试”的提示，不出现白屏或卡死 |
| TD-CAM-19 | 搜索框含XSS脚本 (RISK-CAM-DATA-002) | P1 | negative | merged | 用户有 `camera:view` 权限 | 1. 在名称输入框输入 `<script>alert('xss')</script>` <br>2. 点击**搜索** | 1. 页面未弹出alert弹窗<br>2. 前端对输入进行了转义或编码 |

*(AI Note: TD-CAM-19 扩展自 RISK-CAM-DATA，标记为 merged。)*

### 9. 业务场景验证
 *(基于 BUSINESS_SCENARIOS.md 设计)*
> **注意**: 此维度需要真实的 `BUSINESS_SCENARIOS.md` 文件。以下场景基于常见业务逻辑推断，建议在正式版本中基于该文件进行校准。

| 编号 | 测试场景 | 优先级 | 类型 | 来源 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TD-CAM-20 | **SEED-001: 实时预览加载失败降级展示** | P0 | negative | pair | 1. 已配置至少1个摄像头<br>2. 模拟该摄像头RTSP流不可达 | 1. 打开摄像头管理页面<br>2. 点击某个离线摄像头的“预览”按钮<br>3. 等待10秒 | 预览区域显示“设备离线”或“无法连接”提示，而非白屏。不阻塞页面其他操作。 |
| TD-CAM-21 | **SEED-002: 批量选择摄像头进行配置下发** | P0 | positive | pair | 1. 列表中有≥3个摄像头<br>2. 用户有配置管理权限 | 1. 勾选2个摄像头的checkbox<br>2. 点击“批量配置”按钮<br>3. 修改“录像计划”为“全天录像”<br>4. 点击“确定” | 两个摄像头的配置同时更新。列表刷新后两个摄像头的状态显示为“全天录像”。 |
| TD-CAM-22 | **SEED-003: 设备状态筛选联动** | P1 | boundary | pair | 列表中有在线和离线摄像头各若干 | 1. 筛选器选择“在线”→搜索<br>2. 立即切换为“离线”→搜索<br>3. 再切回“全部”→搜索 | 每次切换后表格数据与筛选条件一致。无数据错乱、无闪烁、无重复行。 |
| TD-CAM-23 | 端到端-摄像头全生命周期管理 | P1 | positive | merged | 用户有完整权限 | 1. 添加一个新摄像头（填写必要信息）<br>2. 在列表中找到该摄像头<br>3. 编辑其名称<br>4. 删除该摄像头 | 1. 添加成功<br>2. 列表更新<br>3. 编辑成功<br>4. 删除成功 |

---

### 自动化优先级建议

| 自动化类型 | 建议范围 | 不建议自动化的场景 |
| :--- | :--- | :--- |
| ✅ **UI 自动化 (推荐)** | **P0场景 (E2E)**: TD-CAM-20, TD-CAM-21<br>**核心CRUD**: TD-CAM-13, TD-CAM-15<br>**搜索与权限**: TD-CAM-17 | 无 |
| 🔄 **API 自动化 (推荐)** | **数据唯一性校验**: TD-CAM-14 (RISK-CAM-002)<br>**权限越级**: RISK-CAM-PERM-001 (需API层面覆盖) | 不适用 |
| ❌ **不建议自动化** | 无 | **原因：** 此页面暂无不适于自动化的复杂视觉/感官验证场景。 |
| **混合模式** | 建议对 `TD-CAM-20 (SEED-001)` 进行自动化覆盖，但需注意 **Selenium 无法检测视频画面状态**。需要结合JS检查 `video.readyState` 作为降级展示的判定依据。 | |