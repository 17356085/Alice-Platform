# 系统管理 — Menu Management 测试用例

> 模块: system | 页面: menu-management | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 25 |
| P0 / P1 / P2 | 9 / 14 / 2 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 维度 1: 页面加载与显示

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-LOAD-01 | 正常加载：超级管理员访问菜单管理页面 | P0 | 1. 使用 admin 账号登录  2. 拥有菜单管理权限 | 1. 点击“系统管理” > “菜单管理” | — | 1. 页面正常加载，无白屏/崩溃  2. 左侧加载出完整的菜单树  3. 右侧显示菜单详情（如无选中节点，则显示“请选择菜单”或空状态）  4. 新增/编辑/删除按钮可见 | 未执行 |
| TC-MENU-LOAD-02 | 空数据显示：系统没有任何菜单时 | P1 | 1. 系统菜单表为空（可通过脚本清理） | 1. 访问菜单管理页面 | — | 1. 页面不崩溃  2. 左侧菜单树显示“暂无数据”或空白  3. loading 状态在数据返回后立即消失（避免 RISK-MENU-009） | 未执行 |
| TC-MENU-LOAD-03 | 权限受限状态：无菜单管理权限的用户访问 | P0 | 1. 使用普通管理员（normal）登录  2. 该角色无 `menu:list` 权限 | 1. 直接通过 URL 访问 `/admin/system/menu` | — | 1. 页面报“403 无权限”或自动跳转到首页  2. 前端菜单列表中“系统管理”不可见或灰色 | 未执行 |

## 维度 3: 表格操作 (菜单管理是树形结构，适用树操作)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-TREE-01 | 树展开/收起 | P1 | 1. 存在包含子菜单的父菜单（如“系统管理”下有“用户管理”） | 1. 点击父菜单节点前的展开/收起图标 | — | 1. 子菜单列表正确展开或收起  2. 动画流畅，无卡顿 | 未执行 |
| TC-MENU-TREE-02 | 树节点选中 | P1 | 1. 菜单树正常加载 | 1. 点击任意叶子节点 | — | 1. 该节点被高亮选中  2. 右侧详情区域显示该菜单的编辑信息（名称、路由等） | 未执行 |
| TC-MENU-TREE-03 | 树节点刷新 | P1 | 1. 菜单树已加载 | 1. 点击页面上的“刷新”按钮（如有）或通过其他操作触发刷新 | — | 1. 树形结构保持当前状态（如展开状态、选中状态）或重置到初始状态（视具体实现） | 未执行 |
| TC-MENU-TREE-04 | 大数据量性能 | P2 | 1. 菜单树节点数超过 500 个 | 1. 展开/收起多级菜单  2. 执行拖拽排序 | — | 1. 页面响应时间 < 500ms  2. 操作流畅，无卡顿/浏览器崩溃 | 未执行 |

## 维度 4: 新增操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-ADD-01 | Happy Path：新增一级菜单 | P0 | 1. 点击“新增”按钮 | 1. 类型选择“目录”  2. 输入名称“测试目录_TC”  3. 输入路由`/test-tc`  4. 选择图标  5. 选择排序  6. 点击“确定” | — | 1. 弹窗关闭  2. 菜单树列表刷新，新增的“测试目录_TC”出现在根节点  3. 页面提示“新增成功” | 未执行 |
| TC-MENU-ADD-02 | Happy Path：新增子菜单 | P0 | 1. 选中一个父菜单（如“系统管理”）  2. 点击“新增”按钮 | 1. 类型选择“菜单”  2. 父菜单自动填充为“系统管理”  3. 输入名称“子菜单_TC”  4. 输入路由`/system/sub-tc`  5. 关联权限标识 `system:sub:list`  6. 点击“确定” | — | 1. 新增的“子菜单_TC”出现在“系统管理”下面  2. 权限标识正确保存 | 未执行 |
| TC-MENU-ADD-03 | 表单校验：必填项为空 | P1 | 1. 点击“新增”  2. 清空所有必填项 | 1. 直接点击“确定” | — | 1. 弹窗不关闭  2. 名称、路由、权限标识（如果配了）等字段显示红色错误提示“请输入xxx” | 未执行 |
| TC-MENU-ADD-04 | 边界值校验：名称超长 | P1 | 1. 点击“新增” | 1. 名称为 51 个字符 `ABCD...`（超出 50 限制）  2. 路由为 201 个字符  3. 点击“确定” | — | 1. 前端 `maxlength` 限制输入 50 字符  2. 后端返回错误“菜单名称长度不能超过 50 个字符” | 未执行 |
| TC-MENU-ADD-05 | 数据冲突：同一父菜单下名称重复 | P0 | 1. 根节点下已存在名为“系统管理”的菜单 | 1. 新增一个同级的名为“系统管理”的菜单 | — | 1. 后端返回错误提示“同级菜单名称已存在”  2. 新增失败 | 未执行 |
| TC-MENU-ADD-06 | 数据冲突：路由重复 | P1 | 1. 已存在一个路由为`/system`的菜单 | 1. 新增一个路由为`/system`的菜单 | — | 1. 后端返回错误提示“路由已被占用”  2. 新增失败 | 未执行 |
| TC-MENU-ADD-07 | 异常处理：网络超时 | P1 | 1. 网络环境被干预（如使用 Charles 模拟超时） | 1. 执行 Happy Path 新增操作 | — | 1. 提交按钮变为 Loading 状态并禁止重复点击  2. 超时后提示“网络请求超时，请稍后重试” | 未执行 |

## 维度 5: 编辑操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-EDIT-01 | Happy Path：编辑菜单名称 | P0 | 1. 选中一个菜单  2. 点击“编辑”按钮 | 1. 修改名称为“系统管理_EDIT”  2. 点击“确定” | — | 1. 菜单树刷新，名称变为“系统管理_EDIT”  2. 提示“修改成功” | 未执行 |
| TC-MENU-EDIT-02 | 数据回显：编辑菜单 | P1 | 1. 选中一个有完整信息的菜单 | 1. 点击“编辑” | — | 1. 弹窗中回显菜单的完整信息（名称、路由、权限标识、图标等）  2. 每个字段与编辑前一致 | 未执行 |
| TC-MENU-EDIT-03 | 并发冲突：两个用户同时编辑同一菜单 | P2 | 1. 用户 A 和用户 B 都打开了菜单管理页面 | 1. 用户 A 和 B 同时编辑菜单 X  2. 用户 A 先保存，用户 B 后保存 | — | 1. 用户 B 保存后，系统提示“菜单已被修改，请刷新后重试”  2. B 的修改不生效 | 未执行 |

## 维度 6: 删除操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-DEL-01 | Happy Path：删除叶子菜单 | P0 | 1. 选中一个没有子菜单的叶子节点 | 1. 点击“删除”按钮  2. 在二次确认弹窗中点击“确定” | — | 1. 菜单树刷新，该叶子节点被移除  2. 提示“删除成功” | 未执行 |
| TC-MENU-DEL-02 | Happy Path：取消删除 | P1 | 1. 选中一个叶子菜单 | 1. 点击“删除”  2. 在二次确认弹窗中点击“取消” | — | 1. 菜单树不变  2. 弹窗关闭 | 未执行 |
| TC-MENU-DEL-03 | 风险场景：删除被角色引用的菜单 | P0 | 1. 目标菜单“订单管理”被角色“客服组”引用 | 1. 选中“订单管理”  2. 点击“删除”  3. 在二次确认弹窗中点击“确定” | — | 1. 删除失败  2. 弹窗提示“该菜单已被角色‘客服组’引用，请先解除引用” | 未执行 |
| TC-MENU-DEL-04 | 风险场景：删除父菜单 | P1 | 1. 选中一个有子菜单的父节点（如“系统管理”） | 1. 点击“删除”  2. 在二次确认弹窗中点击“确定” | — | 1. 删除失败或级联删除（需根据业务规则确定）  2. 提示“该菜单包含子菜单，请先删除子菜单” | 未执行 |

## 维度 7: 权限测试

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-PERM-01 | 接口层权限校验 | P0 | 1. 使用无 `menu:create` 权限的 token | 1. 直接调用 `/api/system/menu` POST 接口 | — | 1. 接口返回 403 或 401  2. 新增失败 | 未执行 |
| TC-MENU-PERM-02 | 角色菜单可见性：切换角色 | P1 | 1. 用户 A 有“运营”“客服”双角色 | 1. 以“运营”角色登录，确认能看到“运营仪表盘”菜单  2. 切换到“客服”角色 | — | 1. 菜单树刷新  2. “运营仪表盘”菜单消失，出现“客服工单”菜单 | 未执行 |

## 维度 8: 异常场景

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-ERR-01 | 后端 500 错误 | P1 | 1. 服务端模拟接口返回 500 | 1. 执行任意新增/编辑/删除操作 | — | 1. 前端弹窗提示“服务器内部错误，请稍后重试”  2. 页面功能不崩溃 | 未执行 |
| TC-MENU-ERR-02 | Token 过期 | P1 | 1. 等待 token 过期 | 1. 点击“新增”按钮并填写信息 | — | 1. 弹出登录失效提示  2. 自动跳转到登录页 | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
