# 储罐管理 — 告警配置 测试用例

> 模块: tank | 页面: alarm-config | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 39 |
| P0 / P1 / P2 | 12 / 17 / 10 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 页面加载与显示

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-LOAD-001 | 页面正常加载（有数据） | P0 | 系统中已存在至少1条告警规则 | 1. 使用有权限的用户登录 2. 进入告警配置页面 | — | 页面标题显示“告警配置”，加载动画结束后，表格显示告警规则列表，页面无报错 | 未执行 |
| TD-LOAD-002 | 页面加载（无数据） | P1 | 系统中没有任何告警规则 | 1. 使用有权限的用户登录 2. 进入告警配置页面 | — | 页面显示“暂无数据”或对应空状态提示，表格为空，操作按钮（新增）可用 | 未执行 |
| TD-LOAD-003 | 无权限用户访问 | P0 | 用户角色没有告警配置页面权限 | 1. 使用无权限的普通操作员登录 2. 尝试直接访问告警配置页面的URL | — | 页面提示“无权限访问”或跳转至首页，后端返回403 | 未执行 |
| TD-LOAD-004 | 页面各区域元素完整性校验 | P2 | 页面正常加载（有数据） | 1. 检查搜索/筛选区域、表格、分页、操作按钮等是否存在 | — | 搜索区域包括关键字输入框、高级筛选区域、搜索/重置按钮；表格列正确（告警名称、级别、阈值、状态、创建时间等）；分页组件显示；操作按钮（新增、批量删除等）可用 | 未执行 |

## 搜索与筛选

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-SEARCH-001 | 单条件关键字搜索（页面内搜索） | P1 | 页面有3条告警规则，其中1条包含“液位” | 1. 在关键字输入框输入“液位” 2. 点击搜索或按回车 | — | 表格只显示包含“液位”的告警规则，其他不显示 | 未执行 |
| TD-SEARCH-002 | 组合筛选（状态 + 告警级别） | P1 | 有不同状态和级别的告警规则 | 1. 在高级筛选中选择状态“启用”，告警级别“紧急” 2. 点击筛选 | — | 表格显示同时满足“启用”和“紧急”的告警规则 | 未执行 |
| TD-SEARCH-003 | 模糊搜索（部分匹配） | P2 | 存在告警规则名称“1号罐液位超高” | 1. 输入“1号” 2. 搜索 | — | 表格显示包含“1号”的告警规则 | 未执行 |
| TD-SEARCH-004 | 重置筛选 | P1 | 页面已处于筛选状态，表格数据被过滤 | 1. 点击“重置”按钮 | — | 搜索框清空，高级筛选条件重置为默认，表格恢复显示全部数据 | 未执行 |
| TD-SEARCH-005 | 特殊字符/超长文本搜索 | P2 | 存在告警规则名称包含特殊字符 | 1. 输入特殊字符如 `<script>` 或超长文本（超过200字符） 2. 搜索 | — | 系统按实际包含的字符串搜索，无脚本执行，无报错 | 未执行 |

## 表格操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-TABLE-001 | 表头排序（单列） | P1 | 至少5条告警规则，阈值不同 | 1. 点击“阈值”列的排序箭头 2. 再次点击切换升降序 | — | 表格按阈值升序/降序排列，刷新页面后排序状态保持（若后端支持） | 未执行 |
| TD-TABLE-002 | 列宽调整 | P2 | 页面正常加载 | 1. 鼠标拖动告警名称列的右侧边界 | — | 列宽可自由调整，调整后表格布局无错乱 | 未执行 |
| TD-TABLE-003 | 数据格式化显示 | P2 | 有告警规则配置了阈值0.5 | 1. 查看“阈值”列 | — | 阈值显示为0.5（保留一位小数，或按配置格式显示） | 未执行 |
| TD-TABLE-004 | 行选中/取消选中 | P1 | 表格显示多条数据 | 1. 点击某行的复选框 2. 再次点击取消选中 | — | 行选中后复选框变蓝色；取消后取消勾选；批量操作按钮（删除/启用/禁用）根据选中行数显示数字 | 未执行 |
| TD-TABLE-005 | 批量操作 | P1 | 至少选中2条告警规则 | 1. 点击批量删除按钮 2. 确认弹窗中点击确定 | — | 选中的告警规则全部被删除，页面刷新后不再显示 | 未执行 |
| TD-TABLE-006 | 分页功能 | P1 | 至少15条告警规则 | 1. 查看底部分页组件 2. 点击第2页 3. 点击首页/末页 4. 改变每页条数为20 | — | 分页正常，每页条数生效，数据正确显示，跳转输入框可输入页码跳转 | 未执行 |

## 新增告警规则

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-ADD-001 | 正常新增告警规则（Happy Path） | P0 | 用户有新增权限 | 1. 点击“新增”按钮 2. 填写必填项（告警名称、关联储罐、告警级别、阈值） 3. 配置通知方式（可选） 4. 点击保存 | — | 弹窗关闭，表格新增一条记录，页面显示成功提示“新增成功” | 未执行 |
| TD-ADD-002 | 表单必填项校验 | P0 | 用户有新增权限 | 1. 点击新增 2. 不填写告警名称直接点击保存 | — | 提示“告警名称不能为空”，其他必填项同理 | 未执行 |
| TD-ADD-003 | 阈值格式校验（边界值） | P0 | 关联储罐有上下限 | 1. 输入小于0的液位阈值（如-1） 2. 点击保存 | — | 提示“阈值不能小于0” | 未执行 |
| TD-ADD-004 | 阈值格式校验（上限） | P1 | 关联储罐罐容100 | 1. 输入阈值150（超过罐容105%上限105） 2. 点击保存 | — | 提示“阈值不能超过罐容的105%” | 未执行 |
| TD-ADD-005 | 重复提交 | P1 | 网络慢 | 1. 点击保存后快速连续点击保存按钮 | — | 只有第一次请求生效，按钮在请求过程中disabled，不会生成重复数据 | 未执行 |
| TD-ADD-006 | 取消新增操作 | P2 | 弹窗打开 | 1. 填写部分信息 2. 点击取消按钮或弹窗右上角关闭 | — | 弹窗关闭，表格无新增记录，已填写内容不保存 | 未执行 |
| TD-ADD-007 | 接口异常/网络中断 | P0 | 保存时断网或接口抛出500 | 1. 填写正确信息 2. 模拟接口超时或返回500 | — | 前端提示“保存失败，请重试”，按钮恢复可点击，页面不卡死 | 未执行 |
| TD-ADD-008 | 告警级别配置错误（重要） | P1 | 告警级别可选 | 1. 新增时将“紧急”告警级别选为“警告” 2. 保存 | — | 应该允许配置，但提示“请确认告警级别是否正确”（可能加二次确认） | 未执行 |

## 编辑告警规则

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-EDIT-001 | 编辑后保存成功 | P0 | 存在一条告警规则 | 1. 点击编辑按钮 2. 修改阈值（如从80改为90） 3. 点击保存 | — | 弹窗关闭，表格数据更新，提示“修改成功” | 未执行 |
| TD-EDIT-002 | 数据回显完整性 | P1 | 告警规则包含较多配置字段 | 1. 点击编辑 | — | 弹窗中显示所有字段的原有值，包括通知方式、接收人等 | 未执行 |
| TD-EDIT-003 | 取消编辑 | P2 | 编辑弹窗打开 | 1. 修改部分字段 2. 点击取消 | — | 弹窗关闭，表格数据不变，未保存修改 | 未执行 |
| TD-EDIT-004 | 并发编辑冲突 | P2 | 两人同时编辑同一规则 | 1. A打开编辑弹窗，B先保存修改 2. A点击保存 | — | 前端提示“数据已被其他管理员修改，请刷新后重试”（乐观锁） | 未执行 |

## 删除告警规则

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-DEL-001 | 单个删除（确认后删除） | P0 | 存在一条可删除的告警规则 | 1. 点击删除图标 2. 弹窗确认点击“确定” | — | 提示“删除成功”，表格数据减少 | 未执行 |
| TD-DEL-002 | 取消删除 | P1 | 点击删除后 | 1. 在确认弹窗中点击“取消”或关闭 | — | 弹窗关闭，表格数据不变 | 未执行 |
| TD-DEL-003 | 批量删除 | P1 | 至少选中2条规则 | 1. 点击批量删除按钮 2. 确认弹窗点击确定 | — | 所有选中规则被删除 | 未执行 |
| TD-DEL-004 | 删除已关联的业务数据（如正在使用的规则） | P2 | 告警规则被其他流程引用 | 1. 尝试删除 | — | 系统提示“该规则正在使用中，无法删除”或级联删除 | 未执行 |

## 权限测试

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-PERM-001 | 只读用户无法编辑/删除/新增 | P0 | 某角色只有查看权限 | 1. 使用该角色登录 2. 观察页面 | — | 新增、编辑、删除按钮不可见或置灰，只可查看列表 | 未执行 |
| TD-PERM-002 | 编辑用户无法批量删除 | P1 | 角色有编辑但无删除权限 | 1. 登录 2. 尝试批量删除 | — | 批量删除按钮不可见或点击弹窗无权限提示 | 未执行 |
| TD-PERM-003 | 越权API调用 | P0 | 普通用户通过爬虫或抓包尝试修改 | 1. 获取普通用户的Token 2. 直接调用删除API | — | 后端返回403，数据未删除 | 未执行 |

## 异常场景

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-EX-001 | 网络断开后操作 | P1 | 页面已打开，网络断开 | 1. 执行搜索、新增等操作 | — | 页面给出网络错误提示，按钮可点击，无白屏 | 未执行 |
| TD-EX-002 | 接口超时（15秒） | P0 | 模拟保存接口超时 | 1. 保存告警规则 | — | 15秒后提示“请求超时，请重试”，按钮恢复可用 | 未执行 |
| TD-EX-003 | 接口500错误 | P0 | 模拟后端返回500 | 1. 执行任一操作 | — | 提示“系统繁忙，请稍后重试”，前端不崩溃 | 未执行 |
| TD-EX-004 | Token过期 | P1 | 用户Token已过期 | 1. 停留在页面，等待Token过期 | — | 操作时弹出会话过期提示，引导重新登录，本地编辑内容保存到草稿（如有） | 未执行 |
| TD-EX-005 | 会话超时 | P2 | 长时间无操作 | 1. 不操作页面，20分钟 | — | 自动弹出重新登录或页面自动跳转 | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
