# 储罐管理 — 储罐监控 测试用例

> 模块: tank | 页面: monitor | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 18 |
| P0 / P1 / P2 | 5 / 10 / 3 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 页面加载与显示

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-DISP-001 | 正常加载 | P0 | 1. 已登录系统。 2. 用户有权限查看`monitor`页面。 3. 后端服务正常。 4. 至少有一个液罐的数据正在更新。 | 1. 导航到`monitor`页面。 | — | 1. 页面在5秒内加载完成。 2. 总览仪表盘显示所有液罐的实时液位、温度、压力数据，且数据值合理（非空、非NaN、非负数）。 3. 页面显示最新的数据时间戳。 4. 无`404`、`500`等HTTP错误。 | 未执行 |
| TD-MON-DISP-002 | 空数据状态 | P1 | 1. 用户权限设置只能看到空罐区（暂无数据的罐）。 2. 后端模拟返回`[]`空数组。 | 1. 导航到`monitor`页面。 | — | 1. 页面加载成功。 2. 仪表盘区域显示“暂无可监控的液罐”或类似友好提示信息。 3. 页面无JavaScript错误。 | 未执行 |
| TD-MON-DISP-003 | 权限受限状态 | P1 | 1. 用户为“普通操作员”角色，无任何罐区权限。 | 1. 导航到`monitor`页面。 | — | 1. 页面加载成功。 2. 页面主体区域显示“您没有访问该页面的权限”或类似友好的权限缺失提示。 | 未执行 |
| TD-MON-DISP-004 | 分辨率适配(1080p) | P2 | 1. 设置浏览器分辨率为`1920x1080`。 2. 模拟有10个液罐的数据。 | 1. 导航到`monitor`页面。 2. 查看仪表盘布局。 | — | 1. 所有仪表盘、图表和元素完整显示，无水平滚动条。 2. 布局没有重叠或错乱，文字和图标清晰可读。(对应 RISK-MON-009) | 未执行 |

## 搜索与筛选

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-SRC-001 | 按罐ID精确搜索 | P1 | 1. 页面已加载，显示多个液罐数据。 2. 已知一个存在的罐ID，如`TANK-01`。 | 1. 在搜索框中输入`TANK-01`。 2. 点击搜索按钮或按回车键。 | — | 1. 页面只显示`TANK-01`的数据。 2. 其他液罐数据被隐藏。 | 未执行 |
| TD-MON-SRC-002 | 搜索不存在的罐 | P2 | 1. 页面已加载。 2. 已知一个不存在的罐ID，如`TANK-NONEXIST`。 | 1. 在搜索框中输入`TANK-NONEXIST`。 2. 点击搜索按钮或按回车键。 | — | 1. 页面显示“未找到匹配的液罐”或类似提示信息。 2. 仪表盘无数据显示。 | 未执行 |

## 表格操作 (若存在)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-TBL-001 | 报警列表分页 | P2 | 1. 存在超过一页的报警记录(如30条记录，每页10条)。 | 1. 导航到`monitor`页面。 2. 滚动到报警列表区域。 3. 点击“下一页”按钮。 | — | 1. 报警列表正确显示第二页的数据(第11-20条)。 2. 页面底部显示当前页码为2。 3. URL未发生变化。 | 未执行 |

## 编辑操作 (配置报警阈值)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-EDT-001 | 正常配置报警阈值 (管理员) | P0 | 1. 用户为“管理员”角色。 2. 页面已加载，显示`TANK-01`。 3. 后端正常。 | 1. 鼠标悬停在`TANK-01`上，点击“配置”按钮。 2. 在弹窗中，修改“液位上限”为`85.0`。 3. 点击“确定”按钮。 | — | 1. 弹窗关闭。 2. 页面提示“配置保存成功”。 3. 后端接口`/api/tank/threshold` 被调用，参数为`{tankId: "TANK-01", liquidLevelMax: 85.0}`，并返回`200`。 (对应 RISK-MON-003) | 未执行 |
| TD-MON-EDT-002 | 无效阈值配置 | P1 | 1. 用户为“管理员”角色。 2. 编辑`TANK-01`的配置弹窗已打开。 | 1. 修改“液位上限”为`-5`或`120`(超过100%的物理极限)。 2. 点击“确定”按钮。 | — | 1. 弹窗不关闭。 2. 显示明显的错误提示，如“液位上限必须在0-100之间”。 3. 后端接口未被调用。 | 未执行 |
| TD-MON-EDT-003 | 无权限用户尝试配置 | P0 | 1. 用户为“普通操作员”角色。 2. 页面已加载。 | 1. 尝试寻找“配置”按钮。 2. 若找到，点击它。 3. (可选)直接调用配置API。 | — | 1. “配置”按钮对普通操作员不可见或置灰(`disabled`)。 2. (若找到并点击) 弹窗不出现或直接提示“您没有权限执行此操作”。 (对应 RISK-MON-004) | 未执行 |

## 权限测试

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-PRM-001 | 管理员看到所有罐区 | P1 | 1. 用户为“管理员”角色。 2. 系统中存在多个罐区(如`TANK_ZONE_A`, `TANK_ZONE_B`)。 | 1. 导航到`monitor`页面。 | — | 1. 页面显示所有罐区的数据。 | 未执行 |
| TD-MON-PRM-002 | 普通操作员只能看到本罐区 | P1 | 1. 用户为“操作员”，权限限定在`TANK_ZONE_A`。 2. 系统中存在`TANK_ZONE_A`和`TANK_ZONE_B`。 | 1. 导航到`monitor`页面。 2. (可选)查看网络请求的返回数据。 | — | 1. 页面只显示`TANK_ZONE_A`的数据。 2. 接口返回的数据中不包含`TANK_ZONE_B`的数据。(对应 RISK-MON-005) | 未执行 |

## 异常场景

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-EXC-001 | 网络断开 | P1 | 1. 页面正常加载。 2. 使用DevTools模拟“Offline”状态。 | 1. 页面运行中。 2. 触发断网。 | — | 1. 页面显示“网络连接已断开”或类似友好提示。 2. 页面不崩溃。 3. 恢复网络后，页面数据能自动或手动恢复更新。 | 未执行 |
| TD-MON-EXC-002 | Token过期 | P1 | 1. 页面正常加载。 2. 手动清除或过期token。 | 1. 页面运行中。 2. 触发token过期。 | — | 1. 页面跳转到登录页。 2. console无JavaScript错误。(对应 RISK-MON-008) | 未执行 |
| TD-MON-EXC-003 | 接口500错误 | P1 | 1. 页面正常加载。 2. 后端模拟`/api/tank/data`接口返回`500`。 | 1. 刷新页面。 | — | 1. 页面显示“服务异常，请稍后重试”或类似友好提示。 2. 页面不崩溃，非数据区域(如导航栏)正常展示。 | 未执行 |

## 业务场景验证 (P2-5: 消费 BUSINESS_SCENARIOS.md)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-MON-BIZ-001 |  | P1 | 1. 操作员账号已登录。 2. 后端A系统运行正常，持续推送更新数据。 3. 所有罐处于正常状态。 | 1. 导航到监控页。 2. 停留并观察页面，每秒刷新一次。 3. 5分钟后，检查当前页面显示。 4. 点击页面右上角的“刷新”按钮。 | — | 1. 页面加载无错误。 2. 液位、温度等数据随时间变化，无卡顿。 3. 页面无任何报警弹出。 4. 刷新后数据仍是最新的。 | 未执行 |
| TD-MON-BIZ-002 |  | P0 | 1. 操作员账号已登录。 2. 通过后台管理工具，将`TANK-01`的液位上限临时设置为`50`。 3. 模拟数据使`TANK-01`液位缓慢上升至`55`。(使用数据构造工具或脚本) | 1. 导航到监控页。 2. 关注`TANK-01`的液位数值。 | — | 1. 当液位`>50`时，`TANK-01`的仪表盘变红、闪烁或显示警告标志。 2. (若有)页面顶部出现报警横幅。 3. (若有点击能力)点击`TANK-01`，弹窗显示详情。 4. 操作结束后，通过后台将阈值恢复为`90`。 | 未执行 |
| TD-MON-BIZ-003 |  | P0 | 1. 操作员账号已登录。 2. 高权限的“主管”账号已登录另一个浏览器。 3. `TANK-01`液位正常。 | 1. (操作员)双击`TANK-01`，弹出操作菜单。 2. (操作员)点击“紧急排液”按钮。 3. (操作员)在弹出的二次确认框中，点击“取消”。 4. (主管)切换到另一个浏览器，刷新监控页。 | — | 1. (Step 2)弹出确认框“确认要执行紧急排液操作吗？” 2. (Step 3)确认框关闭，`TANK-01`的数据无任何变化，操作日志中无记录。 3. (Step 4)页面状态与之前一致。(对应 RISK-MON-001) | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
