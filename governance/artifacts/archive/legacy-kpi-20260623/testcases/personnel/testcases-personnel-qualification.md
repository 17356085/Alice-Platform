# 人员管理 — Qualification 测试用例

> 模块: personnel | 页面: qualification | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 41 |
| P0 / P1 / P2 | 17 / 20 / 4 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 页面加载与显示

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-001 | 正常加载页面 | P0 | 用户已登录且有页面访问权限，数据库中存在多条资质数据 | 1. 导航到 `/personnel/qualification` | — | 1. 页面加载成功，无 `v-loading` 动画 2. 表格正确显示数据 3. 面包屑显示“人员管理 > 资质管理” | 未执行 |
| TD-QUAL-002 | 空数据状态加载 | P1 | 用户已登录，数据库中无资质数据 | 1. 导航到 `/personnel/qualification` | — | 1. 页面加载成功 2. 表格区域显示 `el-empty` 空状态组件，文字为“暂无数据” | 未执行 |
| TD-QUAL-003 | 权限受限状态加载 | P0 | 用户已登录，但无 `personnel:qualification:list` 权限 | 1. 直接访问 URL `/personnel/qualification` | — | 1. 页面不显示表格数据 2. 全局 HTTP 拦截器返回 403，并显示错误提示或无权限页面跳转 | 未执行 |
| TD-QUAL-004 | 页面各区域元素完整性 | P1 | 用户已登录，有权限 | 1. 导航到页面 | — | 1. 搜索区、操作按钮区、表格区、分页区均正常渲染 2. 搜索区内 `search-name`, `search-type`, `search-status`, `search-date`, `btn-search`, `btn-reset` 可见 3. 表格表头与元素清单定义一致 | 未执行 |

## 搜索与筛选

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-005 | 单条件搜索：按资质名称 | P1 | 数据库中存在名称包含“安全”的资质数据 | 1. 在 `search-name` 输入“安全” 2. 点击 `btn-search` | — | 1. 表格仅展示名称包含“安全”的资质记录 | 未执行 |
| TD-QUAL-006 | 组合搜索：名称+类型+状态 | P1 | 数据库中存在“学历证书”类型且状态为“有效”的资质，名称包含“本科” | 1. 输入 `search-name` = “本科” 2. 选择 `search-type` = “学历证书” 3. 选择 `search-status` = “有效” 4. 点击 `btn-search` | — | 1. 表格展示同时满足所有筛选条件的数据 | 未执行 |
| TD-QUAL-007 | 重置筛选条件 | P1 | 执行了一次搜索，表格仅显示部分数据 | 1. 点击 `btn-reset` | — | 1. 所有搜索输入框被清空或重置为默认值 2. 表格数据恢复为未筛选时的全部数据 | 未执行 |
| TD-QUAL-008 | 特殊字符与超长文本搜索 | P2 | 数据库中存在相关特殊记录 | 1. 在 `search-name` 输入 SQL 注入字符串如 `' OR 1=1 --` 2. 点击 `btn-search` | — | 1. 接口返回 400 错误或返回空结果集，无 SQL 错误信息泄露 2. 页面给出友好错误提示，而非崩溃 | 未执行 |

## 表格操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-009 | 表格基础交互 | P2 | 有权限，数据量 >10 条 | 1. 点击表头“资质名称”进行排序 2. 点击 `th-action` 列的 `btn-view` | — | 1. 表格数据按名称排序（升序/降序） 2. 打开查看详情弹窗 | 未执行 |
| TD-QUAL-010 | 分页操作完整链路 | P1 | 数据库总数据量 > 10 条 | 1. 切换到第2页 2. 将每页条数切换为20 | — | 1. 页面跳转到第2页，数据正确 2. 分页组件更新为每页20条 | 未执行 |
| TD-QUAL-011 | 行选中与批量操作 | P1 | 有权限，页面存在可删除数据；`btn-delete` 在表格上方操作栏 | 1. 勾选表格中多行数据前的复选框 2. 点击表格上方的 `btn-delete` 按钮 | — | 1. 弹出删除确认弹窗 2. 确认后，所选数据被删除 | 未执行 |

## 新增操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-012 | 打开新增弹窗与必填项校验 | P0 | 有新增权限，`btn-add` 可见 | 1. 点击 `btn-add` 2. 弹窗 `dialog-qualification` 打开，标题为“新增资质” 3. 不填写任何内容，直接点击 `dialog-save` | — | 1. 表单项 `dialog-name`, `dialog-type`, `dialog-issuer` 下方均显示校验错误提示 2. 弹窗未关闭 | 未执行 |
| TD-QUAL-013 | 新增成功提交 | P0 | 有新增权限 | 1. 填写所有必填项：名称、类型、发证机关 2. 可选：填写获得日期、有效期至、备注、上传附件 3. 点击 `dialog-save` | — | 1. 弹窗关闭 2. 页面顶部显示成功提示消息（需确认具体提示词，如“新增成功”） 3. 表格刷新且新增数据在第一页可见 | 未执行 |
| TD-QUAL-014 | 重复提交防止 | P1 | 有新增权限 | 1. 填写表单 2. 点击 `dialog-save` 后立即再次点击 | — | 1. `dialog-save` 按钮在首次点击后变为 `loading` 状态并禁用 2. 接口仅调用一次 | 未执行 |
| TD-QUAL-015 | 网络异常处理（新增） | P2 | 有新增权限，填写有效表单 | 1. 提交前，通过开发者工具断开网络 2. 点击 `dialog-save` | — | 1. 按钮状态恢复 2. 页面出现网络异常错误提示或 Toast | 未执行 |

## 编辑操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-016 | 编辑数据回显完整性 | P0 | 有编辑权限，数据库中存在一条完整数据记录 | 1. 点击该数据行的 `btn-edit` | — | 1. 弹窗 `dialog-qualification` 打开，标题为“编辑资质” 2. 弹窗中所有字段的表单值与原数据一致 3. 上传组件回显原附件 | 未执行 |
| TD-QUAL-017 | 编辑保存成功 | P0 | 有编辑权限 | 1. 在编辑弹窗中修改“资质名称” 2. 点击 `dialog-save` | — | 1. 弹窗关闭 2. 提示“修改成功” 3. 表格中对应行的数据更新 | 未执行 |
| TD-QUAL-018 | 取消修改 | P1 | 打开编辑弹窗并做了修改 | 1. 点击 `dialog-cancel` | — | 1. 弹窗关闭 2. 表格数据未发生变化 | 未执行 |
| TD-QUAL-019 | 并发编辑冲突 | P2 | 相同记录被两个用户同时打开编辑 | 1. 用户A 和 用户B 同时打开同一记录的编辑弹窗 2. 用户A 先保存成功 3. 用户B 后保存 | — | 1. 用户B 保存时，接口返回冲突异常（如版本号不匹配） 2. 页面提示用户B“数据已被修改，请刷新后重试” | 未执行 |

## 删除操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-020 | 删除确认弹窗 | P0 | 有删除权限，选择一条记录 | 1. 点击该数据行的 `btn-delete` | — | 1. 弹出 `el-message-box` 确认弹窗，标题为“确认删除”，内容包含“删除后不可恢复，是否继续？”等字样 | 未执行 |
| TD-QUAL-021 | 取消删除 | P1 | 弹出删除确认弹窗 | 1. 在确认弹窗中点击“取消” | — | 1. 弹窗关闭 2. 表格数据未被删除 | 未执行 |
| TD-QUAL-022 | 删除成功 | P0 | 有删除权限 | 1. 在确认弹窗中点击“确认” | — | 1. 弹窗关闭 2. 提示“删除成功” 3. 表格刷新，该行数据消失 | 未执行 |
| TD-QUAL-023 | 删除关联数据 | P1 | 目标资质已被引用（如关联了某员工） | 1. 尝试删除该资质 | — | 1. 后端接口前置检查发现有关联 2. 删除失败，页面提示“该资质已被使用，无法删除” | 未执行 |

## 权限测试

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-024 | 无新增权限-按钮隐藏 | P1 | 切换到无 `btn-add` 权限的角色 | 1. 刷新页面 | — | 1. `btn-add` 元素在 DOM 中 `isDisplayed` 为 false 或根本不存在 | 未执行 |
| TD-QUAL-025 | 无编辑权限-按钮隐藏或拦截 | P1 | 切换到无 `btn-edit` 权限的角色 | 1. 刷新页面 | — | 1. `btn-edit` 按钮被隐藏 2. 或者按钮存在但点击后 Toast 提示“无操作权限” | 未执行 |
| TD-QUAL-026 | 无删除权限-按钮隐藏 | P1 | 切换到无 `btn-delete` 权限的角色 | 1. 刷新页面 | — | 1. `btn-delete` 按钮被隐藏 | 未执行 |

## 异常场景

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-027 | 接口500错误处理 | P0 | 假设列表接口返回500 | 1. 用 Mock 工具或修改请求，使列表接口返回 500 | — | 1. 页面 `v-loading` 消失 2. 表格区域清空，显示错误状态 3. 页面顶部显示 `ElMessage.error(“服务器错误，请稍后重试”)` | 未执行 |
| TD-QUAL-028 | Token过期处理 | P0 | 用户正在操作某功能 | 1. 在操作过程中，人为使 Token 过期 2. 点击 `btn-search` | — | 1. 接口返回 401 2. 页面自动跳转到登录页或弹出重新登录的弹窗 | 未执行 |

## 业务场景验证

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TD-QUAL-029 | E2E：完整的资质生命周期管理 | P0 | 有所有权限 | 1. 新增一条名称为“TC-测试人员-注册安全工程师”的资质 2. 编辑刚刚创建的资质，将名称修改为“TC-测试人员-高级注册安全工程师” 3. 搜索修改后的资质名称 4. 删除该条资质 | — | 1. 新增、编辑、搜索、删除各步骤均成功，数据间流转正确 | 未执行 |
| TD-QUAL-030 | 角色协作：无权利角色查看资质 | P1 | 使用无任何 personnel 权限的“访客”角色登录 | 1. 尝试通过搜索“安全”访问页面或直接输入URL | — | 1. 页面不显示数据或直接跳转、提示无权限 | 未执行 |
| TD-QUAL-031 | 业务规则：防止过期资质数据延迟 | P1 | 数据库中已存在一条有效资质记录 | 1. 编辑该资质，将 `dialog-expiry-date` 设置为昨天 2. 点击 `dialog-save` | — | 1. 保存成功后，回到列表页 2. 该资质的 `th-status` 列图标和文字变为“过期”或“无效” | 未执行 |
| TD-QUAL-032 | 数据流：新增→列表展示→详情查看 | P1 | 有权限 | 1. 新增一个完整的资质记录（包括附件） 2. 回到列表，确认新数据在表格第一页 3. 点击该记录的 `btn-view` 查看详情 | — | 1. 列表页数据与新增时填写的一致 2. 详情弹窗中附件可预览或下载 | 未执行 |

## 测试用例

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-QUAL-001 | 页面正常加载及元素校验 | P0 | 1. 用户已登录且有访问权限 2. 数据库中存在 > 0 条资质数据 | 1. 导航到 `/personnel/qualification` 2. 检查各区域元素 | - | 1. 页面加载完成，无 loading 2. 面包屑显示“人员管理 > 资质管理” 3. 搜索区、按钮、表格、分页均可见 | 未执行 |
| TC-QUAL-002 | 搜索：按资质名称精确与模糊搜索 | P1 | 数据库中存在名称为“特种作业操作证”的资质 | 1. 在 `search-name` 输入“特种作业” 2. 点击 `btn-search` | `search-name` = "特种作业" | 1. 表格只显示名称包含“特种作业”的记录 | 未执行 |
| TC-QUAL-003 | 新增成功：提交全部必填项及成功反馈 | P0 | 用户有新增权限 | 1. 点击 `btn-add` 2. 填写 `dialog-name`、`dialog-type`、`dialog-issuer` 3. 上传附件 4. 点击 `dialog-save` | `dialog-name` = "TC-全流程-压力容器操作证"<br>`dialog-type` = "职业资格"<br>`dialog-issuer` = "国家质检总局" | 1. 弹窗关闭 2. Toast 提示“新增成功” 3. 列表中可见新增数据 | 未执行 |
| TC-QUAL-004 | 删除：确认弹窗与取消删除 | P0 | 1. 用户有删除权限 2. 存在一条可删除数据 | 1. 点击某行 `btn-delete` | - | 1. 弹出确认弹窗，文本包含“确认删除”和“不可恢复” 2. 点击“取消”，弹窗关闭，数据不变 | 未执行 |
| TC-QUAL-005 | 权限：无新增权限时按钮隐藏 | P1 | 切换至一个无 `btn-add` 权限的账号 | 1. 刷新页面 | - | 1. `btn-add` 元素不可见或不存在于 DOM | 未执行 |
| TC-QUAL-006 | 异常：接口500错误处理 | P0 | 列表接口可被 Mock 返回 500 | 1. Mock 列表接口返回状态码 500 | - | 1. `v-loading` 消失 2. 表格清空 3. Toast 显示“服务器错误，请稍后重试” | 未执行 |
| TC-QUAL-007 | 业务场景：完整生命周期管理 | P0 | 用户具备所有操作权限 | 1. 新增一条名称为“TC-生命周期-应急救护证”、类型为“技能等级”、发证机关为“红十字会”的资质 2. 编辑该资质，名称改为“TC-生命周期-高级应急救护证” 3. 在搜索框搜索“TC-生命周期” 4. 删除搜索出的该条记录 | 见步骤 | 1-4 步骤均成功，搜索结果为1条勾选数据后被删除 | 未执行 |
| TC-QUAL-008 | 业务场景：过期资质状态自动变更 | P1 | 用户有编辑权限 | 1. 新建一条资质，有效期至设为昨天 2. 保存后查看列表 | `dialog-expiry-date` = "2023-01-01" (假设今天为2023-01-02) | 1. 保存成功 2. 列表中该资质状态显示为“过期” | 未执行 |
| TC-QUAL-009 | 搜索：SQL注入防御 | P0 | 用户有权限 | 1. 在 `search-name` 输入 `' OR 1=1 --` 2. 点击 `btn-search` | `search-name` = `' OR 1=1 --` | 1. 接口返回400或空数据 2. 页面无 SQL 错误信息 | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
