# 人员管理 — My Archive 测试用例

> 模块: personnel | 页面: my-archive | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 37 |
| P0 / P1 / P2 | 16 / 13 / 8 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 页面加载与显示 (TCD-PM-01)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TCD-PM-01-01 | 验证“个人基本信息”Tab默认激活并正确加载 | P0 | 1. 使用在职员工账号登录  2. 系统中有该员工的个人档案数据 | 1. 导航至“我的档案”  2. 观察默认激活的Tab | — | 1. 页面标题显示“我的档案”  2. `basic-info-tab` 高亮且 `el-tab-pane`内容区显示  3. 表单中姓名、部门、职位等信息正确回显 | 未执行 |
| TCD-PM-01-02 | 验证页面无数据时的空状态显示 | P2 | 1. 使用新入职无档案变更记录的员工账号登录 | 1. 切换到“档案变更记录”Tab | — | 1. `change-table` 显示空状态文案“暂无数据”或占位图  2. 筛选区、分页组件正常显示但不影响数据 | 未执行 |
| TCD-PM-01-03 | 权限受限：HR角色的访存限制 | P0 | 1. 使用HR角色账号登录  2. 账号有查看个人基本信息的能力 | 1. 导航至“我的档案”  2. 检查页面元素 | — | 1. 页面正常加载  2. `edit-basic-info-btn` 按钮不可见或置灰（若有此限制） | 未执行 |

## 搜索与筛选 (TCD-PM-02)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TCD-PM-02-01 | 按“变更类型”筛选 | P1 | 1. 员工账号下有多个类型的变更记录 | 1. 切换到“档案变更记录”Tab  2. 在 `change-type-select` 中选择“修改”  3. 点击 `search-btn` | — | 1. `change-table` 仅显示变更类型为“修改”的记录  2. 分页数据总量更新为筛选后的记录数 | 未执行 |
| TCD-PM-02-02 | 按“变更日期范围”筛选 | P1 | 1. 员工账号下有指定日期范围内的变更记录 | 1. 切换到“档案变更记录”Tab  2. 在 `change-date-picker` 中选择一个日期范围  3. 点击 `search-btn` | — | 1. 表格仅显示所选日期范围内的记录 | 未执行 |
| TCD-PM-02-03 | 组合筛选 | P2 | 1. 单条件筛选结果不唯一，组合条件能精确定位 | 1. 设置变更类型为“修改”  2. 设置日期范围为近一周  3. 点击 `search-btn` | — | 表格仅显示在近一周内发生的“修改”记录 | 未执行 |
| TCD-PM-02-04 | 重置筛选 | P1 | 1. 当前有筛选条件在生效 | 1. 执行任意筛选后  2. 点击 `reset-btn` | — | 1. 所有筛选条件恢复到默认值（类型: 全部/空，日期: 清空）  2. 表格恢复到未筛选状态（显示全部记录） | 未执行 |
| TCD-PM-02-05 | 无效/空搜索结果 | P2 | 1. 数据库中没有符合筛选条件的数据 | 1. 选择一个不存在任何变更记录的组合条件  2. 点击 `search-btn` | — | 1. 表格显示“暂无数据”  2. 分页显示“共0条” | 未执行 |

## 表格操作 (TCD-PM-03)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TCD-PM-03-01 | 分页-切换每页条数 | P1 | 1. 员工有超过10条变更记录 | 1. 切换到“档案变更记录”Tab  2. 改变 `pagination` 的 `page-size` 为20 | — | 1. 表格一页显示20条数据  2. `pagination` 显示正确的分页信息 | 未执行 |
| TCD-PM-03-02 | 分页-翻页 | P0 | 1. 员工有超过当前页大小的变更记录 | 1. 点击分页的“下一页”按钮 | — | 1. 表格显示下一页数据  2. 当前活动页码变为2 | 未执行 |
| TCD-PM-03-03 | 数据格式化校验 | P1 | 1. 表格中有某条变更记录 | 1. 检查表格中各列的数据格式 | — | 1. `col-change-time` 显示为“YYYY-MM-DD HH:mm:ss”格式  2. `col-operator` 显示为操作人的真实姓名，而非ID | 未执行 |

## 新增/编辑操作 (TCD-PM-04)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TCD-PM-04-01 | 基本信息-正常编辑保存 | P0 | 1. 使用在职员工账号登录  2. 点击 `edit-basic-info-btn` 打开编辑弹窗 | 1. 修改姓名、部门、职位、联系方式  2. 输入合法数据  3. 点击 `dialog-save-btn` | — | 1. 提示操作成功  2. 弹窗关闭  3. 基本信息表单更新为修改后的值  4. 档案变更记录中新增一条“修改”记录 | 未执行 |
| TCD-PM-04-02 | 编辑-表单必填项校验 | P0 | 1. 打开“编辑基本信息”弹窗 | 1. 清空“姓名”输入框  2. 点击 `dialog-save-btn` | — | 1. 表单校验未通过  2. `dialog-name-input` 下方显示错误提示“姓名不能为空” | 未执行 |
| TCD-PM-04-03 | 编辑-表单数据格式校验 | P0 | 1. 打开“编辑基本信息”弹窗 | 1. 输入不合法的手机号（如`123456`）  2. 点击 `dialog-save-btn` | — | 1. 表单校验未通过  2. `dialog-phone-input` 下方显示“请输入正确的手机号” | 未执行 |
| TCD-PM-04-04 | 编辑-取消操作 | P2 | 1. 打开编辑弹窗并修改了某些数据 | 1. 点击 `dialog-cancel-btn` | — | 1. 弹窗关闭  2. 基本信息页面数据不变，未发生实际更新 | 未执行 |
| TCD-PM-04-05 | 权限：编辑非授权字段 | P0 | 1. 使用普通员工角色登录  2. 前端已限制限制 `dialog-department-select` 中的可选值 | 1. 打开编辑基本信息弹窗  2. 尝试在 `dialog-department-select` 中选择一个不在当前角色授权列表中的部门 | — | 1. `dialog-department-select` 选项中没有该部门  2. 若通过接口请求修改，后端应返回“无权操作” | 未执行 |

## 异常场景 (TCD-PM-06)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TCD-PM-06-01 | 网络异常：保存时断网 | P1 | 1. 打开编辑弹窗并输入数据  2. 模拟网络断开 | 1. 点击 `dialog-save-btn` | — | 1. 前端提示“网络异常，请稍后重试”  2. 保存请求未被服务端处理  3. 再次联网刷新页面，数据未变更 | 未执行 |
| TCD-PM-06-02 | 接口超时：查询变更记录接口超时 | P1 | 1. 切换到“档案变更记录”Tab  2. 模拟 `getChangeRecords` 接口超时 | 1. 观察页面表现 | — | 1. 页面加载状态显示（如骨架屏或loading图标）  2. 超时后页面提示“请求超时，请刷新重试”  3. 不会导致页面白屏或崩溃 | 未执行 |
| TCD-PM-06-03 | 接口异常：保存接口返回500 | P1 | 1. 打开编辑弹窗并输入数据  2. 后端模拟500错误 | 1. 点击 `dialog-save-btn` | — | 1. 前端弹出错误提示（如“操作失败，请联系管理员”）  2. 弹窗未关闭，数据未保存 | 未执行 |
| TCD-PM-06-04 | 会话超时 | P0 | 1. 登录后长时间未操作，Token过期 | 1. 点击 `edit-basic-info-btn` | — | 1. 页面跳转到登录页  2. 清除本地登录状态 | 未执行 |
| TCD-PM-06-05 | 并发编辑：多窗口编辑同一条数据 | P2 | 1. 在窗口A打开编辑弹窗  2. 在窗口B也打开编辑弹窗（数据未变，但后端有版本号） | 1. 在窗口A点击保存  2. 在窗口B点击保存 | — | 1. 窗口A保存成功  2. 窗口B保存失败，提示“数据已被修改，请刷新后重试” | 未执行 |

## 价值层测试场景 (业务场景验证)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TCD-PM-BS-01 | 业务目标验证: 员工成功维护个人信息并完成审计追溯 | P0 | 1. 在职员工账号  2. 部门/职位无变更限制 | 1. 登录→查看基本信息  2. 修改手机号并保存  3. 切换到“档案变更记录”Tab  4. 筛选变更类型为“修改”，并按更新时间排序 | — | 1. 基本信息Tab更新为新的手机号  2. 变更记录Tab中最新的那条记录的`col-change-field`为“手机号”，`col-operator`为当前用户 | 未执行 |
| TCD-PM-BS-02 | 状态机验证: 修改密码后强制注销 | P2 | 1. 在职员工账号  2. 已知当前密码 | 1. 点击“修改密码”  2. 输入旧密码、新密码、确认新密码  3. 点击确认修改  4. 未手动退出，直接尝试导航到其他页面 | — | 1. 成功提示“密码修改成功，请重新登录”  2. 页面跳转到登录页  3. 旧的登录Token失效 | 未执行 |
| TCD-PM-BS-03 | 角色协作验证: 普通员工无法编辑非授权字段 | P0 | 1. 普通员工角色账号  2. 后端已处理越权请求 | 1. 打开编辑基本档案弹窗  2. 修改“部门”字段（该字段为HR专属权限）  3. 点击保存  4. 使用抓包工具或浏览器控制台，手动修改`dialog-department-select`值为非授权部门ID并发送保存请求 | — | 1. 前端校验：部门选择器不显示非授权部门  2. 接口校验：后端返回403或“无权操作该字段”  3. 数据库记录未修改 | 未执行 |
| TCD-PM-BS-04 | 数据流完整性: 档案变更记录数据的精准度 | P1 | 1. 有至少5条不同日期、不同类型的变更记录 | 1. 按“变更类型”筛选“修改”  2. 数据行数等于数据库中类型为“修改”的记录数  3. 点击每页条数调整为“全部” | — | 1. 表格展示全部符合条件的数据  2. 每条记录都显示正确的字段、操作时间、操作人  3. 分页组件底部显示“共X条” | 未执行 |

## 页面加载与显示

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-PM-001 | 基本信息Tab默认激活 | P0 | 1. 使用在职员工账号登录  2. 有档案数据 | 1. 导航至我的档案  2. 观察页面 | - | 1. 页面标题“我的档案”  2. `basic-info-tab` 高亮  3. 表单显示姓名、部门、职位等 | 未执行 |
| TC-PM-002 | 空状态显示 | P2 | 1. 新入职员工账号 | 1. 导航至我的档案  2. 切换到“档案变更记录”Tab | - | 1. `change-table` 显示“暂无数据” | 未执行 |

## 搜索与筛选

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-PM-003 | 按“修改”类型筛选 | P1 | 1. 员工下有“新增”、“修改”、“删除”变更记录 | 1. 切换到“档案变更记录”Tab  2. 选择“修改”类型  3. 点击“查询” | 变更类型: "修改" | 1. 表格仅显示“修改”类型记录  2. 数据总量正确 | 未执行 |
| TC-PM-004 | 按日期范围筛选 | P1 | 1. 有不同日期变更记录 | 1. 切换到“档案变更记录”Tab  2. 选择日期范围  3. 点击“查询” | 日期范围: 上周一 ~ 本周一 | 1. 表格显示该日期范围记录 | 未执行 |

## 编辑操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-PM-005 | 编辑基本信息-成功保存 | P0 | 1. 在职员工账号  2. 打开编辑弹窗 | 1. 输入新的姓名：`testuser_new`  2. 选择部门：`测试部`  3. 输入手机号：`13800138000`  4. 点击“确定” | name: `testuser_new` <br>department: `测试部` <br>phone: `13800138000` | 1. 提示“保存成功”  2. 弹窗关闭  3. 基本信息更新  4. 变更记录新增“修改”记录 | 未执行 |
| TC-PM-006 | 编辑基本信息-必填项校验 | P0 | 1. 打开编辑弹窗 | 1. 清空姓名输入框  2. 点击“确定” | name: `` (空) | 1. `dialog-name-input` 下方提示“姓名不能为空” | 未执行 |

## 异常场景

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-PM-007 | 接口超时-变更记录查询 | P1 | 1. 接口 `getChangeRecords` 超时 | 1. 切换到“档案变更记录”Tab | - | 1. 显示加载状态  2. 超时后提示“请求超时” | 未执行 |
| TC-PM-008 | 会话超时-跳转登录页 | P0 | 1. Token 过期 | 1. 点击 `edit-basic-info-btn` | - | 1. 跳转到登录页  2. 清除本地状态 | 未执行 |

## 价值层测试用例 (业务场景验证)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-PM-BS-001 | 员工个人档案修改并审计追溯 | P0 | 1. 在职员工  2. 有编辑权限 | 1. 登录 → 查看基本信息  2. 修改手机号为 `13912345678`  3. 切换到“档案变更记录”  4. 按“修改”类型筛选 | phone: `13912345678` | 1. 基本信息更新  2. 变更记录最新1条字段为“手机号”  3. 操作人为当前用户 | 未执行 |
| TC-PM-BS-002 | 越权编辑部门字段被拦截 | P0 | 1. 普通员工角色  2. 后端拦截 | 1. 打开编辑弹窗  2. 前端选择非授权部门 -> 无法选择  3. 用开发者工具修改请求 -> 接口返回403 | department_id: `hr_authorized_id` | 1. 前端选择器不显示  2. 接口返回403  3. 数据库未改变 | 未执行 |
| TC-PM-BS-003 | 修改密码后强制重新登录 | P2 | 1. 在职员工  2. 已知旧密码 | 1. 点击“修改密码”  2. 输入旧/新密码并确认  3. 点击保存  4. 尝试导航到其他页面 | - | 1. 提示“密码修改成功”  2. 跳转到登录页  3. 旧Token失效 | 未执行 |
| TC-PM-BS-004 | 数据流完整性验证 | P1 | 1. 有5条不同日期的变更记录 | 1. 按“修改”类型筛选  2. 查看每页条数“全部”  3. 检查每条记录 | - | 1. 条数正确  2. 每条记录精确  3. 分页显示“共5条” | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
