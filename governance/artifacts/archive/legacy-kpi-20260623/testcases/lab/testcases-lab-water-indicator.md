# 实验室管理 — 水质指标管理 测试用例

> 模块: lab | 页面: water-indicator | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 9 |
| P0 / P1 / P2 | 2 / 3 / 4 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 维度 1: 页面加载与展示状态

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-DIS-001 | 正常加载：验证22行数据完整展示 | P0 | 数据库有配置的22条水质指标数据 | 1. 使用具有权限的账号登录   2. 导航至“化验室取样→水质分析→水质分析设计指标”   3. 等待页面加载完成 | — | 1. 页面URL为 `#/lab/water/indicator`   2. 加载动画消失   3. 表格显示22行数据   4. 无异常报错 | 未执行 |
| TC-DIS-002 | 空数据状态：验证友好提示 | P2 | 数据库未配置任何水质指标数据（或使用空租户账号） | 1. 使用具有权限的专用空数据账号登录   2. 导航至目标页面   3. 等待页面加载完成 | — | 1. 页面正常加载，无JS报错   2. 表格内显示“暂无数据”或类似友好提示   3. 页面整体布局无异常 | 未执行 |
| TC-DIS-003 | 加载中状态：验证骨架屏/loading组件消失（已隐含在正常用例流程中，作为可复现性检查） | P2 | 网络环境较差（可通过浏览器开发者工具限速模拟） | 1. 打开开发者工具，选择“网络”标签，切换到Slow 3G   2. 导航至目标页面 | — | 1. 在数据完全加载前，表格区域显示el-loading或骨架屏   2. 数据加载完成后，loading状态消失，数据正常展示 | 未执行 |

## 维度 2: 业务数据准确性验证（核心）

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-DATA-001 | 关键指标阈值验证：自动化验证5个核心指标数据与API一致 | P0 | 1. 数据库中存在ID为 `INDICATOR_ID` (如 `1, 2, 3, 4, 5`) 的明确预期数据   2. 通过API获取这5个指标的JSON数据作为基准 | 1. 获取页面第2列（指标名称）和第6列（阈值）数据   2. 将页面数据与API获取的基准值进行逐行比对 | — | 1. 页面显示的指标名称和阈值与API返回的基准值一致   2. 例如：pH 对应 `6.5-8.5`，COD 对应 `≤ 100` | 未执行 |
| TC-DATA-002 | 列完整性验证：验证所有列数据均存在且非空 | P2 | 数据库有完整的22行数据 | 1. 获取第2列（指标名称）数据   2. 获取第6列（阈值）数据   3. 获取第4列（单位）数据 | — | 1. 指标名称列不为空，且包含的指标数量与数据库一致（如 pH, COD, 氨氮, 总磷, SS等）   2. 阈值列不为空，无空白或格式错误   3. 单位列不为空，如 mg/L, NTU, °C等 | 未执行 |
| TC-DATA-003 | 边界值/格式化验证：验证阈值列的正确显示格式 | P2 | 1. 数据库中存在值为 `0` 的指标   2. 存在值为 `-9999` 或 `99999` 的极端指标   3. 存在浮点精度值 `6.5` 的指标 | 1. 获取第6列（阈值）的全部数据   2. 在页面上找到这些特殊值对应的行 | — | 1. 值为 `0` 的指标显示为 `0`，而非空   2. 极端值 `-9999` 和 `99999` 正常展示，不会截断或显示为科学计数法   3. 浮点值显示为 `6.5` 而非 `6.500000` | 未执行 |

## 维度 3: 权限与安全验证

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-AUTH-001 | 越权访问验证：无权限用户通过URL访问，确认被拦截 | P1 | 1. 准备一个“工艺操作员”角色账号（无`化验室取样`菜单权限）   2. 此账号已登录系统 | 1. 使用此无权限账号登录   2. 在浏览器地址栏直接输入路由 `#/lab/water/indicator` 并回车   3. 观察浏览器行为 | — | 1. 页面跳转到403无权限页面或   2. 被重定向到首页/仪表板，并弹出“无权限访问”提示   3. 不显示表格内容 | 未执行 |
| TC-AUTH-002 | 角色数据隔离验证：低权限角色（如外包人员）数据范围验证 | P1 | 1. 数据库中有22条指标（包含部分敏感指标，如“总氰化物”）   2. 外包人员账号仅被授权查看前15条非敏感指标 | 1. 使用外包人员角色账号登录   2. 导航至目标页面   3. 获取第2列（指标名称）数据 | — | 1. 指标名称列仅显示前15条非敏感指标（如不包含“总氰化物”）   2. 表格总行数显示15行 | 未执行 |
| TC-AUTH-003 | XSS注入验证：验证字段的HTML转义 | P1 | 1. 通过API或数据库修改，在某个指标名称中加入XSS payload（如 `<Script>alert('XSS')</Script>`）   2. 注意清除缓存 | 1. 使用有权限账号登录   2. 导航至目标页面   3. 找到包含XSS payload的指标名称行   4. 检查页面元素 | — | 1. 页面上看到的文本是 `<Script>alert('XSS')</Script>` 的字符串   2. 页面没有弹出 `alert` 弹窗   3. 查看页面HTML源码（或通过开发者工具检查元素），确认 `<>` 已被转义为 `&lt;` 和 `&gt;` | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
