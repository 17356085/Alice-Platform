# 系统管理(旧) — Menu Management 测试用例

> 模块: system-management | 页面: menu-management | 生成日期: 2026-06-22 | 自动转换自源 TEST_CASES.md

| 指标 | 数值 |
|------|:---:|
| 用例总数 | 49 |
| P0 / P1 / P2 | 24 / 13 / 12 |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 通过率 | N/A |

## 维度1: 页面加载与显示 (UI)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-UI-001 | 正常加载页面 | P0 | 用户已登录，有权限访问菜单管理 | 1. 使用有权限账号登录系统。2. 导航至：系统管理 -> 菜单管理。 | — | 1. 页面标题显示“菜单管理”。2. 左侧菜单树正确加载，展示至少一级目录。3. 右侧表格加载并显示菜单列表数据。4. 表格列头包含：菜单名称、图标、排序、权限标识、组件路径、状态、创建时间、操作。 | 未执行 |
| TC-D-UI-002 | 无权限访问页面 | P0 | 用户已登录，但无`system:menu:list`权限 | 1. 使用一个没有该页面权限的账号登录。2. 尝试通过直接输入URL访问 `/api/system/menu`。 | — | 1. 页面显示无权限提示（如404或“您无权限访问”）。2. 右侧内容区不加载菜单数据。 | 未执行 |
| TC-D-UI-003 | 菜单树状态保持 | P1 | 页面正常加载 | 1. 展开左侧菜单树中的某一级节点。2. 点击右侧表格中的某一行。3. 刷新页面。 | — | 1. 刷新后，左侧菜单树的展开/收起状态保持不变。2. 刷新后，页面默认高亮显示“菜单管理”节点。 | 未执行 |
| TC-D-UI-004 | 空数据状态 | P2 | 当前节点下无子菜单 | 1. 选中一个没有子菜单的叶子节点（如一个具体的功能菜单）。 | — | 1. 右侧表格显示空状态提示信息（如“暂无数据”或“暂无菜单”）。 | 未执行 |

## 维度3: 表格操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-TBL-001 | 表格展开/折叠 | P1 | 当前父菜单下有子菜单 | 1. 点击表格中菜单名称前的“展开”图标。2. 再次点击“折叠”图标。 | — | 1. 点击后，显示该菜单下的所有子菜单（缩进显示）。2. 点击折叠后，子菜单行被隐藏。 | 未执行 |
| TC-D-TBL-002 | 点击“展开全部”按钮 | P1 | 页面加载完成，存在多层嵌套菜单 | 1. 点击右侧顶部的“展开/折叠全部”按钮。 | — | 1. 浏览器中所有可展开的菜单行全部展开。2. 按钮文本可能切换为“折叠全部”。 | 未执行 |
| TC-D-TBL-003 | 行内操作按钮可见性 | P2 | 表格有数据 | 1. 检查每一行数据末尾的“操作”列。 | — | 1. “编辑”和“删除”按钮清晰可见，且可点击。 | 未执行 |

## 维度4: 新增菜单操作 (核心)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-ADD-001 | 新增顶级菜单 - 正常流程 | P0 | 页面加载完成 | 1. 点击“新增”按钮。2. 在弹出的“新增菜单”对话框中，录入数据：菜单类型（菜单）、菜单名称（测试菜单）、路由地址（/test）、组件路径（test/index）、排序（1）、状态（启用）。3. 点击“确定”。 | — | 1. 对话框消失。2. 页面成功新增一条顶级菜单记录。3. 该菜单名称显示在左侧菜单树中。 | 未执行 |
| TC-D-ADD-002 | 新增子菜单 - 指定上级 | P0 | 选中一个已有目录节点 | 1. 在左侧树选中一个目录节点。2. 点击“新增”按钮。3. 确认弹窗中的“上级菜单”字段自动填充为该节点。4. 填写其他字段，点击“确定”。 | — | 1. 新菜单成功创建为选中目录的子菜单。2. 在表格中，该目录行下展开可见新菜单。 | 未执行 |
| TC-D-ADD-003 | 新增按钮 - 最小权限 | P0 | 选中一个菜单节点 | 1. 点击“新增”按钮。2. 将菜单类型选为“按钮”。3. 仅填写“菜单名称”和“权限标识”（如 `test:btn`），其他字段均不填。4. 点击“确定”。 | — | 1. 成功创建按钮权限。2. 在表格的“权限标识”列看到 `test:btn`。 | 未执行 |
| TC-D-ADD-004 | 表单校验 - 必填项 | P1 | 打开新增对话框 | 1. 不填写任何字段，点击“确定”。 | — | 1. 表单不提交。2. 在“菜单名称”输入框下显示“菜单名称不能为空”等红色提示。 | 未执行 |
| TC-D-ADD-005 | 表单校验 - 权限标识唯一性 | P0 | 已有一个菜单，其权限标识为 `system:menu` | 1. 打开新增对话框。2. 在“权限标识”输入框输入 `system:menu`。3. 点击“确定”。 | — | 1. 接口返回错误提示（如“权限标识已存在”）。2. 前端显示错误提示（从接口返回信息中获取）。 | 未执行 |
| TC-D-ADD-006 | 表单校验 - 路由路径格式 | P2 | 打开新增对话框 | 1. 在“路由地址”字段输入包含特殊字符和超长字符串（如 `//invalid?path=<script>alert(1)</script>&id=123456789012345678901234567890`）。2. 点击“确定”。 | — | 1. 接口拒绝该输入并返回错误。2. 前端显示“路由地址格式不正确”等提示。 | 未执行 |

## 维度5: 编辑菜单操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-EDIT-001 | 编辑菜单名称和排序 | P0 | 页面有菜单数据 | 1. 点击某菜单行上的“编辑”按钮。2. 修改“菜单名称”为“新名字”，修改“排序”为“99”。3. 点击“确定”。 | — | 1. 表格中该行数据更新为新的名称和排序。2. 页面刷新后，更改保持。 | 未执行 |
| TC-D-EDIT-002 | 编辑菜单状态 | P1 | 选中一个启用的菜单 | 1. 点击“编辑”按钮。2. 将“状态”从“启用”改为“禁用”。3. 点击“确定”。 | — | 1. 该菜单状态列更新为“禁用”（灰色标签）。2. 刷新页面后，该菜单从侧边栏消失（验证业务影响是否符合预期）。 | 未执行 |
| TC-D-EDIT-003 | 编辑后取消 | P2 | 打开编辑对话框 | 1. 修改“菜单名称”和“路由地址”字段。2. 点击“取消”按钮。 | — | 1. 对话框关闭。2. 表格中该行数据未发生任何变化。 | 未执行 |

## 维度6: 删除菜单操作

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-DEL-001 | 删除无子项的叶子菜单 | P0 | 选中一个无子菜单的叶子菜单 | 1. 点击该菜单行的“删除”按钮。2. 在弹出的确认框中点击“确定”。 | — | 1. 该行从列表中成功移除。2. 页面刷新后，该菜单不存在。 | 未执行 |
| TC-D-DEL-002 | 删除包含子菜单的父菜单 | P0 | 选中一个有多个子菜单的父目录 | 1. 点击父菜单行的“删除”按钮。2. 观察系统弹窗提示。 | — | 1. 弹窗应明确提示“该操作将删除 [N] 个下级菜单及所有关联权限”。2. 点击“确定”后，父菜单及其所有子菜单均被删除。 | 未执行 |
| TC-D-DEL-003 | 删除操作中途取消 | P1 | 选中任意菜单 | 1. 点击“删除”按钮。2. 在确认框中点击“取消”。 | — | 1. 确认框关闭。2. 菜单列表和左侧树无任何变化。 | 未执行 |

## 维度7: 权限测试

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-PERM-001 | 无`add`权限用户看不到“新增”按钮 | P0 | 账号 `user_no_add` 只有 `list` 权限 | 1. 使用 `user_no_add` 登录并访问菜单管理页。 | — | 1. 页面顶部无“新增”按钮。 | 未执行 |
| TC-D-PERM-002 | 无`edit`权限用户看不到“编辑”按钮 | P0 | 账号 `user_no_edit` 只有 `list` 权限 | 1. 使用 `user_no_edit` 登录并访问菜单管理页。 | — | 1. 表格每一行的“操作”列中无“编辑”按钮。 | 未执行 |
| TC-D-PERM-003 | 直接调用API越权操作 | P0 | 获得一个API请求示例 | 1. 使用无`delete`权限的用户的token，通过Postman直接调用菜单删除API。 | — | 1. 接口返回 `403 Forbidden` 或无权限的错误信息。 | 未执行 |

## 维度8: 异常场景

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-D-EXC-001 | 网络中断时提交新增 | P2 | 处于可提交数据状态 | 1. 打开新增对话框并填写数据。2. 使用开发者工具模拟“Offline”网络状态。3. 点击“确定”。 | — | 1. 前端应有统一错误提示（如“网络连接失败，请检查网络”）。2. 请求不发送或失败，页面状态保持。 | 未执行 |
| TC-D-EXC-002 | 接口超时 | P2 | 页面正常加载 | 1. 使用开发者工具或代理工具模拟所有接口延迟（慢3g）。2. 访问菜单管理页面。 | — | 1. 页面应显示加载状态（如`loading`动画）。2. 加载完成后，页面组件正常渲染。 | 未执行 |
| TC-D-EXC-003 | Token过期后操作 | P1 | 用户已登录 | 1. 登录系统。2. 等待或模拟Token过期。3. 点击“新增”或“编辑”。 | — | 1. 系统自动跳转到登录页或刷新Token后重试。 | 未执行 |

## P0 - 高优用例 (须100%覆盖)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-001 | 正常加载菜单管理页面 | P0 | 用户已登录，具备 `system:menu:list` 权限 | 1. 访问系统登录页，输入账号和密码。2. 找到系统管理模块，点击进入。3. 点击“菜单管理”子菜单。 | `账号`: admin<br>`密码`: 123456 | 1. 页面标题显示“菜单管理”。2. 左侧树形菜单正常展开，至少有`系统管理`一级。3. 右侧表格包含：菜单名称、图标、排序、权限标识、组件路径、状态、创建时间、操作。 | 未执行 |
| TC-MENU-002 | 无权限页面访问被拒绝 | P0 | 账号A没有 `system:menu:list` 权限 | 1. 使用账号A登录系统。2. 在浏览器地址栏直接输入菜单管理URL并回车。 | `URL`: `/api/system/menu` | 1. 页面显示无权限的提示信息（如“您无权限访问此页面”）。2. 右侧内容区域无表格数据。 | 未执行 |
| TC-MENU-003 | 成功新增顶级菜单 | P0 | 页面加载完成，具有`add`权限 | 1. 点击“新增”按钮。2. 在弹窗中选择菜单类型为“菜单”。3. 输入菜单名称。4. 输入路由地址。5. 输入组件路径。6. 输入排序。7. 点击“确定”。 | `菜单名称`: TC-测试顶级菜单<br>`路由地址`: /tc-test-top<br>`组件路径`: test/tc-index<br>`排序`: 10 | 1. 对话框关闭。2. 页面成功新增一条顶级菜单记录。3. 左侧树也刷新并出现该菜单。 | 未执行 |
| TC-MENU-004 | 成功新增子菜单 | P0 | 页面加载完成，左侧树中有可用的目录 | 1. 点击左侧树上的一个目录节点（如“系统管理”）。2. 点击“新增”按钮。3. 确认“上级菜单”字段自动填充为“系统管理”。4. 输入菜单名称。5. 点击“确定”。 | `上级菜单`: 系统管理<br>`菜单名称`: TC-测试子菜单<br>`路由地址`: /tc-child | 1. 新菜单创建成功。2. 在表格中，展开“系统管理”行，能看到该子菜单。 | 未执行 |
| TC-MENU-005 | 成功新增按钮权限 | P0 | 页面加载完成，选中一个菜单节点 | 1. 点击“新增”按钮。2. 菜单类型选为“按钮”。3. 输入菜单名称。4. 输入权限标识。5. 点击“确定”。 | `菜单名称`: TC-测试按钮<br>`权限标识`: tc:test:btn | 1. 成功新建按钮。2. 表格权限标识列显示 `tc:test:btn`。 | 未执行 |
| TC-MENU-006 | 新增时权限标识重复校验 | P0 | 已存在一个权限标识为 `system:menu` 的菜单 | 1. 点击“新增”按钮。2. 输入菜单名称。3. 输入已存在的权限标识。4. 点击“确定”。 | `菜单名称`: TC-测试重复权限<br>`权限标识`: system:menu | 1. 接口返回“权限标识已存在”错误。2. 前端弹窗或表单上显示该错误。 | 未执行 |
| TC-MENU-007 | 成功编辑菜单名称和排序 | P0 | 表格中存在一条菜单数据 | 1. 点击某菜单行的“编辑”按钮。2. 在弹窗中修改菜单名称和排序。3. 点击“确定”。 | `新名称`: TC-编辑后名称<br>`新排序`: `99` | 1. 表格行中名称和排序更新。2. 刷新页面后更改依然存在。 | 未执行 |
| TC-MENU-008 | 成功删除无子项菜单 | P0 | 存在一个无子菜单的叶子菜单 | 1. 点击该叶子菜单行的“删除”按钮。2. 在确认框中点击“确定”。 | `菜单`: 将被删除的目标菜单 | 1. 菜单从列表中消失。2. 刷新后不存在。 | 未执行 |
| TC-MENU-009 | 删除含子项父菜单时提示确认 | P0 | 选中一个有多个子菜单的父目录 | 1. 点击该父目录行的“删除”按钮。2. 观察弹窗提示。 | `父菜单`: 被删除的目录 | 1. 弹窗明确提示将删除N个下级菜单和关联权限。2. 点击确定后，父菜单及全部子菜单被删除。 | 未执行 |
| TC-MENU-010 | 越权：无新增权限看不到新增按钮 | P0 | 账号B只有`list`权限，无`add`权限 | 1. 使用账号B登录系统。2. 访问菜单管理页。 | `账号`: user_no_add | 1. 页面顶部无“新增”按钮。 | 未执行 |
| TC-MENU-011 | 越权：无编辑权限看不到编辑按钮 | P0 | 账号C只有`list`权限，无`edit`权限 | 1. 使用账号C登录系统。2. 访问菜单管理页。 | `账号`: user_no_edit | 1. 表格每行“操作”列无“编辑”按钮。 | 未执行 |
| TC-MENU-012 | 越权：直接API调用被拒 | P0 | 获得账号D的无删除权限的token | 1. 使用Postman设置token。2. 发送删除菜单的API请求。 | `Headers`: Bearer token...<br>`Method`: DELETE<br>`Endpoint`: ... | 1. 接口返回403状态码或无权限提示。 | 未执行 |

## P1 - 中优用例 (>80%覆盖)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-013 | 展开/折叠表格行 | P1 | 表格包含可展开的父级菜单 | 1. 点击某父级菜单行前的“展开”图标。2. 再次点击“折叠”图标。 | `父菜单`: 系统管理 | 1. 点击展开后，显示子菜单（缩进）。2. 点击折叠后，子菜单隐藏。 | 未执行 |
| TC-MENU-014 | 展开/折叠全部 | P1 | 页面已加载，存在多层嵌套菜单 | 1. 点击“展开/折叠全部”按钮。 | `功能`: 展开全部 | 1. 所有菜单行全部展开。2. 按钮文字变成“折叠全部”。 | 未执行 |
| TC-MENU-015 | 编辑菜单状态（启用->禁用） | P1 | 选中一个启用的菜单 | 1. 打开编辑弹窗。2. 将“状态”改为“禁用”。3. 点击“确定”。 | `状态`: 禁用 | 1. 表格该行状态列变为灰色“禁用”。2. 刷新后，侧边栏该菜单消失。 | 未执行 |
| TC-MENU-016 | 删除时取消操作 | P1 | 选中任意菜单 | 1. 点击某行“删除”按钮。2. 在确认框中点击“取消”。 | `操作`: 取消删除 | 1. 确认框关闭。2. 菜单列表和树无变化。 | 未执行 |
| TC-MENU-017 | 必填项表单校验 | P1 | 打开新增对话框 | 1. 直接点击“确定”。2. 观察表单校验信息。 | `必填项`: 菜单名称 | 1. 表单不提交。2. 输入框下方显示“菜单名称不能为空”提示。 | 未执行 |
| TC-MENU-018 | Token过期后操作 | P1 | 用户已登录，等待Token过期 | 1. 等待Token过期。2. 点击“新增”按钮。 | `状态`: Token过期 | 1. 自动跳转至登录页，或自动刷新Token。 | 未执行 |

## P2 - 常规用例 (>50%覆盖)

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 测试数据 | 预期结果 | 实际状态 |
|---------|---------|--------|---------|---------|---------|---------|---------|
| TC-MENU-019 | 空数据状态展示 | P2 | 选中一个没有子菜单的叶子节点 | 1. 点击左侧树中的叶子节点。2. 观察右侧表格。 | `节点`: 具体功能菜单 | 1. 右侧表格显示“暂无数据”或空状态图标。 | 未执行 |
| TC-MENU-020 | 行内操作按钮可见性 | P2 | 表格有数据 | 1. 检查每一行的“操作”列。 | `操作列`: 编辑/删除按钮 | 1. “编辑”“删除”按钮可见。 | 未执行 |
| TC-MENU-021 | 路由地址输入超长/特殊字符 | P2 | 打开新增对话框 | 1. 在路由地址填入超长字符串。2. 点击确定。 | `路由地址`: `//invalid?path=<script>alert(1)</script>&id=123456789012345678901234567890` | 1. 接口拒绝该输入并返回错误。2. 前端显示提示。 | 未执行 |
| TC-MENU-022 | 编辑后取消 | P2 | 打开编辑对话框 | 1. 修改菜单名称。2. 点击“取消”。 | `菜单名称`: 新名称（取消后应不变） | 1. 对话框关闭。2. 表格中该行数据不变。 | 未执行 |
| TC-MENU-023 | 网络中断时提交新增 | P2 | 填写好新增表单 | 1. 开发者工具模拟“Offline”。2. 点击“确定”。 | `网络`: Offline | 1. 前端显示“网络连接失败”提示。2. 请求不发送。 | 未执行 |
| TC-MENU-024 | 接口超时 | P2 | 页面需要加载数据 | 1. 模拟慢3g网络。2. 访问页面。 | `网络`: 慢3g | 1. 页面显示loading状态。2. 加载完成后正常渲染。 | 未执行 |

---
*Auto-generated from source TEST_CASES.md — 2026-06-22 11:02*
