# 测试用例清单

## 模块: workflow, 页面: approval-chain

### 1. 数据检索模块

| 用例ID | 测试标题 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TC-SEARCH-01 | 使用有效关键词搜索 | P0 | 1. 页面已加载，列表展示正常。<br>2. 列表中存在已知的审批链（如“采购审批链”）。 | 1. 在搜索框输入“采购”。<br>2. 点击搜索按钮或按回车键。 | 列表刷新，仅显示名称或编码中包含“采购”的审批链数据。 |
| TC-SEARCH-02 | 搜索无匹配结果 | P1 | 1. 页面已加载，列表展示正常。 | 1. 在搜索框输入不存在的字符串“test123xyz”。<br>2. 执行搜索。 | 列表清空，显示“无数据”或类似的友好提示信息。 |
| TC-SEARCH-03 | 清空搜索条件后重置 | P1 | 1. 已通过关键词（如“采购”）进行搜索并得到结果。 | 1. 清空搜索框内容。<br>2. 再次点击搜索按钮。 | 搜索条件被清除，列表恢复至默认显示全部或按当前状态筛选的结果。 |
| TC-SEARCH-04 | 按状态筛选列表 | P0 | 1. 页面已加载，列表中存在不同状态（启用/禁用）的数据。 | 1. 点击状态下拉框。<br>2. 分别选择“启用”、“禁用”、“全部”。 | 选择“启用”后，列表仅显示状态为“启用”的数据；选择“禁用”后，列表仅显示“禁用”数据；选择“全部”后，列表显示所有状态数据。 |
| TC-SEARCH-05 | 搜索特殊字符 | P2 | 1. 页面已加载，列表展示正常。 | 1. 在搜索框输入特殊字符“!@#$%^&*”。<br>2. 执行搜索。 | 系统无报错，界面正常响应。列表因无匹配项显示为“无数据”。 |
| TC-SEARCH-06 | 搜索超长字符串 | P2 | 1. 页面已加载，列表展示正常。 | 1. 在搜索框输入超过输入框长度的字符串（如50个字符以上）。<br>2. 执行搜索。 | 输入框可正常处理（如自动滚动或截断显示），搜索功能正常执行，页面无崩溃。 |
| TC-FILTER-01 | 组合查询（关键词+状态） | P0 | 1. 页面已加载，列表中存在多种状态和名称的数据。 | 1. 在搜索框输入关键词“审批”。<br>2. 同时在状态下拉框选择“启用”。<br>3. 执行搜索。 | 列表仅显示同时满足“名称/编码包含‘审批’”且“状态为启用”的数据。 |
| TC-FILTER-02 | 仅状态筛选 | P1 | 1. 页面已加载，列表展示正常。 | 1. 保持搜索框为空。<br>2. 选择状态为“启用”。 | 列表刷新，仅显示所有状态为“启用”的审批链。 |
| TC-FILTER-03 | 重置所有筛选条件 | P1 | 1. 已设置搜索关键词和状态筛选条件。 | 1. 点击“重置”按钮（若页面存在）。 | 搜索框清空，状态下拉框恢复为默认“全部”，列表恢复显示全部数据。 |

### 2. 数据操作模块

| 用例ID | 测试标题 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TC-LIST-ADD-01 | 新建审批链入口验证 | P0 | 1. 页面已加载，处于列表视图。 | 1. 点击页面上的“新建”按钮。 | 成功跳转至新建审批链页面或弹出新建对话框，界面元素（如名称、节点配置区）加载正常。 |
| TC-LIST-EDIT-01 | 编辑审批链入口与数据回显 | P0 | 1. 列表中存在至少一条审批链数据。 | 1. 点击某条数据行上的“编辑”按钮。 | 成功跳转至编辑页面或弹出编辑对话框，且该条数据的现有信息（名称、节点数、状态等）被正确预填充到对应字段。 |
| TC-LIST-DELETE-01 | 触发删除操作 | P0 | 1. 列表中存在至少一条审批链数据。 | 1. 点击某条数据行上的“删除”按钮。 | 弹出二次确认对话框，提示“确定删除吗？”等警告信息。 |
| TC-LIST-DELETE-02 | 确认删除 | P0 | 1. 已触发删除确认对话框。 | 1. 在确认对话框中点击“确定”。 | 1. 提示“删除成功”。<br>2. 该条数据从列表中移除。<br>3. 列表自动刷新。 |
| TC-LIST-DELETE-03 | 取消删除 | P1 | 1. 已触发删除确认对话框。 | 1. 在确认对话框中点击“取消”或关闭对话框。 | 对话框关闭，数据保留在列表中，列表无任何变化。 |
| TC-LIST-DELETE-04 | 防止重复删除操作 | P1 | 1. 列表中存在至少一条数据。 | 1. 快速连续多次点击同一条数据的“删除”按钮。 | 系统只弹出一次删除确认对话框，或在接口响应期间按钮处于禁用状态，防止重复提交。 |
| TC-LIST-DELETE-05 | 删除后分页信息更新 | P1 | 1. 列表数据量超过一页，处于第二页。 | 1. 删除当前页的一条数据。<br>2. 观察列表和分页信息。 | 1. 数据从当前页移除。<br>2. 分页总数（如“共X条”）减少1。<br>3. 若当前页变为最后一页且为空，则可能自动跳转至上一页。 |
| TC-LIST-TOGGLE-01 | 停用启用状态的审批链 | P0 | 1. 列表中存在状态为“启用”的审批链。 | 1. 点击该条数据上的“停用”按钮。 | 1. 提示操作成功。<br>2. 该条数据状态变为“禁用”。<br>3. 操作按钮文字变为“启用”。 |
| TC-LIST-TOGGLE-02 | 启用禁用状态的审批链 | P0 | 1. 列表中存在状态为“禁用”的审批链。 | 1. 点击该条数据上的“启用”按钮。 | 1. 提示操作成功。<br>2. 该条数据状态变为“启用”。<br>3. 操作按钮文字变为“停用”。 |
| TC-LIST-TOGGLE-03 | 状态切换操作中防止重复提交 | P1 | 1. 列表中存在可操作的数据。 | 1. 点击“启用/停用”按钮。<br>2. 在接口响应完成前，尝试再次点击该按钮。 | 按钮在首次点击后显示加载状态（如loading图标）或暂时禁用，直至本次操作完成，防止重复提交。 |

### 3. 数据展示与分页模块

| 用例ID | 测试标题 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TC-LIST-VIEW-01 | 验证列表数据展示准确性 | P0 | 1. 页面已加载，列表有数据。 | 1. 查看列表某行数据的各列信息（编码、名称、状态、节点数、更新时间等）。 | 各列数据展示完整、准确，格式正确，与后端数据或业务规则一致。 |
| TC-LIST-VIEW-02 | 数据空状态展示 | P1 | 1. 通过搜索或筛选，使查询结果为空。 | 1. 观察列表区域。 | 列表区域显示友好的“暂无数据”或类似提示，而非一片空白。 |
| TC-PAGINATE-01 | 翻页（上一页/下一页） | P0 | 1. 列表数据量超过一页（例如总共有3页）。 | 1. 点击“下一页”。<br>2. 点击“上一页”。 | 点击“下一页”后，列表数据更新为下一页内容，页码信息（如“2/3页”）同步变化；点击“上一页”后，列表数据和页码信息相应变化。 |
| TC-PAGINATE-02 | 切换每页显示条数 | P0 | 1. 当前每页显示10条，总数据量大于10条。 | 1. 点击“每页条数”下拉框。<br>2. 选择“20条/页”。 | 列表立即刷新，每页显示20条数据，总页数相应减少。 |
| TC-PAGINATE-03 | 跳转至有效页码 | P1 | 1. 列表总页数大于1（例如为3页）。 | 1. 在“跳至”输入框中输入有效页码“2”。<br>2. 点击“跳至”或按回车。 | 列表跳转到第2页，显示对应数据，页码信息更新。 |
| TC-PAGINATE-04 | 跳转至无效页码 | P2 | 1. 列表总页数为3页。 | 1. 在“跳至”输入框中输入“0”、“-1”、“5”或“abc”。<br>2. 尝试跳转。 | 系统应阻止跳转，输入框恢复原值或清空，或给出提示（如“页码无效”）。 |
| TC-PAGINATE-05 | 单页数据分页状态 | P1 | 1. 通过筛选使结果数据条数少于每页显示条数（例如总共3条，每页10条）。 | 1. 观察分页组件。 | 分页组件显示“共3条，1/1页”，且“上一页”、“下一页”按钮为禁用（灰色）状态。 |
| TC-PAGINATE-06 | 切换条数后分页联动 | P1 | 1. 当前每页20条，总数据15条（共1页）。 | 1. 切换每页显示条数为“10条/页”。 | 列表刷新，显示第1页（10条），分页信息变为“共15条，2/2页”。 |
| TC-PAGINATE-07 | 操作后分页信息联动 | P1 | 1. 列表有多页数据，当前在第2页。 | 1. 删除第2页的某条数据。<br>2. 观察列表和分页信息。 | 1. 被删数据从列表消失。<br>2. 分页总数（“共X条”）减少。<br>3. 若第2页因删除变为空，则自动跳转到上一页。 |