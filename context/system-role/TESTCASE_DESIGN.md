好的，遵照您的指示。我将为 `system-role` 模块设计页面级测试用例。

基于 `PAGE_CONTEXT.md` 和 `RISK_MODEL.md` 的描述（文档内容未提供，我将基于 “system-role” 模块的常见功能进行合理推断），测试用例将覆盖角色管理的核心CRUD流程，并包含正向与反向场景。

常见 `system-role` 模块功能包括：角色列表查询、新增角色、编辑角色、删除角色、角色状态启用/禁用、角色权限配置等。

以下是生成的 `TEST_CASES.md` 文件内容。

---

**TEST_CASES.md**

# 系统角色管理模块 - 测试用例

## 概述
本文档旨在对 `system-role` 模块进行全面的页面级功能测试，确保角色管理的各项功能正常运行、数据准确、交互符合预期。测试用例覆盖了角色列表查询、新增、编辑、删除、状态修改及权限配置等核心功能点。

## 测试用例

| ID | 标题 | 前置条件 | 步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-ROLE-001** | **[正向] 角色列表成功加载与分页显示** | 1. 系统中至少存在3个不同角色。<br>2. 用户已登录并拥有查看角色权限。 | 1. 点击左侧导航栏 “系统管理” -> “角色管理”。<br>2. 等待页面加载完成。 | 1. 页面正确展示角色列表表格。<br>2. 表格列显示：角色名称、角色编码、状态、创建时间、部门范围、操作等。<br>3. 分页组件显示当前页、总记录数和总页数。 | P0 |
| **TC-ROLE-002** | **[反向] 角色列表加载失败时的错误提示** | 1. 后端服务模拟为不可用状态（如断电、网络不通）。<br>2. 用户已登录。 | 1. 点击左侧导航栏 “系统管理” -> “角色管理”。 | 1. 页面不显示空白内容。<br>2. 页面上方或中央显示错误提示信息，如“网络异常，请稍后重试”或“加载失败”。 | P1 |
| **TC-ROLE-003** | **[正向] 新增角色 - 成功创建基础角色** | 1. 用户已登录并拥有新增角色权限。<br>2. 系统中不存在名为“测试专员”的角色。 | 1. 在角色列表页，点击 “新增” 按钮。<br>2. 在弹出的新增页面/弹窗中，输入：**角色名称** = “测试专员”，**角色编码** = “tester”。<br>3. 点击 “确定” / “保存” 按钮。 | 1. 系统提示“新增成功”。<br>2. 角色列表页刷新，表格中出现 “测试专员” 角色，状态默认为启用。 | P0 |
| **TC-ROLE-004** | **[反向] 新增角色 - 必填项为空** | 1. 用户已登录并拥有新增角色权限。 | 1. 在角色列表页，点击 “新增” 按钮。<br>2. 不输入任何信息，直接点击 “确定” / “保存” 按钮。 | 1. 页面不关闭，不清空已填内容。<br>2. “角色名称”和“角色编码”输入框下方显示红色错误提示：“角色名称不能为空”、“角色编码不能为空”。 | P1 |
| **TC-ROLE-005** | **[反向] 新增角色 - 角色编码重复** | 1. 用户已登录并拥有新增角色权限。<br>2. 系统中已存在角色编码为 “admin” 的角色。 | 1. 点击 “新增” 按钮。<br>2. 在新增页面中，输入：**角色名称** = “管理员2”，**角色编码** = “admin”。<br>3. 点击 “保存”。 | 1. 系统提示“新增失败”或“角色编码已存在”。<br>2. 页面停留在新增页面，输入框内容保留。 | P0 |
| **TC-ROLE-006** | **[正向] 编辑角色 - 成功修改角色名称** | 1. 用户已登录并拥有编辑角色权限。<br>2. 存在一个待编辑的角色（如“测试专员”）。 | 1. 在角色列表页，找到 “测试专员” 角色，点击其 “编辑” 按钮。<br>2. 在弹出的编辑页面中，将 **角色名称** 修改为 “高级测试专员”。<br>3. 点击 “保存”。 | 1. 系统提示“修改成功”。<br>2. 角色列表页刷新， “测试专员” 的名称更新为 “高级测试专员”。 | P0 |
| **TC-ROLE-007** | **[反向] 编辑角色 - 修改后的角色编码与已有角色重复** | 1. 用户已登录并拥有编辑角色权限。<br>2. 存在待编辑的角色A (编码: editor) 和角色B (编码: admin)。 | 1. 找到角色A (editor)，点击 “编辑”。<br>2. 将其 “角色编码” 修改为 “admin”（即角色B的编码）。<br>3. 点击 “保存”。 | 1. 系统提示“修改失败，角色编码已存在”。<br>2. 编辑页面不关闭，输入框内容保留。 | P1 |
| **TC-ROLE-008** | **[正向] 删除角色 - 成功删除未使用角色** | 1. 用户已登录并拥有删除角色权限。<br>2. 存在一个未分配给任何用户的角色（如“测试专员”）。 | 1. 在角色列表页，找到 “测试专员” 角色，点击其 “删除” 按钮。<br>2. 系统弹出确认对话框：“确认要删除角色[测试专员]吗？”<br>3. 点击 “确定” 按钮。 | 1. 系统提示“删除成功”。<br>2. 角色列表页刷新，“测试专员” 角色从列表中消失。 | P0 |
| **TC-ROLE-009** | **[反向] 删除角色 - 删除已有用户绑定的角色** | 1. 用户已登录并拥有删除角色权限。<br>2. 存在一个已分配给至少一个用户的角色（如“项目经理”）。 | 1. 在角色列表页，找到 “项目经理” 角色，点击其 “删除” 按钮。<br>2. 在弹出的确认对话框中点击 “确定”。 | 1. 系统提示“删除失败，该角色下有用户关联”。<br>2. 角色列表保持不变。 | P0 |
| **TC-ROLE-010** | **[正向] 角色状态切换 - 启用/禁用角色** | 1. 用户已登录并拥有修改角色状态权限。<br>2. 存在一个启用的角色(如“客服用”)和一个禁用的角色(如“封停角色”)。 | 1. 在角色列表页，找到“客服用”角色，点击其状态开关将其设为禁用。<br>2. 找到“封停角色”，点击其状态开关将其设为启用。 | 1. 步骤1后，“客服用”角色状态变为“禁用”，页面提示“禁用成功”。<br>2. 步骤2后，“封停角色”角色状态变为“启用”，页面提示“启用成功”。 | P1 |
| **TC-ROLE-011** | **[正向] 为角色配置菜单权限** | 1. 用户已登录并拥有修改角色权限。<br>2. 待编辑的角色为“新员工角色”（该角色当前无任何权限）。 | 1. 点击 “新员工角色” 的 “权限设置” / “分配权限” 按钮。<br>2. 在权限树中，勾选 “系统管理” 下的 “用户管理” 节点。<br>3. 点击 “保存” 按钮。 | 1. 系统提示“权限配置成功”。<br>2. 重新进入该角色的权限设置页面，“用户管理”的勾选状态应被保存。 | P0 |
| **TC-ROLE-012** | **[反向] 搜索/筛选 - 无效关键词搜索** | 1. 用户已登录。<br>2. 角色列表中存在角色“管理员”、“运维工程师”等。 | 1. 在列表页的搜索框中输入一个不存在的角色名称，如 “!@#$%”。<br>2. 点击 “搜索” 按钮或按回车键。 | 1. 列表显示为空状态，提示 “暂无数据”。<br>2. 分页组件显示总记录数为0。 | P2 |