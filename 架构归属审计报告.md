# 架构归属审计报告

**项目**: AITest 平台  
**日期**: 2026-07-09  
**审计人**: 首席软件架构师（AI Agent）  
**方法论**: 基于证据的代码检查

---

## 执行摘要

**迁移状态**: 约 70% 完成。SDK 提取工作**执行良好**，实现了以下清晰分离：
- **alice-engine** — 运行时 SDK（core、providers、workflow、extensions）
- **alice-governance** — 治理数据（agents YAML、skills MD）
- **aitest/** — 平台层（server、web、CLI、infra）

**策略**: 使用**兼容性别名**（例如 `aitest/engine/executor.py` → `alice_engine.core.executor`）实现渐进式迁移，避免破坏性变更。这在架构上是合理的。

**剩余工作**:
1. 清理已弃用的 `aitest/llm/providers/`（6 个文件，约 50KB）
2. 解决 `aitest/runtime/` 归属问题（5 个文件 — SDK 还是 Platform？）
3. 删除重复的 `aitest/discovery/`（package 已存在）
4. 审计 CLI 导入（确保使用 SDK 公共 API）
5. 明确 `aitest/graphs/` vs `alice_engine/workflow/` 的关系

**整体健康度**: **6.8/10** — 基础扎实，需要最后的清理工作。

---

## 1. 已验证的架构状态

### 1.1 SDK 成熟度（alice-engine）

✅ **结构良好的公共 API**:
```python
# packages/alice-engine/alice_engine/__init__.py
from alice_engine import Engine, Project, RunResult
from alice_engine.runtime import KnowledgeStore, MemoryStore
from alice_engine.providers import LLMProvider, get_provider
from alice_engine.extensions import AuditExtension, ComplexityExtension
# ... 99+ 个公共导出
```

✅ **清晰的模块结构**:
```
alice_engine/
├── core/               # 执行原语（executor、agent_loop、skill_executor）
├── providers/          # LLM 提供者（claude、deepseek、mimo、openai、ollama、mock）
├── workflow/           # 工作流/图原语
├── runtime/            # 运行时能力（knowledge、memory、observability）
├── extensions/         # 扩展系统（audit、complexity、cost）
├── discovery/          # 发现接口
├── adapters/           # 适配器接口
└── audit/              # 审计接口
```

✅ **提供者已在 SDK 中**:
- `alice_engine/providers/base.py` — 基础 LLM 提供者
- `alice_engine/providers/claude.py` — Claude 提供者（10KB）
- `alice_engine/providers/deepseek.py` — DeepSeek 提供者（9KB）
- `alice_engine/providers/mimo.py` — MiMo 提供者（8KB）
- `alice_engine/providers/openai.py` — OpenAI 提供者（8.5KB）
- `alice_engine/providers/ollama.py` — Ollama 提供者（5KB）
- `alice_engine/providers/mock.py` — Mock 提供者（4KB）

---

### 1.2 迁移策略（兼容性别名）

✅ **aitest/engine/executor.py**（6 行）:
```python
"""Compatibility alias to the canonical executor module."""
import sys
from alice_engine.core import executor as _impl
sys.modules[__name__] = _impl
```

✅ **aitest/llm/provider.py**（5 行）:
```python
# Re-export — 原文件已搬到 adapters/llm/interface.py
from aitest.adapters.llm.interface import (
    LLMResponse, StreamEvent, LLMProvider, get_provider, ...
)
```

✅ **aitest/engine/__init__.py**（263 行）:
- 提供便利的 `Engine` 类，封装 SDK 原语
- 通过 `alice_engine.core.runtime_environment` 设置运行时环境
- 可接受的平台包装层

---

### 1.3 治理结构

✅ **alice-governance 包**:
```toml
# packages/alice-governance/pyproject.toml
[tool.setuptools.package-data]
alice_governance = [
    "skills/**/*.yaml",
    "skills/**/*.md",
    "agents/**/*.yaml",
    "agents/**/*.md",
    "context/**/*.yaml",
    "context/**/*.md",
]
```

✅ **governance/ 目录**:
```
governance/
├── agents/         # Agent 定义（YAML）
├── skills/         # Skill 提示词（MD）
├── context/        # 领域上下文
├── knowledge/      # 知识制品
└── artifacts/      # 生成的制品
```

---

## 2. 关键问题（基于证据）

### 优先级: 高

#### 2.1 **aitest/runtime/ — 归属不明确**

**文件**:
```
aitest/runtime/
├── config.py          # 97 LOC — RuntimeConfig 类
├── context.py         # 约 350 LOC（估计）
├── error_handling.py  # 约 200 LOC（估计）
├── paths.py           # 约 180 LOC（估计）
├── _paths_core.py     # 约 40 LOC（估计）
└── __init__.py        # 空文件
```

**问题**: 这些文件应该属于 SDK（`alice-engine/runtime/`）还是平台（`aitest/infra/`）？

**需要分析**:
1. 检查导入 — 它们依赖 SDK 还是平台？
2. 检查使用情况 — 它们被 SDK 使用还是仅被平台使用？

**建议**: 
- 如果被 SDK 使用 → 移至 `alice-engine/runtime/`
- 如果仅被平台使用 → 移至 `aitest/infra/`
- 如果混合使用 → 拆分或使用适配器模式

---

#### 2.2 **aitest/discovery/ — 重复**

**证据**:
- `packages/alice-discovery/` 作为 SDK 包存在
- `aitest/discovery/` 也存在实现：
  ```
  aitest/discovery/
  ├── base.py
  ├── browser_use.py
  ├── registry.py
  └── source/
  ```

**问题**: 两个发现代码库。违反 DRY 原则。

**建议**:
1. 运行 `grep -r "from aitest.discovery" .` 查找使用情况
2. 如果找到，迁移至 `from alice_discovery import ...`
3. 删除 `aitest/discovery/` 目录

---

#### 2.3 **aitest/llm/providers/ — 已弃用但仍存在**

**证据**:
```python
# aitest/llm/providers/__init__.py
"""Deprecated provider package.

Legacy provider implementations are retained for compatibility only.
Prefer the unified runtime/provider path and `aitest.runtime.config`.
"""
```

**但目录包含**:
```
aitest/llm/providers/
├── claude.py     # 10KB
├── deepseek.py   # 9KB
├── mimo.py       # 8.7KB
├── mock.py       # 8.5KB
├── ollama.py     # 5.6KB
└── openai.py     # 8.5KB
```

**总计**: 约 50KB 已弃用代码。

**建议**:
1. 运行 `grep -r "from aitest.llm.providers" .` 查找使用情况
2. 如果未找到 → 删除目录
3. 如果找到 → 迁移至 `alice_engine.providers`

---

#### 2.4 **aitest/cli/ — 验证 SDK 公共 API 使用**

**担忧**: CLI 命令可能直接导入 SDK 内部模块而不是公共 API。

**需要的证据**:
```bash
# 检查 CLI 是否导入内部模块
grep -r "from aitest\.engine\." aitest/cli/
grep -r "from aitest\.graphs\." aitest/cli/
grep -r "from aitest\.llm\." aitest/cli/
```

**期望的模式**:
```python
# 好：公共 API
from alice_engine import Engine, Project

# 坏：内部导入
from aitest.graphs.sop_graph import build_graph
from aitest.engine.executor import AgentLoop
```

**建议**: 审计 CLI 导入。重构任何内部导入以使用 SDK 公共 API。

---

### 优先级: 中

#### 2.5 **aitest/graphs/ — 归属不明确**

**证据**:
```
aitest/graphs/
├── state.py       # 图状态创建
├── checkpoint.py  # 检查点设置
└── __init__.py
```

**同时存在**:
```
alice_engine/workflow/
├── state.py
└── ...
```

**问题**: `aitest/graphs/` 是平台特定的包装器还是重复的 SDK 逻辑？

**代码证据**:
```python
# aitest/engine/__init__.py 第 194 行
from aitest.graphs.state import create_initial_state
from aitest.graphs.checkpoint import get_checkpointer
```

**建议**:
1. 比较 `aitest/graphs/state.py` 与 `alice_engine/workflow/state.py`
2. 如果重复 → 合并到 SDK
3. 如果是平台特定包装器 → 保留但重命名以明确（例如 `aitest/platform/graph_utils/`）

---

#### 2.6 **aitest/agents/ — 与 SDK 重叠？**

**目录**:
```
aitest/agents/
├── ab_test.py
├── agent_benchmark.py
├── agent_scheduler.py
├── context_agent.py
├── human_feedback.py
├── pipeline_router.py
├── prompt_benchmark.py
├── skill_executor.py
└── __init__.py
```

**问题**: 这些是否与 SDK 的 `alice_engine/core/agent_*` 模块重叠？

**建议**: 
1. 检查 `aitest/agents/skill_executor.py` 是否重复了 `alice_engine/core/skill_executor.py`
2. 验证哪些 agent 是平台特定的（如 `ab_test`、`agent_benchmark`）而哪些是 SDK 级别的

---

#### 2.7 **aitest/adapters/ — 与 SDK 的关系不明确**

**目录**:
```
aitest/adapters/
├── audit/
├── event/
└── llm/
```

**SDK 也有**:
```
alice_engine/adapters/
└── interfaces.py
```

**建议**: 明确适配器模式的使用。如果 `aitest/adapters/` 实现 SDK 接口，那是正确的。如果重复 SDK 适配器，则需合并。

---

#### 2.8 **aitest/audit_engine/ — 关注点混合**

**目录**（14 个文件）:
```
aitest/audit_engine/
├── cost_auditor.py
├── daily_report.py
├── diff_extractor.py
├── event_bus.py
├── failure_attributor.py
├── governance_kpi.py        # ← 这应该在 Governance 中？
├── online_monitor.py
├── qa_loop.py
├── review_trigger.py
├── safety_auditor.py
├── scheduled_audit.py
├── sop_auditor.py           # ← 包含 SOP 验证逻辑？
├── sop_optimizer.py
└── step_efficiency.py
```

**问题**: 某些文件可能包含治理验证逻辑（如 `governance_kpi.py`、`sop_auditor.py`）。

**建议**:
1. 将**审计**（观察）与**验证**（执行）分离
2. 验证逻辑 → `alice-governance` 或 SDK 扩展
3. 审计/监控 → 保留在 `aitest/audit_engine/`

---

#### 2.9 **aitest/knowledge/ — SDK 还是平台？**

**目录**:
```
aitest/knowledge/
├── knowledge_extractor.py
├── knowledge_server.py
├── rag_engine.py
├── rag_indexers.py
├── redis_vector.py
└── skill_proposer.py
```

**SDK 有**:
```
alice_engine/runtime/
├── intelligence/    # （可能包含 RAG 接口）
└── ...
```

**问题**: `aitest/knowledge/rag_engine.py` 是平台特定实现还是应该在 SDK 中？

**建议**: 如果 RAG 是可复用的运行时能力 → 移至 SDK。如果是平台特定集成 → 保留在 `aitest/`。

---

## 3. 正确放置的模块

| 目录 | 状态 | 理由 |
|------|------|------|
| `packages/alice-engine/` | ✅ **正确** | 结构良好的 SDK，有清晰的公共 API |
| `packages/alice-governance/` | ✅ **正确** | 纯数据包（YAML、MD） |
| `packages/alice-discovery/` | ✅ **正确** | 发现 SDK |
| `aitest/platform/` | ✅ **正确** | 平台服务（capability_router、complexity、memory、hooks、lifecycle） |
| `aitest/server/` | ✅ **正确** | FastAPI REST API |
| `aitest/web/` | ✅ **正确** | Vue.js 前端 |
| `aitest/cli/` | ✅ **正确** | CLI 命令（待导入审计） |
| `aitest/infra/` | ✅ **正确** | 基础设施（database、cache、security、logging） |
| `aitest/engine/` | ✅ **可接受** | 薄包装层（约 450 LOC）提供平台便利功能 |
| `governance/` | ✅ **正确** | Agent YAML、Skill MD、上下文文件 |
| `context/` | ✅ **正确** | 领域知识文件 |
| `docs/` | ✅ **正确** | 文档 |
| `tools/`、`scripts/` | ✅ **正确** | 工具 |
| `tests/` | ✅ **正确** | 测试套件 |
| `.tlo/` | ✅ **正确** | 项目特定运行时数据（根据 ADR-001） |

---

## 4. 依赖分析（需要抽样检查）

### 4.1 平台 → SDK 依赖（预期）

**好**（平台调用 SDK）:
```python
# aitest/engine/__init__.py
from alice_engine.core.runtime_environment import runtime_environment_scope
from alice_engine.workflow.sop_graph import build_sop_graph
from alice_engine.workflow.state import configure_paths
```

### 4.2 SDK → 平台依赖（不应存在）

**检查**（SDK 应该零平台导入）:
```bash
# 验证 SDK 没有 aitest.* 导入（测试除外）
cd packages/alice-engine
grep -r "from aitest\." alice_engine/ | grep -v "__pycache__" | grep -v "test"
```

**期望结果**: 空（无匹配）或仅测试相关导入。

### 4.3 CLI → SDK 内部依赖（不应存在）

**检查**（CLI 应仅使用 SDK 公共 API）:
```bash
grep -r "from aitest\.engine\." aitest/cli/
grep -r "from aitest\.graphs\." aitest/cli/
grep -r "from aitest\.llm\." aitest/cli/
```

**期望结果**: 所有导入应来自 `alice_engine`，而不是 `aitest.engine`、`aitest.graphs` 等。

---

## 5. 架构健康度评分

| 维度 | 分数 | 理由 |
|------|------|------|
| **SDK 成熟度** | 8/10 | alice-engine 有 99+ 个公共导出，结构清晰；缺少一些文档 |
| **迁移完成度** | 7/10 | 约 70% 完成；核心在 SDK 中，但需要清理（llm/providers、discovery、runtime） |
| **边界清晰度** | 6/10 | 大部分清晰，但 aitest/runtime、aitest/graphs 归属不明确 |
| **兼容性策略** | 9/10 | 别名模式清晰，允许渐进式迁移 |
| **SDK 独立性** | 7/10 | SDK 可能可以发布，但需要验证零平台依赖 |
| **平台独立性** | 6/10 | 平台可能仍导入内部模块（需要 CLI 审计） |
| **治理分离** | 8/10 | 治理是分离的，但 audit_engine 中有一些验证逻辑 |
| **代码重复** | 5/10 | Discovery 重复，providers 标记为弃用但仍存在 |
| **公共 API 设计** | 7/10 | SDK 有公共 API，但缺少全面的示例/文档 |

**整体健康度**: **6.8/10**

**解读**: 迁移**执行良好**，有坚实的基础。剩余工作是**清理**而不是根本性重构。

---

## 6. 推荐行动计划

### 阶段 1: 验证（第 1 周） — **立即执行**

1. **依赖审计**:
   ```bash
   # SDK → 平台（应为空）
   grep -r "from aitest\." packages/alice-engine/alice_engine/ | grep -v test
   
   # CLI → 内部模块（应仅使用 alice_engine）
   grep -r "from aitest\.engine\." aitest/cli/
   grep -r "from aitest\.graphs\." aitest/cli/
   
   # Discovery 重复
   grep -r "from aitest\.discovery" . | grep -v __pycache__
   
   # 弃用的 providers
   grep -r "from aitest\.llm\.providers" . | grep -v __pycache__
   ```

2. **记录发现** 在 `docs/architecture/dependency-audit.md`

---

### 阶段 2: 清理（第 2-3 周） — **高优先级**

3. **删除重复项**:
   - 如果未使用，删除 `aitest/discovery/`
   - 如果未使用，删除 `aitest/llm/providers/`

4. **解决 aitest/runtime/ 归属**:
   - 审计每个文件的导入和使用情况
   - 相应地移至 SDK 或平台

5. **明确 aitest/graphs/**:
   - 与 `alice_engine/workflow/` 比较
   - 合并或清楚地记录分离

---

### 阶段 3: 重构（第 4-6 周） — **中优先级**

6. **CLI 重构**:
   - 用 SDK 公共 API 替换任何内部导入
   - 示例：`from alice_engine import Engine` 而不是 `from aitest.engine.executor import AgentLoop`

7. **审计引擎分离**:
   - 审查 `aitest/audit_engine/`
   - 将验证逻辑提取到治理
   - 将可观察性保留在平台

8. **知识/代理审查**:
   - 明确 `aitest/knowledge/` vs SDK intelligence
   - 明确 `aitest/agents/` vs SDK agent 原语

---

### 阶段 4: 文档（第 7-8 周） — **中优先级**

9. **SDK 文档**:
   - 公共 API 指南
   - 迁移指南（面向外部用户）
   - 示例目录

10. **架构文档**:
    - 用最终结构更新 ADR
    - 记录 SDK/平台边界
    - 创建依赖关系图

---

### 阶段 5: 验证（第 9-10 周） — **低优先级**

11. **独立 SDK 测试**:
    ```bash
    # 创建仅使用 alice-engine 的测试项目
    mkdir /tmp/sdk-test
    cd /tmp/sdk-test
    pip install /path/to/packages/alice-engine
    # 编写仅使用 alice_engine 导入的测试脚本
    # 验证在没有 aitest 平台的情况下工作
    ```

12. **SDK 发布**:
    - 将 `alice-engine` 发布到内部 PyPI
    - 将 `alice-governance` 发布到内部 PyPI
    - 将 `alice-discovery` 发布到内部 PyPI

---

## 7. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| **破坏现有工作流** | 低 | 中 | 兼容性别名已到位 |
| **清理过程中遗漏依赖** | 中 | 中 | 删除前进行全面的 grep 审计 |
| **SDK 不是真正独立的** | 低 | 高 | 通过独立项目测试验证 |
| **性能回归** | 低 | 低 | 没有架构更改，仅清理 |
| **团队速度下降** | 低 | 低 | 最少的新代码，主要是删除/移动 |

---

## 8. 成功标准

### 必须具备

- [ ] SDK 从 `aitest.*`（测试除外）**零导入**
- [ ] 平台/CLI 通过**仅公共 API** 导入 SDK（没有 `aitest.engine`、`aitest.graphs` 等）
- [ ] 没有**重复**模块（`aitest/discovery/` vs `alice-discovery` 等）
- [ ] 没有**弃用**代码（`aitest/llm/providers/` 已删除或记录保留原因）
- [ ] 仅使用 **`alice-engine`** 的独立项目工作

### 应该具备

- [ ] `aitest/runtime/` 归属明确（移至 SDK 或平台）
- [ ] `aitest/graphs/` vs `alice_engine/workflow/` 关系已记录
- [ ] `aitest/audit_engine/` 验证逻辑提取到治理
- [ ] SDK 发布到内部 PyPI

### 最好具备

- [ ] 全面的 SDK 文档
- [ ] SDK 示例目录
- [ ] 面向外部用户的公共 API 指南

---

## 9. 结论

本项目使用兼容性别名和渐进式迁移**执行了复杂的 SDK 提取**。架构**合理**，约 70% 完成。

**关键优势**:
- 清晰的 SDK 结构和延迟导入
- 兼容性别名防止破坏性更改
- 清晰的关注点分离（运行时、治理、平台）

**剩余工作**:
- 清理弃用代码
- 解决 3-4 个目录的归属
- 通过独立测试验证 SDK 独立性

**建议**: 继续进行**阶段 1-2 清理**（2-3 周）。这是低风险工作，将使项目达到 **85%+ 迁移完成度**，并使外部 SDK 用户可用。

---

**报告结束**
